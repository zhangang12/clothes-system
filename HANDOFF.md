# HANDOFF · 会话交接文档

> **用途**：新会话/新接手人**先读这份**，30 秒还原"现在到哪了、下一步做什么"。
> **维护约定（强制）**：**每次变更（commit）都必须同步更新本文件**——至少更新「一句话现状」「立即待办」「最近变更」。仓库已配 `.githooks/pre-commit` 拦截未更新本文件的提交（确无需更新时用 `git commit --no-verify` 跳过）。
> 纵深阅读：红线与操作规范看 [`CLAUDE.md`](CLAUDE.md)；项目全貌/业务规则/事故复盘看 [`docs/项目记忆.md`](docs/项目记忆.md)。

---

## 一句话现状（最后更新 2026-08-26）

> 同日追加：**反馈的「全量查询」可以按账号开通了（King + 主管）**。查下来分两种情况：**主管本来就有**——`RolesGuard` 与前端 `hasRole` 都按「SUPERVISOR 视同 ADMIN」放行，`resolveMenuKeys` 给管理级角色返回全量菜单，接口和侧栏都通，无需改动；**King 是 BUSINESS**，三道都挡着：路由 `meta.admin`、侧栏 `canMenu('feedbacks')`（`feedbacks` 标了 `adminOnly`，不进业务默认菜单）、接口 `@Roles(ADMIN)`。
> 【为什么不是简单放开角色】改成 `@Roles(BUSINESS)` 等于所有业务都能看；把他提成主管等于把整套管理权限一起给出去。系统里其实早有账号级开关 `sys_user.menu_keys`（账号管理里逐项勾选），但它**只管前端**——菜单给了、点进去仍 403。
> 【做法】新增 `MenuGuard` + `@MenuAccess('feedbacks')`，把**同一套** `resolveMenuKeys` 口径接到接口层（两处规则一旦分叉就会出现「侧栏有、点进去没有」）；`JwtStrategy` 的 `select` 顺带取 `menu_keys`（本来每请求就查这一行，不增加查询，且改完菜单**立刻生效**不必重登）；路由 `/feedbacks` 由 `meta.admin` 改为 `meta.menu='feedbacks'`。**只开「查」**：列表与导出走菜单授权，**回复 / 标记已处理仍限 ADMIN/主管**，页面上对只读账号直接不渲染那一列（按钮摆着点下去只会 403）。King 的账号菜单已在生产配好（业务默认菜单 + `feedbacks`）。api jest **465**（+9）；**变异测试 4 次**全部如期变红。

> 最新：**审计遗留的两个独立任务已了结（9-02）**。
> ① **孤儿合同行 cm#41 已改绑**：生产核实 cm#41（335T尼龙涂层/黑色/1350米，HT-20260724-001 生效中）指向已删除的 om#221，而订单 32 同名同色唯一行是 om#244——备份表 `_bk_cm41fix` 留底后把 order_material_id 改绑 221→244，回读断言通过：om#244 的「已订」已亮，**全库悬空引用清零**（LEFT JOIN 扫过，仅此一条；合同 28 另 4 行 om_id=NULL 是手工行，合法）。
> ② **importFromQuote 两缺陷已修**（cm#41 悬空的成因正是「生成合同后重导」）：**a)** 订单名下有未删除合同时拒绝重导（BadRequestException 点名合同号；与 generateFromOrder 幂等守卫同哲学——整表 delete+重插会把 contract_material 的行级溯源全打断）；**b)** 重导不再静默清掉拆分设置——split_mode/size_specs 是订单侧决策、报价里没有对应物，现在品名两边都唯一时原样携带（多行同名不猜，宁可不带）。
> **验证**：api jest **491**（+4）/ tsc / 构建绿；**变异 3 次全红**（拆重导闸、拆设置携带、唯一性检查放水）。前端零改动（报错走既有 errToast），零 schema 变更。

> 前一轮：**#120 B改良版 上线后自审计（8 路对抗式复查）→ 修补一批已落地**。审计结论：订单 73 外科手术干净（合同金额分毫不差、无悬空引用、钱链零引用）、无决策被推翻；但闸有旁路、有三处同根因的 `?? ` 击穿。本轮修补：
> ① **判定规则细化并下沉到 @i9/types 共用**（`findSplitDupConflicts`）：同名多行+拆分时，「各行部位互异非空且颜色全空」= 按部位分摊净耗（订单 42 的正当用法）**放行**；颜色互异（订单 73 式一色一行）或部位/颜色重复的誊行才拦，reason 分三种文案。前端 lineCheck 改为委托这份规则——**前后端一份口径，杜绝两份实现漂移**（#120 事故本身就是漂移的产物）。
> ② **后端三道门上闸**（此前只有前端保存提示，存量脏数据/复制订单/API 直写全都绕过）：`generateFromOrder`、`create` 选订单自动带出材料（两扇钱的门，BadRequestException 点名材料+行号+原因）、`order.service.buildMaterials`（create/update/importFromQuote 共同漏斗）。
> ③ **fp=0 击穿修复**：生产 543/800 行 `final_purchase` 存 0 而非 NULL，`?? ` 跳不过 0——`expandMaterialLines` 两处、ContractEditView `splitLinesOf`/带入默认量、orderPrint/orderExcel 采购量列，统一改「>0 才算有效」。
> ④ **无声降级留痕**：标了拆分却没矩阵时整单兜底，`qty_source` 写 `采购量含损耗·矩阵未分组`（原与「本来就不拆」长得一样，业务看不出没拆成）；带入行 qty_source 按真实来源写（原一律「单耗×件数」）。
> ⑤ **打印/导出颜色列**：分色行印「自动分色」，不再印建单残留的单色（订单 73 的工厂单实翻过车）；DTO `split_mode` 加 `@IsIn` 枚举校验。
> **验证**：api jest **487**（+8）/ web vitest **569**（+16 用例）/ 四包构建全绿；**变异 11 次全部如期变红**（规则本体 5：拆判定/换条件/恒真/every→some/行号；后端 6：三道闸各拆一次、两处 fp>0 改 >=0、降级标记恒 false）。**零 schema 变更**。
> **遗留给用户拍板**：订单 73 幸存三行（om 1025/1026/1028）`final_purchase` 仍是手工时代的 4000/2250/2280，与系统算的 7976/4478/4528 差 ±10% 以上，页面会亮红旗——取哪套值是业务决定。**待拆分独立任务**：孤儿 cm#41（合同28/订单32，om 221 悬空 → om#244 缺「已订」）；importFromQuote 重导会把 split_mode 重置 NONE 且 DELETE 重插会断「已订」。

> 前一轮：**#120 用户拍板「B改良版」并已实施**。用户自己点破了核心：「系统其实分了颜色，但表面看起来没分」——里子（矩阵、展开面板、合同拆行）一直按色分，面子（系统采购量列、颜色格）全看不出，daisy 才会一色建一行导致翻倍。**方案 A/B 之争就此化解为展示层改造**：
> ①「系统采购量」列拆分行直接显示分组明细（`7976` 下挂 `米白11-0602 3988 / 浅棕18-1048 3988`，超 3 组给总数）；矩阵分不出组时**明说**「矩阵未分出组，按整单计」——顺带把审查项 H「无声退回整单」的前端侧堵了；
> ② 按颜色/色码拆的行，「颜色」格由手填输入框改为**「自动分色」标记**——那格不参与计算，且手打的「咖色」对不上矩阵的「浅棕18-1048」（两套颜色词表：矩阵是真名，材料行是外号）；按尺码拆的行颜色仍可填（后端 BY_SIZE 语义确实用它）；
> ③ **防呆闸**（lineCheck 新增 `duplicateSplitGroups`/`duplicateSplitMessage`）：同名材料多行且任一行标拆分 → 保存前点名行号拦下，文案讲清「多建一行=多算一整份」；同名但全 NONE 不拦（一料两部位是正常用法）；
> ④ 合同页两缺陷一并修（8-31 审查项 E/F）：`splitLinesOf` 补 **BY_BOTH**（原来「颜色+尺码」的料带入时静默退回整单一行，key 口径与后端一致用 JSON 化）；带入候选默认量改取 `final_purchase ?? total_purchase`（原来 `net_usage×orderQty` 连损耗都没乘）。
> **算法零改动、后端零改动、历史数据零迁移。** web vitest **562**（+7）/ api 479 / 构建+tsc 全绿；变异 3 次全红。**待办**：订单 73 数据清理（外科手术式：删三张草稿合同里 order_material_id∈{1035,1036,1037} 的重复行并改合同总额、删材料重复行，全程留底）；#120 回复 daisy 时要**顺带问判据**：不同颜色会不会配不同供应商/不同单价——会的话再议升级到 A。

> 前一轮：**9-01 两条反馈（#120/#121，均 daisy），#121 已修，#120 等口径**。
> **#121 分码尺寸输入框每敲一个字符就关**——根因在 element-plus 源码里逐行核实过：`el-table` 对 `:data` 是 **deep watch**（table/style-helper.ts），展开面板里写 `row.sizeSpecs` 每敲一键都触发 `setData → updateExpandRows`，而 **无 rowKey 的分支是 `expandRows.value = []` 直接清空**（store/expand.ts）——面板应声关闭、焦点没了，填 57.5 要重新点开 4 次。修法：材料表加 `:row-key="matRowKey"`，用 **WeakMap 发号**（不往行对象塞字段——会进保存载荷，且渲染期改响应式对象有重渲染循环风险；复制/新建的新对象天然拿新号）。全仓扫过：另一处带展开列的表（CustomsDocsView）面板纯展示无输入，不受影响。**新增守卫** `expand-rowkey-guard.spec.ts`：展开面板里有 v-model 的表必须声明 row-key（变异实测：拿掉 row-key 守卫即红）。web vitest **555**（+2）。
> **#120 两问都还没动，等拍板**：①**采购量分色算错**（订单 O-20260901-001 三张草稿材料合同**多签 202,094.40 元**，未推送供应商；根因是「一行=一个颜色」vs「一行=全订单」两套心智模型相撞，前后端共 8 处同根因）——修法取决于业务口径：同名材料分色多行时，每行到底出该色的量还是全部颜色的量？需问 daisy 或用户拍板；三张错合同怎么处理也待定。②**船期列**：审查确认 `matrix_data` 是 JSON 列、**零 schema 变更**即可加，但前端保存时 pos 是白名单映射（OrderEditView:748 只发 po_no/destination/consignee），不改那行填了会静默丢——用户昨天说不做，daisy 今天又提，待用户重新表态。

> 前一轮：**#119 qiao「用款申请看不到是哪个业务申请的」——数据一直在，只是从没带出来过**。`payment_request.created_by` 从建表起就在写，但列表接口 `enrichNames()` 只补了工厂名与合同号，没补申请人；页面自然没有这一列，财务审批时不知道找谁核对。
> 【改法】`enrichNames()` 里加一次**批量**查账号（与工厂名/合同号同一路子，列表最多 100 行，绝不逐行查）；`COALESCE(NULLIF(real_name,''), username)` 取真名、没真名退账号名；查不到留 `null` 让前端显示「—」，**不伪造成"未知用户"**。该函数同时服务预付款列表，那边一并有了。前端付款申请表在「工厂」后加「申请人」列；**工厂账单导出**也加了这一列（qiao 就是靠它对账），插在「申请日期」之后。
> 【单测抓到一个真缺陷】UT-PR-NAME-4 一开始是红的：早期数据 `created_by` 为空时 `uids` 是空数组，原写法把赋值放在 `if (uids.length)` 里，这些行**连 `created_by_name` 这个键都没有**（undefined 而非 null），与「查不到显示 —」的口径不一致。改为无条件赋值。
> 【构建 tsc 比 vitest 严】新加的 4 处 `exportFactoryStatementExcel(st)` 漏了第二个参数（导出时间戳），vitest 跑得过、`pnpm build` 直接失败——已补。
> **验证**：api jest **479**（+4）/ web vitest **553**（+4）/ 四包构建 + tsc 全绿；**变异 6 次**全红（后端 3：伪造未知用户、改成 N+1 查、赋值放回 if 内；导出 3：表尾少占位、数据行漏列、列插错位置）。**零 DB 结构变更**（`created_by` 早已存在）。

> 前一轮：**8-29 无新反馈；error_log 抓到新流程的第一次实战反馈并已修**。`error_log id=16`：昨天 14:09 有人在**同一张订单 69** 上把「从报价导入」重试了 **10 次**、全被 A7 拒（选了草稿态报价）。两个结论：①#116/#117 那次「先建草稿再导入」的顺序**起作用了**——10 次都打在同一单上，没有重复建单；②导入弹窗把草稿报价也列了出来，等于摆一个必然被拒的选项。已修：弹窗只列 QUOTED/ADJUSTING/ORDERED（`importableQuotes`，与后端 A7 同口径），选项带状态字样，提示写明「草稿报价请先在报价页发出」；**「关联报价单」下拉未动**（老订单可能挂着任意状态的报价，过滤会让已选项显示成裸 id）。web 549 全绿、构建 + tsc 全绿；纯前端过滤无独立单测（口径数组三个值、与后端枚举同源）。**另**：`/uploads/file` 缺参数 400 已累计 ×102（原 ×53），仍未定位到发起方，等业务侧 F12 的 Initiator 截图。

> 前一轮：**8-28 一条反馈（#118 daisy），两个诉求都落了**。
> ①**生产通知单改「每 PO 一行、尺码作列」**——她给了工厂实际在用的样张（I25.230.02757：PO# / 洗标号 / 颜色 / P M G GG / 合计 / 船期）。默认版式是「行 = 款·色·码，列 = PO」，3 色 4 码 6 个 PO 要 12 行 × 6 列数量格，工厂对着 PO 裁剪/包装得跳着找；转置后 6 行看完。新增 `matrixPivotRows()`（导出供测试）+ `matrixPivotTable()`，**只用于 factory 模式**（对客/内部维持原版式，读者是按款·色·码核数的）；空组合不出行（省纸的关键）、洗标号整列为空不占列、有目的地补一列。**「船期」列没做**：PO 维度只存目的地/收货人，船期只有订单级 delivery_date——没有的数据不能编，回复里已说明，要加就是 schema 变更（红线一），等拍板。**fixture 用她样张的真实数字**（合计 3624/4322/3905/3149/15000 分毫不差）。
> ②**订单材料明细加行内「复制」**——「TC底PU 下合同有黑/酒红/咖三色，报价只带了黑色」。**复制必须剥掉 `id` 和已订标记**：行上的 id 是库行主键、后端按 id 原地更新，带着复制等于两行写同一条记录、后存覆盖先存（静默丢行家族的新成员，这次提前掐死）；contracted/contracts 是「已订」绿标，新行没下过单不能继承。
> **验证**：web vitest **549**（+8）、构建 + tsc 全绿；**变异 4 次**全红（转置取错 PO 下标、不跳空组合、合计漏加、尺码被字典序打乱）。**零后端改动、零 DB 结构变更。未做**：没实际打印过，请 daisy 在订单页 打印→生产通知单 走一遍对版式。

> 前一轮：**8-27 两条反馈（#116、#117）同指一处——新建订单找不到「从报价导入」**。Grace 说「5 分钟前还有，再建就没了，系统不稳定」——其实不是不稳定：按钮条件是 `v-if="!readonly && editId"`，**新建页没有 editId 按钮就消失**；她 5 分钟前是在已保存的草稿上。预期流程「先保存草稿→按钮才出现→再导入」没有任何提示，没人猜得到。
> 【为什么按钮当初要求 editId】导入走后端 `POST /orders/:id/import-from-quote`，需要已存在的订单；而字段映射（quote_item_id 关联、「源报价已变更」水位 `quote_synced_at`、材料行展开 `buildMaterials`）**只在后端有一份**。前端照抄映射必然漂移（本周 #109/#110 的教训），所以不做前端映射。
> 【修法】新建页也显示按钮；点导入时**先按所选报价静默建一张草稿**（从报价详情取 `customer_id`，满足 create 的 NOT NULL），**先 `router.replace` 跳到草稿编辑页、再调同一个导入接口**——顺序特意这么排：万一导入被拒（如选了草稿态报价，后端 A7 拦），人已经站在草稿上，**重试不会再建一张单**（若先导入后跳转，失败时留在新建页，再点一次就重复建单）。同组件不同路由不重挂载，跳转后手动 `load()`。弹窗提示补了「新建页导入会先建立草稿订单」。
> **验证**：web vitest 541 全绿、构建 + tsc 全绿。**零后端改动。未做**：没在生产实点（不代输密码）；此流程涉及路由跳转与两次接口串联，**建议让 Grace 实际走一遍**：新建订单 → 从报价导入 → 选一张已报价的单 → 应落在草稿编辑页且明细已带出。

> 前一轮：**待办 0.2 收口——合同/报价/样衣三张导出的数字改成真数值**（#115 只修了订单，这轮把剩下三张一并改完）。
> 【口径】数量 `General`（合同数量带 2 位小数，钉死小数位会平白多 .00）、金额 2 位、单价/耗用 4 位；款号/颜色仍文本。**新增公共出口 `numOr()`**（docExcel）：能转就转数值、**转不了保留原文**——样衣材料的数量/克重是自由文本列，历史可能有「3条」这类值，用 `numCell` 会被丢成空格，这是三张里最容易改出事故的点，已用「脏值保原文」用例钉死。
> 【实现差异】合同/报价走 `exportDocXlsx` 区块，替换 `n2/n4` 即可；**样衣是手搭 ExcelJS**，`bodyRow` 原来把每格钉 `'@'`，现改为识别 `{num,fmt}` 数值格（kv 行的 `val()` 包装也要跳过数值格）。
> 【测试】老断言「toContain('12800.00')」断的是文本表现，改为 `numericCells()`（testKit 新增：value 是 number 且 numFmt≠'@'）；**变异 4 次全红，其中 1 次第一遍漏网**——12800 在 kv「合同金额」也有一份数值，明细合计退回文本时被 kv 那格顶包，已改按**出现次数**钉（≥2）。
> **验证**：web vitest **541**（+3）/ api 475 / portal 19 / 四包构建 + tsc 全绿。**零后端改动、零 DB 结构变更。未做**：没在生产实际导出打开核对（不代输账号密码），请让 YSM 任导一张合同/报价/样衣，选中数量列看右下角求和。

> 前一轮：**深度审查「界面让填、保存静默丢弃」这一类**（用户：「太多同样的问题了」）。不靠人工看，写**机械检测器**扫全站：模板里 `v-model` 能编辑的字段 vs 保存载荷里真发出去的字段，取差集。
> **检测器自身先修了两处缺陷**（否则又是拿错结论办事）：①匹配 `buildDto` 的正则不认返回类型里的逗号（`Record<string, unknown>`），整段载荷没扫到，误报 21 个字段；②看不懂 `...rest` 整体展开，把样衣材料行全判成"丢失"。修完噪音从 21 降到 2，两处经核实**都是误报**（订单那 4 个字段是 `readonly` 只读展示；客户子表走 `clean()` 整行透传）。
> **但顺着误报揪出了真问题**：`clean()` 是**按关键字段过滤整行**——于是换个角度专扫「保存载荷里用 `.filter()` 丢行」的写法，**又查出 4 处**：客户联系人/银行/快递（缺姓名手机 / 缺银行名账号 / 缺公司账号即整行丢）、出口发票款项（金额≤0 即丢，且只有"一行不剩"才提示）。
> **随后加的源码守卫当场再抓 2 处我人工没看到的**：工厂联系人、订单材料行（`filter(m => m.itemName)`，同报价/样衣）。
> **合计这一类已达 7 处 / 9 张子表**：合同货物明细、报价明细、样衣材料、客户×3、出口发票款项、工厂联系人、订单材料。全部改为**保存前点名行号并说明"不改会被丢掉"**；判定一律排除有默认值的列（报价 `lossRate`、订单 `lossRate`）与空数组（样衣 `colorGroups`）。
> **钉死复发**：新增 `utils/__tests__/silent-drop-guard.spec.ts` —— 凡在保存载荷里对集合做 `filter` 丢行的页面，必须用上 `lineCheck` 的提示函数，否则红。**已实测：把订单那处提示删掉，守卫立刻变红。**
> **验证**：web vitest **538**（+10）/ api 475 / 四包构建 + tsc 全绿；**变异 3 次**（不看保留条件、空行也报、提示不说缺什么）如期变红。**零后端改动、零 DB 结构变更。**

> 前一轮：**8-26 四条反馈（#112~#115）**，其中 **#115 又是订单导出的老账、#113 是"界面让填保存丢掉"的第三例**。
> **#115 导出的表格不能求和**（YSM：「计算后任务栏不显示求和」）——`docExcel` 给所有单元格钉了文本格式（款号形如 `I27.230.03929`，不钉会被 Excel 截成 27.23），**唯一的数值出口是 `numCell()`**；`factoryStatementExcel` 用了 23 次，而**四个单据导出器一次都没用**（当初判断"单据自带合计行、无所谓"）。她恰恰要在导出的表里二次核算。已把订单导出的数量/耗用/单价/金额全部包成 `numCell`（款号、颜色仍是文本），空值仍给空单元格（未填 ≠ 0）。**合同/报价/样衣三张导出仍是文本，同一问题还在**（见待办）。
> **#113 拆分选「颜色+尺码」时各码尺寸存不进去**——界面按 `hasSizeDim`（`BY_SIZE` 或 `BY_BOTH`）显示「尺寸」列让人填，而 `buildDto` 里写的是 `m.splitMode === 'BY_SIZE'`，**BY_BOTH 时填的尺寸保存即丢**，合同明细自然带不进尺寸。后端本就支持（`contract.service.ts` 注释写着「BY_BOTH 时按尺码维度取」）。一行条件改成 `hasSizeDim(m.splitMode)`。**这是本周第三例"界面让填、保存静默丢弃"**（前两例：合同货物明细 #111、报价/样衣明细行）。
> **#114 合同"显示不了"——两个原因叠加，均非显示 bug**：①真库核实，该款的 登山布/口袋布/拉链1/拉链2 **压根还没生成合同**（只有裤钩/转移标/主标/洗标/胶袋 5 项生成了）；②门户状态筛选是**精确匹配当前状态**，而流水线是 草稿→已推送→已盖章→出货中→…，她选「已盖章」时那张已走到「出货中」的（`HT-20260818-001`）就消失了。已把选项文案改成「已推送（待盖章）」「已盖章（待发货）」并把占位符写成「当前停在哪一步」，只改文案不动逻辑。
> **#112 发票登记在哪**（business_user）——非缺陷，三处：对账管理新建对账单里的「税率%/发票号/发票金额」（草稿可改）、付款管理新建付款申请里的「发票号/发票附件」、销项走「出口发票」菜单。
> **验证**：web vitest **528**（+7）/ api 475 / 四包构建 + tsc 全绿；**变异 5 次**如期变红（数量退回裸数字、款号误包数值、空值补 0、合计退裸数字、`size_specs` 条件退回 BY_SIZE）。**零后端改动、零 DB 结构变更。**

> 同日追加：**按 #109~#111 三类问题做了一轮举一反三**（用户要求），用**生产真库的列名**当基准扫，不靠印象。
> **①「字段名与接口对不上」**：扫全部导出/打印工具的 `detail.*` 读取 vs `information_schema` 真实列名 → 只揪出 3 个候选，其中 `qtyStats`/`source_order_changed` 经核实是合同详情接口**派生**的（`contract.service.ts:705` 确实返回），不是 bug；`orderExcel` 的 `detail.customer_name` 接口不返回、靠 `?? middleman_name` 兜住，属死表达式。又针对**我犯的那两个具体错误**全仓扫描：`.matrix` 未剥 `matrix_data`、`total_qty` 误名 —— **别处均无残留**。再把四个导出器遍历的集合名（`materials/shipments/items/fees/shipRounds`）与各详情接口返回逐一对照 —— **全部对得上**，只有 `matrix` 那一处错且已修。
> **②「fixture 按臆想写」**：扫导出类 spec 的 fixture 键 vs 真实列名，命中项经核查全是**嵌套对象键**（`pos/rows/net_usage`）或**入参不是该表**（`factoryStatementExcel` 收的是接口汇总对象）造成的误报，无真问题。
> **③「校验不说是哪一行」→ 查出更严重的一类并已修**：报价 `QuoteEditView` 与样衣 `SampleEditView` 保存时都是 `form.items.filter((i) => i.itemName)` —— **没填品名的行被静默丢弃**，页面还提示"保存成功"，且只有当**所有行**都没品名时才报错。空行被丢是对的（表单自动补占位行），但「填了数量/颜色/单价、只漏品名」是人没填完，等于**悄悄丢数据**。工具 `goodsLineCheck.ts` 改名 `lineCheck.ts` 并泛化，新增 `halfFilledRows()`/`halfFilledMessage()`：**点名行号**并说明「不补品名保存会丢掉这几行」。判定时**排除有默认值的列**（报价 `lossRate` 自带统一损耗%，否则空行会被误判），数组列（样衣 `colorGroups`）空数组才算没填。
> **验证**：web vitest **521**（+8）/ api 475 / 四包构建 + tsc 全绿；**变异 3 次**如期变红。**零后端改动、零 DB 结构变更。**

> 最新：**8-25 三条反馈（#109~#111），其中两条是我上周加的订单导出自己的 bug**。
> **#109/#110 订单导出来没有明细、没有配比**（YSM）——`orderExcel.ts` 里**字段名全凭印象写**，与接口实际形状对不上：①搭配数据在 `matrix.matrix_data` 里（接口回的是 OrderSizeMatrix 实体），我直接读了 `detail.matrix`，永远 undefined → 导出一张空表还不报错；②`order_main` 根本**没有 `order_date` 这一列**（是 `make_date`）；③大货总数叫 **`qty_total` 不是 `total_qty`**，那格一直显示 0。打印那边（`orderPrint.ts`）一直是对的，是我加导出时抄漏了。已全部对齐并补上 品名/业务员 两格。
> **为什么测试没抓住（重点）**：我把 fixture 写成了**自己臆想的形状**（`matrix:{pos,rows}`、`order_date`、`total_qty`），于是实现与测试一起错、全绿。现已**按 order_main 真实列名重写 fixture**，并补 UT-ORD-X11b 钉住抬头几格。**教训：fixture 跟着接口走，别跟着实现走。**
> **另一处自伤**：新加的断言最初写成 `expect(s).toContain('500')` 扫全串，而单价 12.5 会格式化成「12.5000」——里面就含 500，把 `qty_total` 改成不存在的列时照样绿。已改为**按字段名精确取那一格**。
> **#111 辅料下单保存不行**（YSM）——报「货物明细：品名必填、数量须大于 0」，可她截图里第一行明明填着「门襟拉链 / 105」。根因是校验用 `find()` 找到任意坏行后抛一句**不带行号**的通用提示，二三十行里没人找得到是哪一行；而空白行很可能正是「插入」「复制选中」「从订单带入」留下的。新增 `utils/goodsLineCheck.ts`：**点名到第几行**、把填的值原样带出来，并把**空行**单独说成「勾选后点『— 删除』」——空行是多出来的、不是填错的，处理方式完全不同。
> **验证**：web vitest **513**（+11）/ 四包构建 + tsc 全绿；**变异 4 次**（三处字段路径 + style_name）全部如期变红。**零后端改动、零 DB 结构变更。未做**：仍没在生产上实际导一次 Excel 核对。

> 同日再追加：**预付款权限放开到业务**（用户拍板）。按房子里既有口径走——**发起类**（建付款申请、改自己草稿）本就是 `ADMIN/FINANCE/BUSINESS`，**动钱那几步**（提交/审批/登记实付/标记已付）只给 `ADMIN/FINANCE`，删除只给 `ADMIN`。于是把 `POST /payments/prepayments` 与 `GET /payments/prepayments/balance` 一并改成 `ADMIN/FINANCE/BUSINESS`（余额必须跟着放，否则业务能登记却看不到余额，等于一笔糊涂账）；**审批/实付/删除一个没动**。前端**新开** `canPrepay`（= `canCreatePR`）只管预付按钮与余额，**不碰 `canEdit` 本体**——`canEdit` 还管着提交/审批/付款那几个按钮，塞进 BUSINESS 等于一次放出几条必 403 的路径（文件里原有的告诫注释已按新事实更正）。**风险边界**：业务登记的预付只是台账余额，钱要真出去仍须走付款申请并由管理员/财务审批，冲抵就发生在 `approve` 那一步。
> **一条老测试被这次改动挡下并已更新**：`PaymentListView.spec.ts` 里原有「BUSINESS 仍看不到创建预付款」是按旧口径写的，现改为断言业务**能**看到，并**补一条「版师看不到」**——放开只到业务，不是放给所有能进这页的角色。api jest **475**（+5）/ web vitest **502**（+1）/ 四包构建 + 三处 tsc 全绿；**变异 4 次**如期变红（创建没放开、审批越权放给业务、删除放给财务、前端 `canPrepay` 退回 `canEdit`）。

> 同日再追加：**那笔 30,420 用户拍板「不迁」**，保持现状。已核实**停用 PREPAY 不会把它卡住**——草稿编辑接口只收 发票号/发票金额/税率/说明，压根不碰 `subType`；提交/复核/退回也都不传，所以那张单照常能改能走流程，列表里显示为「预付款（已停用）」。顺手补上一个**还没爆的 403 口子**：付款页那行「工厂可用预付款余额」调的接口限 ADMIN/主管/财务，而前端 `loadBalance()` 在选中工厂时无条件触发，业务/版师/打样间选一家工厂就会撞 403；现加 `canEdit` 双重保护（不显示那行、也不发请求）。nginx 日志显示该接口**至今一次都没被调用过**（预付款记录本就是 0），故属预防性修复。web vitest 501 全绿。

> 同日追加：**「预付款」两套并存，已收口到付款管理那条**（用户拍板）。查下来系统里有两个同名东西、互不相通：①**付款管理·预付款管理**（`prepayment` 表，有余额、能在付款申请里「冲抵预付款」）；②**对账管理·无合同对账单的子类型 PREPAY**（只是一张对账单）。**要命的是**付款申请冲抵时余额只从 `prepayment` 表算（`getAvailablePrepayBalance` 只读这一张），所以走②记的钱**永远抵不掉**，业务却会以为能抵。
> 【已做】`ReconcileSubType.PREPAY` 标 `@deprecated`、新增 `RECONCILE_SUBTYPE_OPTIONS`（只含 EXPENSE / CASH_NO_INVOICE）供前端下拉与后端校验**共用一份**；DTO 由 `@IsEnum(ReconcileSubType)` 改 `@IsIn(RECONCILE_SUBTYPE_OPTIONS)`，**新建不再收 PREPAY**；枚举值与标签保留（标签改为「预付款（已停用）」）**仅为显示历史单据**；页面上原来手写的两份标签表也收口到共享那份。生产上 `prepayment` 的 2 笔测试数据（工厂名 `F_dt1784551692_12704`、7-20 建、`used_amount=0`、无人冲抵）已删除，整行留底在 `_bk_prepay_20260822`。api jest **470**（+5）；**变异 2 次**如期变红。
> 【**停下来没做**】用户原话要把那笔 30,420 迁进预付款管理，但查明细后发现**它根本不是预付款**：`DZ-费用-008` 摘要与费用行都是「V27.250.25052加工费」，`has_invoice=1`、发票号 `26342000002074108561`、税率 13%、税额 3954.6，ZYT 建的草稿。迁进 `prepayment` 会**丢掉发票号与税额**（该表没有这些字段），还会凭空多出 30,420 可冲抵余额——而这是应付的加工费，不是预付出去的钱。已把证据摆给用户，**建议改为把子类型纠正成「费用」**，等确认。

> 最新：**#108 —— 8-19 那个「搜不到款号」的修复，我改错了地方**（ZYT：「报价从样衣导入，有些搜款号还是搜不到」）。
> **怎么查出来的**：翻 nginx 日志发现发版后 `samples?...size=100` 的请求有 17 次、**带 `keyword` 的一次都没有**——说明远程搜索根本没被触发；再看她的截图，用的是**报价列表页**的「从样衣建报价」弹窗（`/quotes`），而我 8-19 改的是**报价编辑页**里的「从样衣导入」对话框。同一个业务动作在两个页面各有一个入口，我只改了没人用的那个。
> **这次改全并加了防线**：抽出 `utils/remoteOptions.ts`（`useRemoteOptions` + `listParams`）统一口径，四处选样衣/选报价的下拉全部改成远程搜索——报价列表「从样衣建报价」、报价编辑「关联样衣」、报价编辑「从样衣导入」、订单「从报价导入」+「关联报价单」。**并新增源码守卫** `remote-select-guard.spec.ts`：扫所有 `.vue`，凡是选样衣/选报价的 `el-select` 必须带独立 `remote` 属性，缺了就红——**这条守卫当场又抓出订单页「关联报价单」也是漏的**（我自己也没想到还有第五处）。
> **生产条数实测**：样衣 **175**、报价 **117**（都已超过 100，确实在坏）；订单 27 / 工厂 69 / 客户 10 暂未超，写进注释备查。
> **验证**：web vitest **501**（+11）、四包构建 + tsc 全绿；**变异测试 4 次**，其中 **2 次第一遍没红**：①守卫用 `\bremote\b` 判定，而它会命中 `:remote-method` 里的 `remote`，于是「只写 filterable+remote-method」这种正是要防的写法被放行——改成只认独立属性 `(^|\s):?remote(?![-\w])` 后立刻变红；②trim 那次是我 perl 模式没匹配上（变异根本没应用），换 Python 精确替换后如期变红。**后端零改动。未做**：仍没在生产上实际点过。

> 前一轮：**8-19 两条反馈（#106、#107）**，仍是 **零 DB 结构变更、零后端改动**。
> ①**#107 报价「从样衣导入」搜不到款号**（ZYT：「样衣管理里面搜款号能搜到，为什么报价管理从样衣导入搜不到」）——那个下拉是 `sampleApi.list({page:1,size:100})` 拉**前 100 条**、再用 `filterable` 做**本地过滤**；生产样衣已 **172 条**，第 101 条之后（如 `S-20260804-038 / WIA273M502`）在这儿永远搜不出来，而样衣管理页走的是后端 `keyword` 智能搜索，所以那边找得到。改为 `remote` + `remote-method`，输入即调后端（`keyword` 已支持 样衣编号/款号/中间商/买家/版师/制单人），清空输入回落到最近 100 条。**后端一行没动**。这正是待办里那条「全站关联下拉只取前 100 条」在样衣上的实例。
> ②**#106 订单没有导出 Excel**（YSM：「生成合同下载不能转换Excel么」，截图是订单详情页）——样衣/报价/合同早就有「导出Excel」，**唯独订单只有「打印」下拉**，她在订单页找下载入口只看到打印。新增 `utils/orderExcel.ts` + 订单页「导出Excel」下拉。**刻意沿用打印那三套脱敏口径而不是一把全量导出**：订单里同时装着对客价与用料成本+供应商，一份全量 Excel 转手发给工厂＝把两头底价一起送出去。对客不含成本/供应商（且整个用料区不出），对工厂不含客户与价格，内部才全量；文件名带口径（`订单-O-…-生产通知单.xlsx`）好让人一眼看出能不能外发。
> **验证**：web vitest **490**（+11）、api 465、portal 19、四包构建 + 三处 tsc 全绿；**变异测试 6 次**全部如期变红。**过程中测试自己露了个洞并已补**：原来只测了「对客不含供应商」，而对客口径压根不出用料区——真正危险的「给工厂那份带了供应商/成本」没人测，把 `withSupplier` 改成恒真时测试全绿。补 UT-ORD-X11 后该变异立刻变红。**未做**：没在生产上点过这两处（不代输账号密码）。

> 前一轮：**8-14 三条反馈（#103~#105）**，仍是 **零 DB 结构变更**。
> ①**#103 合成的第一轮只带了单号和尺码，数量与寄样日期是空的**（nina：「快递运单号是根据上面填的自动生成的，那数量和寄样时间是否也可以自动生成」）——`findOne()` 给「还没建多轮记录」的样衣合成第一轮时，`qty` 取的是**版师才会填**的 `piece_count`、`ship_date` 取的是顶层 `ship_sample_date`，业务建单那会儿这两个都还空着，于是一行里三格有值两格空，只能照着上面再抄一遍。改为按「版师填的 → 业务填的」兜底：`piece_count ?? sample_qty`、`ship_sample_date || material_ship_date`。
> ②**#104 填了寄样时间列表不显示**（nina）——她填的是**多轮寄样子表**里的日期，而列表那一列读的是顶层 `ship_sample_date`，两边从来没打通。新增 `latestShipDate()`：顶层为空时用**最后一轮**的寄样日期兜底（那一列要回答的是「最近一次什么时候寄的」），人手填的顶层值优先、各轮都没填就保持空。顺带把顶层「寄样日期」也改成能清空（原先 `|| undefined`，清了存不回去，与 #96 同一类）。
> ③**#105 模板下载下来改完传不回去**（YSM：「表格下载保存不了原格式呢？所以订单也上传不了」）——尺码数量搭配的模板发的是 **CSV**、上传框也只收 `.csv`，页面还要求「Excel 另存为 CSV UTF-8」。可她改完是**直接按保存**的，Excel 弹「CSV 不支持多工作表…」后多数人存成 `.xlsx`，于是传不回去；中文 Windows 上还有第二个坑：「另存为 CSV」默认写 **GBK**，我们按 UTF-8 读会整片乱码、界面却只报「款号为空」。改法：**模板改发真 .xlsx**（exceljs 写，数量写成数字、表头冻结），上传 `accept` 放开到 `.xlsx/.csv/.txt`，读取逻辑抽到 `utils/sheetGrid.ts`——xlsx 取第一个工作表、csv 解码出替换字符就**自动换 GBK 再读一遍**、`.xls` 老格式直接给出「另存为 .xlsx」的可操作提示。页面里的 `parseCsv` 一并上移到该工具，原 `csvCell`（只服务 CSV 导出）成了死代码已删。
> **验证**：api jest **456**（+9）、web vitest **479**（+11）、portal 19、四包构建 + 三处 tsc 全绿；**变异测试 9 次**全部如期变红（数量不兜底 sample_qty、日期不兜底 material_ship_date、update 不汇总、汇总改取第一轮、create 不兜底、去掉 GBK 兜底、不拒 .xls、不吃 BOM、空表不报错）。**未做**：没连真库跑端到端（本轮无 schema 变更、只动既有列上的取值逻辑），也没在生产上把这三处点一遍。

> 前一轮：**8-13 一批反馈（#93~#102）全部落地**，本轮 **零 DB 结构变更**（只加 DTO 字段，红线一不适用）。
> ①**#96 材料寄出日期不再自动填今天**（Nina：「当天寄出当天来不及填系统，都是寄出后才有空填」）——原来「推送版师」那一下写死 `today()`，而她通常隔几天才录，等于**系统里每一条寄出日期都是错的**。改为手填优先、不填才落当天；同时补上 `update()` 的字段映射——此前这两列（单号/日期）在编辑保存时被**静默丢弃**，录错了根本改不回来。
> ②**#95 误点「标记已寄出」可以撤**（Nina）——原先一进 SHIPPED 就退不回来，只能重开一张单。新增 `PATCH /samples/:id/undo-ship`：**只允许 SHIPPED → SAMPLING**，并清掉寄样日期（留着就是一个假日期）；已对账/已完成一律不给退（牵扯版师数据与对账单）；记 `UNDO_SHIP` 操作日志。
> ③**#97/#99/#100 合同货物明细能复制、能插行**（YSM）——她的截图里整屏都在手打「同上」：一条拉链按颜色×尺码摊十几行，除颜色尺码外每格都一样。加了行内「复制/插入」和工具栏「复制选中」（她是**勾了行**去找复制按钮的）。**插入方向特意做成往上插**：她的箭头指着第 1 行，往下插永远补不到那个位置，而"补在最后"本来就有「添加行」。复制**连 `order_material_id` 一起带**——那是订单页「已订」绿标的凭据，订单侧按它 GROUP BY，不会重复计数；抹掉反而两行都不算已订。
> ④**#101 报价明细行可拖拽排序**（EVA：「新加的行在队尾，要拉到第二行得一个一个点上箭头」）——**没引 sortablejs**，原生 HTML5 拖放 80 行搞定（首屏包刚从 775KB 压到 480KB，不想加回去）；**只有拖拽手柄可拖**，整行可拖会让人在单元格里选不中文字。
> ⑤**#102 单位改成可搜索下拉 + 可手输**（EVA 点名的 米/公斤/条/副/套/个/只/粒/码/平方米），报价与合同两处一起改；`allow-create` 保证列表外的单位照样能输。
> ⑥**顺手修了两个同表的数据丢失**：合同货物明细的**拉头/拉齿/码带**三列——能填能存，但打开合同时**没读回来**，再保存就被清空；且保存时压根没往后端发，页面上手改一直是白改（只有「从订单生成」那条路写得进去）。`order_material_id` 同样没回传，等于每存一次就把订单侧的「已订」标记洗一遍。
> **验证**：api jest **447**、web vitest **468**、四包构建 + 三处 tsc 全绿；**变异测试 10 次**全部如期变红（把手填日期改回当天、删掉 update 映射、撤销不清日期、插入改往下、多行复制改从前往后、复制改成共用同一对象、dragover 不 preventDefault、整行可拖、原地放下也回调、dragend 不复位）。**#98 是非系统问题**（King 要微信），**#76 仍缺业务口径**（需先回答四个问题才能开工）。

> 8-08 那轮：**新增独立菜单【AI工具集】，落地场景 1「清关单据生成」——一份 PO 一次出齐箱单 / 发票 / 装柜计划三份**。本轮是 **AI 落地性专项**，只做前端、**零 DB 变更、零 API 变更、不进单据链路**（红线一/红线二本轮不适用）。业务背景：一次出运有 4 份表，只有工厂给的**采购合同 PO 是源**，另外三份今天全靠人拿计算器抠，最容易出的错就是**三份件数互相对不上**——本工具三份共用同一次解析与同一次装箱，天然同源，界面上还有一条「三份件数互校」自检。
> **三份各自的口径**（全部是拆客户真实件反推的，不是拍脑袋）：
> ①**箱单** ← 装箱结果。三条硬规则：**箱号在「同一定单号的同一款号」内从 1 重排**（真实件里 ELA263F553-11 编到 46 箱、下一款号又从 1 起；装柜计划的 CTNS 正是按款号统计，两边对得上）；同款**先排完所有尺码的整箱、再排尾数**，故同一尺码会出两行（整箱行+尾箱行），不是重复数据；**拼箱续行只有第一行写箱号**、箱数/体积/重量留空计 0，否则一箱会被下游重复统计。用户指定「假设 1 箱装 6 件」，实现成可改参数（尾数支持「各自成箱/同款拼箱」）。
> ②**发票** ← **不经装箱**，行粒度与 PO 明细一一对应，单价/金额直接取 PO。保留真实件里的「订单数/差额」核对列：本工具由 PO 生成故差额恒为 0，真短装时改 QTY 差额立刻露出来。
> ③**装柜计划** ← 装箱结果按「定单号+款号」汇总，层级 **收货国块 → 定单块 → 每款一行**，每定单一行小计、每收货国一行合计、末尾总计，单证/柜号/卸货港在国家块内纵向合并。**PO 上没有的两样**：收货国（按单号里的目的地代号预填 RS→SERBIA / BH→BOSNIA / EC→ROMANIA / WC→SLOVAKIA，逐单可改，猜不出留空绝不瞎填）；报关归类（HS 4 位 → 中文品名 + 10 位商编，默认值从真实装柜计划 364 行统计得来，4 种组合各自唯一）。**顺带修了他们手工件的一处毛病**：真实装柜计划里 ELA263M132-01 同时被归成「男裤子/6203439090」和「男上衣/6201409000」，ELA263G150-01、ELA263G151-71 也各有两种归法——本工具一律按 HS 4 位推，同款不可能出现两种归类。
> **源文件里没有的**（箱规、净重、毛重）在界面上给默认值让人改，默认值也是反推的：58×37.5×37.5cm（单箱 CBM 0.0815625 小数位对得上）、**毛重−净重恒为 1kg**（9.5−8.5 / 7.8−6.8 / 13−12 / 10−9 全部成立）。计重口径给了两种：「按件」尾箱按实际件数算轻一些（默认，讲得通）、「按箱固定」复刻真实件的做法（同款每箱记同一个净重，哪怕没装满），另可按款号覆盖。
> **一个必须保留的坑**：真实 PO 文件里除 10 张「一表一 PO」外还有一张 `Sheet1` 把所有 PO 平铺了一遍（1894 行 / 81917 件），全勾进去件数直接翻倍——界面按「表名 = 表内唯一定单号」自动判定并默认只勾这种，汇总表打标签且默认不勾，另有重复键告警。
> **验证**：web vitest **324 全绿**（新增 36 例守住上述口径）、api jest 352、四包构建 + 三处 tsc 全绿；拿**真实 PO 实跑**——10 张 PO 表全识别、`Sheet1` 正确判为汇总表、10 个定单自动落到 4 个收货国；三份产物用 openpyxl 逐格比对真实件：**发票合计 17532 件 / ¥2,524,701.74 与 PO 自己的 TOTAL ORDER 行分毫不差**；装柜计划的小计行落在 R39/R77/R112、国家合计在 R113、合并区 `K2:K113`/`L2:L113`/`M2:M113`——**与真实件完全一致**；箱单抬头/两行表头/合计块列位置也完全一致。最后在浏览器里走完整条链路（上传→勾选→装箱→三个页签预览）截图确认。
> **同轮追加·把「过程」摊开给人看**（用户：「要用户看的清楚过程才行，不然数据哪里搞错了都不知道」）。本工具最容易出错**且出错还不报错**的地方是「列靠表头关键词猜」——猜歪一列，整列数据全错、界面上毫无征兆。所以每一步都要交代自己干了什么：
> ①**顶部步骤条**：读源文件 → 选定单 → 体检 → 装箱 → 对账，每步一句话带数，出问题的那步标红。
> ②**列识别核对面板**（每张表一个抽屉）：表头行在第几行、每个字段认到第几列、**表头原文 + 前 3 行示例值**、**这列认错会毁哪份单**；列可下拉手改，改完当场重算并报「N 行 / N 件」。附**源文件原文预览**（表头行高亮、已识别列标字段名）。
> ③**跳过行按原因归类**（带 Excel 行号与例子）：合计行 / 说明·条款行 / 款号为空 / 尺码为空 / 数量不是正数 / 空行。**其中「说明·条款行」要特判整行合并单元格**——exceljs 读合并区会把同一句话在每列都返回一遍，于是款号=尺码=数量全是那句条款，不特判就会显示成「款号和尺码都有、只是数量不对」，看的人一头雾水（真实 PO 每张表 7 行中招）。判据：整行去重后只剩一个值。
> ④**源数据体检**：没条码 / 没 HS / 单价为 0 / 件数非整 / 源文件自带金额与「单价×件数」对不上 / 同键重复……按「重·轻」分级，每条写清**会影响哪份单**并给源文件行号。
> ⑤**装箱参数区当场演算**：拿源文件里一条真实明细算给你看，参数一改数就变。
> ⑥**对账页签排在结果区第一位**：PO/发票件数、箱单件数、装柜计划件数三列并排，可按定单号或按款号逐级看、可只看对不上的。**总数一致不代表每个款号一致**，单测里专门造了「A 少 6 件、B 多 6 件」的总数守恒篡改来证明它抓得住。
> ⑦**箱单每行可展开看算式**：「源 50 件 ÷ 每箱 6 ＝ 8 整箱余 2；本行＝整箱段 8 箱×6 件」+ 源文件行号 + 体积/重量怎么乘出来的。
> **这一轮当场就抓出两个真问题**（旧版会静默带进报关件）：**（a）10 张定单表里只有 2 张真的有 HS CODE 列**，1515 行缺 HS → 新增「按同款号跨定单补 HS」（可关，补了多少行、哪些款号补不上都写在界面上），补回 1473 行；**（b）款号 ELA263F585-61 在不同定单里分别写了 6202 和 6204**——**冲突的一律不自动补**，红框报出来让人回源文件改（静默取其一等于替客户瞎报关）。剩 42 行确实没有 HS（含只在一张表里出现过的 ELA253G503-Z2），照实报。
> **补充验证**：web vitest **343 全绿**（customsDocs 单测 20→**55**）、api jest 352、四包构建 + 三处 tsc 全绿；浏览器端拿真实 PO 走完整条链路复核了步骤条、列识别抽屉、跳过行归类、补 HS 与冲突告警、逐款对账、单行算式展开。
> **同轮追加·装箱规格改成按款号（用户拍板：先不动库，做款号预设表）**。起因是拿工具产物跟客户真实三份件逐层比对：**箱数偏差 44.8%**，根因是「每箱 6 件」这个全局假设——客户实际每箱 1~34 件、每箱净重 6.8~12kg **按款号跳**。`PackParams` 用 `specByStyle`（每箱件数/箱规/净重逐款可覆盖）取代原来只能覆盖净重的 `netByStyle`；`CartonLine` 记下本行**生效的**每箱件数，`explainRow` 据此讲算式（否则设过预设的款会解释错）。预设表存 **localStorage**、可**导出/导入 JSON**，业务自己维护一份、跑几票验稳了再谈入不入库。**主数据（本司主体/收货人台账/客户/字典/HS归类）本轮一律不接**，维持纯前端。
> **效果实测**（用客户实发量 + 从其真实箱单反推的 32 款预设）：箱数偏差 **44.8% → 7.7%（按款号预设）→ 3.3%（再开同款拼箱）**；逐款 90 个「定单+款号」里完全一致 26 个、相差≤2 箱 66 个；净重/毛重/体积也都收敛到 **3.5% 以内**。剩余缺口是客户**按尺码**定每箱件数、我们只到款号一级。
> **测出来的一个真隐患，已修**：预设里的「净重」一列含义随计重口径变——按件是单件净重、按箱是每箱净重，**差着一整箱的件数**。实测把「每箱 8.5kg」当成单件净重用，总净重从 2.8 万变 **58 万公斤，而界面一个字都不提示**。三道修法：①预设文件里记 `netBasis`，导入时口径不一致自动切并告知；②文件**没记**口径且带净重时**弹窗问清楚，绝不猜**（老文件/手写文件正是最危险的一类）；③结果区常驻「平均单件净重」哨兵，落在 0.05~5kg 之外就标红并给修正指引。

> 前一条：**8-07 五条反馈全部实现并发版（含一次 schema 加宽，红线一已走完）**——①**#57 合同列表能按供应商/款号模糊搜**（King「合同号记不住」）：供应商名不在 contract 表上，先按名查工厂拿 id 再并进 OR 分支，`findAndCount` 的分页/排序一个字没动；②**#59 客户PO号从矩阵 PO 列一键带入 + Excel 导入后自动带**（King「太多了」）：**查出这条会踩列宽事故**——`customer_po` 是 `VARCHAR(50)`，而他截图里 6 个 PO 拼起来约 90 字符，直接实现就是 `Data too long` 保存必 500；按红线一把列加宽到 255（init.sql → `gen-column-sync.py` 重生成 → **真库验证**：以生产当前结构建临时库→应用 hotfix→与全新装机库逐列/逐索引比对，索引完全一致、`customer_po` 无差异、二次执行幂等，临时库已清理），并且带入时**装不下就整个 PO 不带并明确告知**，绝不截半个单号；③**#58 上传图片慢**（Grace「1-2分钟」）：实测生产传 2MB 只要 **2.9 秒**、后端只是落盘无图像处理，真原因是**整张手机原图（3-10MB）走用户上行带宽**，故加**上传前浏览器端压缩**（长边 2000、>1.5MB 才压、失败一律退回原图）；同条还有「粘贴」——UI 上其实早写着「聚焦后 Ctrl+V」，只有反馈弹窗开了 `globalPaste`，正是她拿来对比的那个；改为所有上传框都进 `pasteStack`，document 单监听按**后挂载优先 + 本框满了让给下一个**派发，弹窗与页面表单不再串台；④**#61 报价明细「统一损耗%」**（daisy，此前 #44 口径不清让她重提，这次说清了是损耗率）：顶部一个输入框，改了就刷全表、新增/粘贴行也用它，**保留每行那列**（拉链和面料损耗本就可能不同）；⑤**#60 材料拆分支持颜色×尺码同时拆**（King）：新增 `BY_BOTH`——**故意不叫 `BY_COLOR_SIZE`，因为 `split_mode` 是 `VARCHAR(10)` 而它有 13 字符**，又是「组合值超列宽」那一类；前端预览与后端 `expandMaterialLines` 用**同一套分组口径**（颜色或尺码缺一个就跳过），`size_specs` 按**纯尺码**取而不是「黑色 S」这个组合键。验证：api jest **352**（+14）/ web vitest **255**（+13）/ 四包构建 + 三处 tsc 全绿；并做**变异测试**（把 size_specs 改成用组合键取、把关键词分支的 `...base` 去掉）确认断言真的会红。

> 更早：**导出件里的图片全线修好（合同 / 报价，接上 8-06 样衣那条）**——`docExcel` 公共层新增**真 .xlsx 出口 `exportDocXlsx`**，与原 `.xls(HTML)` 出口共用同一份 `Block[]` 声明。根因同 YSM 那条：HTML 工作表里图片只能写成 `<img src="data:...">`，而 **Excel 打开 HTML 工作表时根本不渲染 data: URI 图片**——文件里有、界面上没有、还不报错。**举一反三查出报价单导出（`quoteExcel.ts` 款图）是同一条路子的同一个病**，一并改掉。**真机取证而非只跑单测**：拿生产 `/data/uploads` 最大的一张真实照片（2480×3508、1.7MB）跑完整导出链路落盘 xlsx，解包核对 `[Content_Types]` 声明、`sheet1→drawing1→media/image1.jpeg` 关系链、`oneCellAnchor` 锚点落在**照片列 M、材料行 25**（无错位），再用 macOS QuickLook 独立渲染肉眼确认图**真的显示出来**。过程中揪出两个单测看不见的缺陷：**①图被拉扁**——原来把 `ext` 写死成 96×72 横框，竖拍照片直接变形；改为从图像字节里解原始尺寸（`imageSize()`，纯解 PNG IHDR / JPEG SOF / GIF 头，不依赖 DOM，浏览器与 jsdom 行为一致）后**等比缩进方框**；**②图往上飘压住表头**——标题/表头行没写死行高，渲染器按「自动行高」各算各的；现钉死标题/表头行高（数据行仍自动，否则长备注会被切）。重渲染确认图规规矩矩落在自己那格。附带：`toDataUrl`/`splitDataUrl` 从 `sampleExcel` 上移到公共层（原来是**公共层反过来 import 具体单据**）；`downloadXls` 统一改为延后 revoke（立刻 revoke 在移动端 Safari 会让下载拿不到数据）。**未做且已登记**：门户合同导出仍无照片列（门户是移动端包，为一列照片引 ~1MB 的 exceljs 不划算，且白名单口径是「屏幕上没有的文件里也不该有」）；对账/付款/结算三张单没有图片列，继续走 `.xls`，**要迁只需把 `exportDocExcel` 换成 `exportDocXlsx`，区块声明一字不动**。验证：web vitest **242**（+22，含用真实生产照片尺寸做的等比断言、DHT 干扰段解析、以及**变异测试**——把代码改回旧的 data-URI 写法确认断言真的会红）+ tsc 全绿；**零 schema 改动、零后端改动**。

> 前一条：**8-04 十三条反馈：第一批 11 项已修并发版；诊断出的 94 条隐患清单已入库待续**——用 14 路并行诊断（8 组按主题定位 + 5 路举一反三扫描）产出 **94 条结论**（59 bug / 9 回归 / 13 需求 / 9 非问题 / 4 配置，仅 1 条需动 schema 且是需求类未做）。原始清单见 `docs/反馈诊断-2026-08-04.md`。**本次实修 11 项**：①**#05 中间商必填回归**——7-27「中间商全链可空」只摘了 rules、漏了 `el-form-item` 的 `required` 属性，Element Plus 据此自绑必填规则并报出 `relatedMiddleman is required`（与截图逐字一致）；②客户 CSV 导入仍拿 7-27 已废除的规则整行拒收最终买家；③报价模块整条漏网（前端双必填 + DTO `middlemanId` 硬必填），改为「中间商/最终买家至少一个」，service 按 `middlemanId ?? customerId ?? buyerId` 兜底（`quotation.customer_id` 是 NOT NULL，不能简单放开）；④**#08 存不了单**——样衣材料 `buildDto` 整行原样透传表格字符串（同文件的 `buildRounds` 却做了归一），空串/非数字直撞 `@IsNumber`，现补数值归一 + 提交前行级中文报错（「材料明细第 N 行『数量』填的是『若干』」）；⑤⑥**重复样衣的真根因**——样衣「复制」零 in-flight 守卫、保存的 `saving` 置位排在 `await validate()` 之后，点几次建几条（截图里 S-20260804-028~034 连号 8 条即此），两处都补上早退守卫；⑦**#07** 部位/颜色列对调；⑧**#04 色组被拆**——色组靠逗号串承载、`splitColors` 按 `[，,]` 拆，用户单元格里自己打的逗号被当成组边界；改为导入侧直接产出结构化 `colorGroups`（**一个源列=一个色组，列内永不二次拆**），同时满足 Helen 7-29/7-30 的「跨列要分组」与 Nina 的「列内别拆」，落库格式不变；⑨`extraColorCols` 补「已被其它字段占用的列不得再当色组」；⑩**#13 币种**——新建 `utils/currency.ts` 收口符号/文案，报价页「美金单价×2/报价美元价格/提示文案」改为跟随单据币种；⑪**EXTRA-CUR-3 回归**——订单列表按 `currency === 'RMB'` 判符号，而币种字典发的是 **CNY**，该分支永不命中→人民币订单一律显示 `$`。验证：api jest **334**（+2）/ web vitest **197**（+3）/ 四包构建 + 双 tsc 全绿；**零 schema 改动**。**未做（如实登记，见下方待办）**：#01/#11 经诊断是**配置问题**（本司主体档案里公司名仍是开发期种子「I9 服装制造有限公司」、电子章没上传），不该改代码；#02 门户导出 Excel、#03 码带/拉头（源表没有这两列，需按值语义分流）、#09 删除入口（涉及角色口径）、#10（利润率≠损耗率，用户意图需确认）、#12（归因取决于 King 的角色，需线上核）均待定；其余约 60 条 EXTRA 隐患中，**根因级的 `Partial<Dto>` 让 5 个编辑端点整条跳过 ValidationPipe** 风险面最大，需专门一轮评估再动。

> **同日第二批（已发版）**：又修 6 项，重点是**会静默写坏数据**的那类——①**订单用料核算 / 报价明细·费用**的数值列是自由文本框，用户填「若干」这类文本 → `NaN` → **`JSON.stringify(NaN) === 'null'`，静默存成 NULL 且不报错**（订单采购量直接按 0 算，用户以为存进去了）。新建 `utils/numGuard.ts` 收口：`num()` 不外发 NaN，`checkNumericCells()` 在提交前报出「材料明细第 3 行『单件耗用』填的是『若干』」；样衣/订单/报价三页统一走它。②三页的**保存防连点窗口**一并闭合（`saving` 原本置位在 `await validate()` 之后）。③报价「复制」（列表页 + 编辑页）补 in-flight 守卫——确认框只挡同一次操作，关掉再点仍会再建一条（后端 copy 无幂等）。④报价列表「美金总计」写死 `$` 改为跟随币种。验证：web vitest **211**（+14，新增 numGuard 7 + currency 7）/ api 334 / 四包构建 + 双 tsc 全绿。

> **同日第三批（已发版）**：把第二批诊断的 17 条送进**对抗验证**（每条派证伪 + 回归风险两个独立视角）后再动手，结果**验证救回一次真回归**——我原本已改好的「合同页币种/列头跟随 currency」被推翻并**全部回退**：`contract.service.ts:210` 是 `currency: order.currency ?? 'CNY'` 而订单币种默认 **USD**，批量生成的材料合同带着 USD、金额却是人民币（诊断 EXTRA-CUR-8 的真实数据 bug）；**今天写死的「元」在这批单子上恰好是对的**，改成跟随币种后每一行会印「单价(USD)」压着人民币数字，而这是**拿去盖章的法律文件**——等于把静默数据 bug 升级成盖章件上的显性错误。另外币种换 `DictSelect` 会因其 `allow-create` **把新值写回全局字典**（`type=currency` 为报价/订单/报表共用），老单据里的 `'RMB'` 一旦被写进去，CNY/RMB 并存且 RMB 无汇率，等于把当天刚修的 EXTRA-CUR-3 从字典层重新种回去，而字典删除是 ADMIN 专属。此二者**须等用户先回答「材料/加工合同有没有跟境外供应商用外币签的真实场景」**才能定方向。**本批实修 2 项（均属会写坏数据）**：①**订单列表「复制」既无守卫也无确认框，点一下立刻建单**，连点即出连号草稿——补两选确认 + in-flight 守卫；用 `:disabled` 而非 `:loading`（操作列固定 320px + flex-wrap，行内塞 spinner 会让整行按钮重排跳动）。**新写的单测当场抓出我自己的实现缺陷**：守卫「检查」在 `await confirm` 之前、「置位」在之后，两次快速点击会同时穿过检查各自弹框各自通过、真发两次请求；置位已挪到确认框之前、取消时复位。②样衣材料行「🟠生成采购」补会话内守卫 + 「已生成过 XXX，确定再生成一张？」二次确认（一行材料可重复生成多张打样对账单、直通付款）；**后端耐久幂等本轮未做**——每存一次样衣材料行 id 全部重建，没有稳定去重键，做真幂等要给对账单加来源列、撞红线一。验证：web vitest **214**（+3 订单复制防连点，此前该文件 0 覆盖）/ api 334 / 四包构建 + 双 tsc 全绿。

> **同日第四批（已发版）**：再修 3 项。①**订单新建的假客户 ID**——`buildDto` 里 `customer_id: q?.customer_id ?? form.customerId ?? 0`，那个 `?? 0` 把「没填客户」伪装成合法值送进 NOT NULL 列，用户只能看到后端一句英文 400；已去掉兜底，并在 save() 前置用中文拦下。②同处那句 `新建订单建议先关联报价并导入；已按当前信息创建` 是**双重错误**（不是「建议」而是必须；也根本没有「已创建」），删除。③**#06 的真正根因**：`DictSelect` 的 placeholder 是「可回车创建新值」的唯一提示位，订单页 4 个下拉一传短文案就把它盖掉，用户根本不知道能手填——改为「选或填」「目的地·选或填」（列宽仅 100~130px，不能用组件默认那句长的）。**验证内部对「订单是否该允许不挂报价直接建单」给出两个相反方向**（一个主张强制关联报价，一个主张补客户选择器放开），涉及业务流程变更，本轮只做两者一致同意的部分，方向待用户拍板。web vitest 214 / 四包构建 + 双 tsc 全绿。

> **同日第五批（已发版）**：对抗验证工作流 35/35 全部跑完，其施工单指出 HEAD 已前移、5 条已落地或已被有意回退（勿重复施工）。本批再修 3 项。①**#12 的真答案不是「入口藏得深」而是权限门写窄了**：后端 `POST /payments/requests` 是 `@Roles(ADMIN, FINANCE, BUSINESS)`、注释白纸黑字写着「业务可发起无合同付款」，前端却按 `canEdit`（仅 ADMIN/FINANCE）关掉了「新建付款申请」——业务能进页面能看列表，就是建不了单。新增 `canCreatePR` 单独放行 BUSINESS，**没有把 BUSINESS 塞进 `canEdit` 本体**（创建预付款/提交/付款三个按钮后端都是 ADMIN/FINANCE，那样等于一次放出三条必 403 的路径）；并给预付款余额 watcher 加 `canEdit` 早退——`GET prepayments/balance` 是 ADMIN/FINANCE 专属，本地 catch 拦不住全局拦截器，BUSINESS 一开弹窗就会弹权限红字。②**#09 另一半**：`RuleHint` 原文只说「不可删除可改废弃」，正是 Dean 误以为「废弃能清行」的来源，改为明确区分「废弃=留档改状态、行仍在列表」与「删除=移出列表，仅待派单且未被引用、管理员操作」；批量删除原本把后端 msg 整个吞掉、只提示「拦截 N 条」，改为逐条列出原因。③**修掉一条假绿测试**：`PaymentListView.spec` 有两处断言用的是组件里出现 **0 次**的文案「标记付款」（实际是「付款」），`toBeUndefined()` 恒真——「BUSINESS 看不到付款按钮」这层保护此前是零覆盖。改对文案后 27 条仍全绿，说明保护现在是真的；另补 2 条用例钉住新的建单入口角色门。web vitest **216**（+2）/ 四包构建 + 双 tsc 全绿。

> **同日第六批（已发版）**：补上施工单里剩下的最高优先级一条——**样衣「生成采购」的后端零查重**。`purchaseMaterial()` 此前每次调用无条件发号 + 插一张 DRAFT 对账单；前端会话守卫（第三批）只挡同一次会话的连点，隔天/换人再点照样多一张，而重复单的描述/金额/供应商完全一致，混进复核队列后很难分辨，且 `settlement.classify()` 按 `/打样|样品|样衣/` 归桶会把打样费**重复计入、歪掉毛利**。现加软守卫：description 末尾追加行标识 `[行#id]`，发号前按「同款号 + 该行标识」查已存在的草稿单，命中则 400 并报出已存在的单号。**去重键刻意用行标识而不是 description 整串**——材料明细里「仅部位不同的复制行」品名/数量/单价全同、描述逐字节相同，整串匹配会把第二行的合法采购误挡（UT-SAM-P3 就是钉这个的回归锚点）；用 `style_no` 收范围走 `idx_style_no`，避免在无索引的 TEXT 上全表扫；`打样材料` 前缀一字未动（classify 依赖它）。**如实定性：这是软守卫不是幂等**——①查重与插入不在同一事务，真并发仍可穿透；②样衣 update 是删表重插、材料行 id 每次全换，守卫随之失效。做耐久幂等要给对账单加 `sample_material_id` 来源列，撞红线一，另行排期。当前形态刻意向安全方向失效：宁可漏挡，绝不误挡。api jest **337**（+3）全绿。

> **同日·生产配置改动（非代码，已执行）**：#01/#11 经诊断确认是配置问题而非代码 bug，用户要求代为调整，已改生产库 `company_profile`（改前已备份至 `/data/backups/company_profile_before_20260805_230119.sql`）：①`name` 由开发期种子「I9 服装制造有限公司」→「南京达泰服装有限公司」、`short_name` 由 `I9`→`达泰`；②反馈 #11 附的「合同专用章」（525×509 **带 alpha 透明底** PNG，适合叠印不挡字）按系统自身约定上传至 `/data/uploads/misc/2026/08/d661be3e-….png`（属主 i9app、644），`seal_url` 指向 `/api/v1/uploads/file?p=…`；已验证 API 直连与经 nginx 两路均 200/image-png、md5 与源文件一致。PC 打印走 `company.getDefault()`（is_default=1）、门户走 `is_default DESC, id ASC`，两条链路都会取到这一行。**刻意未填**：`address / tax_no / bank_name / bank_account / legal_rep` 全部留空——它们会原样印在**盖章合同**上，属法律与财务事实，无权威来源不得代填；因此合同「乙方地址」目前仍显示「—」，需用户在「本司主体档案」页补齐。**~~遗留~~已清理（同日）**：`company_profile` 里那行 deep-test 残留 `新主体_dt1784551692`（id=2，`dt`+unix 时间戳≈7-20，与那次 deep-test 清理同批、当时漏网）此前仍出现在合同编辑页「乙方(需方)」下拉中、选中即印出测试抬头。已核实「引用该主体的未删除合同数 = 0」且非默认主体后**软删**（`deleted=1`，三重条件防误伤：限 id=2 + 非默认 + 名字符合测试残留格式）。现可选主体只剩「南京达泰服装有限公司」一个。撤回：`UPDATE company_profile SET deleted=0 WHERE id=2;`另：8-04 批次 14 条反馈中 **11 条已回复并标已处理**，余 3 条（#44 利润率意图待确认 / #51 码带·拉头需源表有该列 / #52 门户导出为需求）保持 PENDING。

> **同日·用户拍板「国内采购一律人民币」，按此收敛（已发版）**：此前悬而未决的「材料/加工合同有没有外币场景」已明确——**没有，国内采购一律 CNY**。据此把 `contract.service.generateFromOrder` 的 `currency: order.currency ?? 'CNY'` 改为**固定 `'CNY'`**：`order.currency` 是**对外销售**的外销币种（默认 USD），而材料合同是**对国内供应商的采购合同**，金额直接取订单用料核算的 `order_material.unit_price`（人民币采购单价，列 COMMENT 写明）、**不做任何换算**——继承过来就是「币种 USD、数字是人民币」的合同，还会一路流到对账/付款/结算，且合同是要盖章的法律文件。**实测隐患就在一步之外**：生产现有 **3 张 USD 订单**，对其中任一张点「生成材料合同」即触发；所幸存量 9 张合同（8 材料 + 1 加工）**当前全是 CNY**，尚未产生坏数据（手工建合同页默认 CNY 且不继承订单，是这条路救了它）。新增 `UT-CON-35` 钉死：USD 订单生成的材料合同必须是 CNY。合同编辑页币种字段加 tooltip 写明口径（未删除 USD 选项——那是业务规则不是技术约束，留作极端情况的人工出口）。**连带结论**：8-03 那轮被主动回退的 `EXTRA-CUR-4/CUR-5`（合同币种放宽到字典全量、列头跟随 currency）**方向确认为错，正式关闭、不再重提**——口径既然是一律 CNY，列头写死「(元)/(RMB)」就是对的。api jest **338**（+1）/ web 216 / 四包构建 + 双 tsc 全绿。

> **同日·门户合同导出（反馈 #52，已发版）**：供应商希望把合同存成文件留档。**PDF 其实一直能出**（打印窗口里选「另存为 PDF」），但门户打印页是 `<body onload="window.print()">`，在微信 X5 等移动浏览器上**静默无反应且页面上没有任何可点的按钮**——用户只会以为功能坏了；现加了屏幕端置顶操作条（`@media print` 隐藏）：一个「🖨 打印 / 另存为 PDF」按钮 + 一句说明，`onload` 也包了 try/catch。**Excel 是真没有，本次新增** `packages/portal/src/utils/contractExcel.ts`。两个关键取舍：①**不引 exceljs**——它单块近 1MB（PC 端实测 938kB），门户是移动端包；沿用 PC 端 `docExcel` 已验证的零依赖做法（HTML 工作表存 .xls，并收掉 UTF-8 BOM 防乱码 / `mso-number-format:\@` 强制文本防款号被当数字截断 / `<x:Name>` 工作表名 三个坑）。实测门户包只涨 **4.5 kB**（ContractDetailView 29.2→33.8 kB），`package.json` 一个依赖没加。②**字段走白名单，逐字段显式声明**——`GET /portal/contracts/:id` 目前是把整个 Contract 实体展开返回的，里面混着 `guarantor_id_photo`（担保人身份证）、`snapshot_json`（盖章快照全量）、审批状态、`created_by`；导出若图省事写个全量落表，等于把这些一次性交给供应商。口径是「屏幕上没有的，文件里也不该有」。**门户由此有了第一批 vitest 用例**（6 条，含专门钉白名单的「绝不导出敏感字段」一条；顺带把 `a.click()` 打桩，免得 jsdom 的 `Not implemented: navigation` 变成未处理错误——本项目踩过一次「表面 passed 实则带 21 个未处理错误」）。

> **2026-08-06 两条反馈（已发版）**：①**#01 YSM「下载保存表格后没有图片」**——查明是**格式限制不是 bug**：样衣导出走的是全站通用的「HTML 表格存成 .xls」（`docExcel` 零依赖路子），图片以 `<img src="data:image/png;base64,…">` 内联；数据没问题，**但 Excel 打开 HTML 工作表时并不渲染 data: URI 图片**——文件里有、界面上就是不显示，且无任何报错。已核实不是 2MB 上限所致（该样衣的图仅 54KB，远未触发退回链接的分支）。**改为生成真 .xlsx**（`exceljs`，图片作为独立媒体条目打包进 zip 并由 drawing 锚定单元格，Excel/WPS 均正常显示）。`exceljs` 本就是 web 的既有依赖（导入功能解析 .xlsx 在用），且一律**动态 import**——构建后仍是独立 938kB 分块，主包不受影响。文件名同步改 `.xlsx`（继续发 `.xls` 会让 Excel 报「格式与扩展名不符」）。测试**改为把生成的文件用 exceljs 读回来断言**（比字符串匹配强得多），其中一条专门验 `wb.model.media` 非空且图片确实锚定到工作表。**同类未修（登记）**：PC 端 `utils/contractExcel.ts` 的材料照片仍是同一条 data-URI 内联路子，同样不会在 Excel 里显示——本轮未动，需要时照此法改。**（已于同日清账：见「最近变更」首条，合同 + 报价一并改走公共层的真 .xlsx 出口，并用真实生产照片渲染核实。）**②**#02 Nina「一个款号需要打2组色时无法导入2组色」**——**不是** 8-05 那次色组改动的回归（已用 `git show` 对证：`/颜色|color/i` 自 f9e8053 起未变）。根因是**系统自己的 UI 全都管它叫「色组」**（材料表列头「颜色（色组）」、按钮「＋色组」、导入预览列「色组1」「色组2」），用户照着系统的叫法把工艺单列命名为「色组一/色组二」，而导入关键词只有 `/颜色|color/`——`色组` 一个都不认，于是两组色永远只能导进一组。关键词扩为 `/颜色|色组|配色|color/i`，并验证不误伤 部位/品名/成份/码带/拉头/尺寸 等表头。web vitest **220**（+8）/ 四包构建 + 双 tsc 全绿。

> 前一条（2026-08-03）：

> 最新：**回滚路径加固 + 剩余 4 处 `grep -q` 竞态清账（「立即待办 6/7」已清）**——①**`rollback.sh` 原来会把服务器打成 detached HEAD**（`git checkout <commit>`），而 `receive.denyCurrentBranch=updateInstead` 只在「推的分支正是检出分支」时更新工作树：detached 之下，下次 `deploy-local.sh` 的 `git push ecs main` 只挪 ref、**工作树纹丝不动**——新产物照常 rsync+重启，源码树却停在回滚版，`deploy.sh` 读的还是旧 `hotfix-schema.sql`，版本错配且零提示。改为停在原分支上 `git reset --hard`（不动未跟踪文件，后续发版仍正常快进）。②**待办里「给 rollback 开 `--skip-build`」的建议是错的，没有采纳**：服务器上的 dist 正是要滚掉的那一版，跳过构建 = 代码回了、跑的还是坏产物，等于没回滚且毫无提示，比 OOM 更糟——构建在回滚里是必需的。③原待办的 OOM 前提**已过时**：真机核实服务器现有 **4G swapfile**（仅用 117M），7-16「无 swap」条件不再成立，风险降为「构建期挤 swap 拖慢 mysqld」；改为**内存 preflight**（可用+swap < 2048MB 即中止并给替代路径）。④**剩余 4 处 `cmd | grep -q` + `pipefail` 竞态**一并清掉（`health.sh` nginx 探活与 redis 容器探测、`clean-db.sh` MySQL 容器探测、`upgrade-db.sh` pm2 探测），nginx 那处顺带去掉 CLAUDE.md 明令不用的 `curl -f` 并把实际状态码带进输出。⑤**更正 HANDOFF 一处不实记载**：「推送凭据在 `.env.local`」——该文件在开发机上**根本不存在**，照着找会扑空；实际是 keychain 无 github.com 条目、SSH 未配公钥，`git push origin main` 需交互输 PAT，且 `deploy-local.sh` 只推 ecs 不推 GitHub（发完版 GitHub 仍落后，需手动补推）。**验证**：4 个脚本 `bash -n` 通过；新 `health.sh` 在真机实跑输出与改前逐项一致（全绿 exit 0）；`rollback.sh` 的分支判定真机取到 `main`（走 reset 而非 detach）、内存闸门用超高阈值实测**在任何改动之前中止**（exit 1，事后核对服务器仍 `6de9954`/`main` 未被触碰）。**未做**：没有在生产实跑一次真回滚——那要把线上退回旧版再滚回来，收益不抵风险；回滚的执行段（reset→deploy）沿用既有路径未改逻辑，改动集中在「怎么切版本」和「切之前拦不拦」。**无业务代码改动**，故未跑四包构建与单测。

> 前一条：**发版失败时打印的回滚 commit 指向"出问题的那一版"，已修（待发版）**——`deploy.sh` 用 `git rev-parse --short HEAD` 记「发版前 commit」，可默认发版路径是开发机 `deploy-local.sh`：**先 `git push ecs main`、后才 ssh 调 `deploy.sh --skip-pull`**，而服务器 `receive.denyCurrentBranch=updateInstead`（真机已核实），push 一到工作树当场就换成新 commit——`deploy.sh` 读到的 HEAD 已经是新版本。实证 8-03 两次发版日志：`部署成功 commit=54ca4c7 (上一版 54ca4c7)`、`commit=bb737b3 (上一版 bb737b3)`，**上一版==当前版**；一旦中途失败，照提示回滚就是回到出问题的那一版，等于**没有回滚路径**。三处改动：①`deploy-local.sh` 在 push **之前**先 ssh 取一次服务器 HEAD，用 `PREV_COMMIT=` 传给远端 `deploy.sh`；②`deploy.sh` 新增 `/var/lib/i9/last-deployed-commit`（**跑完整条部署才落盘**），回滚目标按「上次成功部署记录 → 传入值 → 自己拉码前的 HEAD → `HEAD@{1}`」依次取，候选必须在本仓库存在、且 `--skip-pull` 下不得等于当前 HEAD，实在拿不到就明说「未能确定」而不是打一条假命令；③补 `ERR` trap——`rsync`/`systemctl`/`pnpm` 这类没包 `die` 的命令失败此前被 `set -e` 静默掐掉、一行提示都没有。**沙箱实测 9/9**（本地「开发机+服务器」双仓库复刻 `updateInstead`，ssh/rsync/pnpm/docker/systemctl 打桩不碰真机）：含用修复前脚本复现原 bug、故意让结构升级 SQL 失败、rsync 失败走 ERR 兜底、CI 自拉码路径。**未在生产做失败演练**（要故意让线上发版失败，收益不抵风险），真机只做只读核实。详见「最近变更」首条。

> 前一条：**AUTO 段补上索引同步（「立即待办 4」已清），待发版**——`gen-column-sync.py` 原先把建表体里所有约束行直接跳过，只生成建表和补列，于是 `init.sql` 里声明的索引对存量库**永远补不上**（上一条改动加 `contract_material.idx_order_material` 就是靠手工在 hotfix 手工段写 `_i9_add_index` 才补上的）。现在 `parse_tables()` 除列以外也解析 `KEY` / `UNIQUE KEY`，在 AUTO 段列补齐**之后**（索引依赖列先存在）生成 `_i9_add_index` / `_i9_add_unique`：45 表 / 666 列 / **77 索引**（60 普通 + 17 唯一），与 `init.sql` 声明集合做过程序化比对**完全一致**（不缺不多）。`PRIMARY KEY` 跳过（随建表产生、也不能 `ADD INDEX` 补），多列索引列序原样保留。**同时堵住复发路径**：认不出的约束行（前缀长度 / `DESC` / `FULLTEXT` / 外键等 `ADD INDEX` 表达不了的形式）**直接报错退出**，不再静默丢弃——这次的坑根源就是"静默跳过"。**红线一已验**（本机无 MySQL/Docker，在服务器临时库验，生产库全程只 `mysqldump --no-data` 读结构）：以生产当前结构为老基线 → 升级前实测**正缺那 5 个索引**（`customer.idx_type`/`factory.idx_type`/`feedback.idx_user`/`reconciliation.idx_style_no`/`settlement.idx_style_no`）→ 应用 hotfix 后与全新 `init.sql` 库做 information_schema 索引 diff **缺口为空、也无多余索引** → 二次执行**幂等**（结构零变化、零报错）→ 临时库已 DROP。**唯一非空差异是既有历史漂移、非本次引入**：`customer.grade` 生产为 `NOT NULL DEFAULT 'B'`、HEAD 为可空——`_i9_sync_col` 只比 `COLUMN_TYPE`（两边同为 `enum('A','B','C')`）看不见可空性/默认值差异，已登记待办。**无代码改动**（只动生成器 + 其产物 SQL），故未跑构建/单测。

> 前一条：**8-03 两条反馈已实现，待发版**（King 提交，页面 `/contracts` 与 `/orders/32/edit`）——①**合同类型文案统一为「材料合同」**：枚举值仍是 `MATERIAL`，纯展示文案；代码里本就不一致（`contractExcel.ts`/`ContractEditView`/`ContractPicker` 早已叫「材料」），只剩 web 合同列表（筛选下拉 + 类型列）与门户列表/详情 4 处还叫「面料合同」，一并改齐；打印件标题「原料/辅料购销协议」「委托加工合同」是法律文书名，不动。②**订单里标出哪些材料已生成合同**（「未标颜色表示没有订」）：`contract_material` 新增 `order_material_id` + `idx_order_material`，**精确到行**（用户在匹配口径上拍板要精确关联，不用品名猜）；生成合同时 `expandMaterialLines` 写入（分色/分码展开的多行都指回同一条订单材料），合同编辑「删了重建」明细时按 品名|颜色|尺码 接力该列防标记被冲掉；订单详情接口按行返回 `contracted`+`contracts`（合同号），订单编辑页材料行**浅绿底 + 品名列「已订」标签**（不单靠颜色传达信息）。**连带修了一个隐患**：`order.update()` 原本整表 delete+重插 `order_material`，每存一次订单行 ID 全变——新外键会立刻悬空、标记全丢；改为「带 id 原地更新、只删本次未提交的行」，并加越权守卫（只认属于本订单的行 ID）。存量合同由 hotfix 一次性按「同订单+品名+合同供应商」幂等回填。**红线一已验**（本机无 MySQL/Docker，改在服务器临时库验：以生产当前结构为老基线→应用 hotfix→新列✓/索引✓、与 init.sql 完整性 diff 无本次相关差异、二次执行幂等✓，临时库已清理，生产库只被读结构）；新增原生 SQL 与回填 SQL 也在真库跑过样本数据验证。api jest **332**（+5）/ web vitest 194 / 四包构建全绿。详见「最近变更」首条。

> 前一条：**门户打印合同 + 工厂联系人子表补列完成，待发版**——①**门户打印合同**（供应商截图诉求「电脑登门户要能打印一张合同出来，纸质盖章后再上传盖章照片」）：门户合同详情底部加「🖨 打印合同（纸质盖章用）」按钮，弹 A4 版式（与 PC 端 contractPrint 同模板：甲乙方落款/货物明细/付款条件/条款/担保条款/电子章贴图）并自动唤起浏览器打印；后端 `portal.service.getContract` 补返回 `factory`（本厂名称/地址/联系人/章图，锁 token 的 factory_id）与 `company`（本司默认主体），只 select 打印所需字段。未盖章时落款提示「打印本页纸质盖章后，拍照回传门户『纸质盖章照片』」，正好接上纸质盖章流程。②**工厂联系人子表补列**（Grace 反馈「电话也填写不了」）：设计稿子表 7 列（姓名/部门/职务/电话号码/手机号码/电子邮件/备注）实现只有 4 列，补部门/职务/电话号码三列（badge 4→7 列）；后端实体/DTO/首行回填早已支持 phone，纯前端漏列。省市不全问题远端 f9e8053 已修（全国补全+allow-create），本地重复版本已弃。验证：api jest 全量过（portal 20/20）、portal vue-tsc 0 / web 工厂单测 4/4。无 schema 变更。详见「最近变更」首条。

> 前一条：**导入预览「颜色合并一列」已修复并发版上线（commit `a6a80be`，ecs+GitHub 均已推送，health 全绿）**——Helen 反馈（7-30）导入对话框的预览把 颜色一/颜色二 合并显示在一列：①预览改为**色组动态分列**（色组1/色组2…各自一列，随源列自动扩展）；②颜色组拼接**取消去重**——源表两列同值也保留两列结构（她的工艺单就是 颜色一/颜色二 两列结构，去重会把 色组二 抹掉）。Playwright 实测：上传含 颜色一/颜色二 的 xlsx，预览表头显示「色组1 | 色组2」且值分列 ✓。反馈（#38）已回复。vue-tsc 0 / vitest **194**（+3）全绿。详见「最近变更」首条。

> 前一条：**导入解析「举一反三」已发版上线（commit `83216b2`，ecs+GitHub 均已推送，health 全绿）**——针对 7-29 工厂导入全空行问题做全站同类排查：新公共解析器 `utils/parseTable.ts`（CSV/TSV/分号自适应 + 引号包裹内嵌逗号/换行安全 + BOM/\r 处理）统一收口 4 处此前各自写 split 的实现——①`CsvImportDialog`（5 模块导入）；②报价明细 Excel 粘贴 `pasteRows.ts`（旧实现遇单元格内嵌换行断行）；③报价/订单历史导入 `split(/\t|,/)`（名称含逗号即错列，且首行像表头自动跳过）；④工厂导入**省市后缀归一**（「广东省/东莞市」全称按列表驱动匹配成 广东/东莞，不写死后缀防 吉林市 误伤）。vue-tsc 0 / vitest **191**（+9 解析器用例）全绿；Playwright 实测 Helen 原数据全列解析通过 ✓。（发版当日晚间 SSH 被服务器防暴力机制短暂限流，恢复后自动补发完成。）

> 前一条：**7-29 三条反馈已修复并发版上线（commit `299b367`，ecs+GitHub 均已推送，health 全绿）**——①**工厂批量导入全空行根治**（Helen）：`CsvImportDialog` 此前只认逗号分隔文本，Excel 直接复制是 **Tab 分隔**、上传 **.xlsx** 是二进制——两条路都解析成全空行报「厂商名称必填」。现共享导入组件支持 **xlsx 直接解析 + Tab 分隔**，工厂/客户/样衣/报价/订单五处导入同时受益；真机实测 Helen 的 TSV 数据「通过 1 行」✓。②**色组分列**（Helen）：样衣材料颜色从单个逗号串输入框改为**色组动态多列**（色组一/色组二各自一列、可「＋色组」扩到四五个、可逐列删），存储仍逗号串零 schema 改动，表格导入的颜色组自动分列展示。③**合同日期语义**（Grace）：「最后发货日」（整单出货截止日）与每行「交货期限」（该行材料应交日期，默认订单交期−45 天）本就是两个字段，加 tooltip 说明防混淆。**3 条反馈（#35-37）已逐条回复；3 条历史报错（7-14/7-22/7-27，均已修未复发）已标「已处理」**。vue-tsc 0 / vitest 182 全绿。详见「最近变更」首条。

> 最新：**「举一反三」系统性扫查完成，20+ 项同类隐患全部修复并发版上线（commit `b5ebfae`，ecs+GitHub 均已推送，health 全绿）**——用户「这个系统的 bug 太多了，排查是否有类似问题」：按已修反馈的四类模式三路扫查。**①空串/null 写列 500 类**：合同编辑账期等 NOT NULL 字段 el-input-number 清空给 null 裸写 500（NON_NULL 守卫）；出口发票/收汇日期 '' 500（|| null 归一）；分批付款 pay_date/pay_method 守卫；退税「留空按预估」被 +'' 清 0 矛盾；样衣主档 sampleQty/patternmakerId '' 归一。**②字段长度类**：颜色链 quotation_item/order_material/contract_material.color + contract_material.spec 放宽到 VARCHAR(200)（7-28 双色组合并放大，>50 即 Data too long 500）；费用事由 expense_name DTO 补 @MaxLength(200)。**③下拉/批量/派生类**：省份补全全国 34 省级行政区+可手填；工厂联系人姓名与电话同款可写；报价/订单币种统一走 currency 字典（原 5 项/2 项硬编码三处口径不一）；订单材料加批量设置供应商/单位/损耗；导入供应商按名称自动匹配回填 ID；导入对话框明示图片不导入。**④打印导出类**：样衣打印适配寄样多轮+轮次表；报价打印补贸易国别/结汇/价格条款/备注说明、列表/批量打印带款图、导出 Excel 嵌图+补字段；订单打印 PO 列头补收货人+佣金+附件档案区（彩稿嵌图/非图清单）；合同打印+导出材料照片列；对账/付款/结算/合同敏感附件（发票/水单/纸质章）导出裸 URL 死链统一改标注「🔒系统内查看」。生产报错记录核查：3 条历史 500 均已修未复发。api jest 327 / web vitest 182 / 双 tsc 0 错；红线一真库验证：升级✓/diff 空✓/幂等✓；真栈冒烟：合同 null 账期保值 ✓、发票无日期入库 ✓、55 字符颜色落库 ✓。详见「最近变更」首条。

> 前一条：**7-28 导出件 7 条反馈全部实现并发版上线（commit `f9e8053`，ecs+GitHub 均已推送，health 全绿）**——①材料明细加**克重列**（`sample_material.gram_weight`，表格/打印/导出/导入映射全链贯通）+ 材料行 ↑↓ 调序；②样衣制作单打印基本信息加**样衣尺码/数量**、底部嵌**样衣照片图稿**，导出 Excel 照片转 base64 内联离线可看；③材料**批量设置供应商**（勾选行一次性应用）；④**两组颜色合并导入**（颜色一/颜色二两列自动合并去重，克重进导入映射）；⑤**部位 >50 字符保存失败根治**：三表 part 放宽到 VARCHAR(200)（Helen 89 字符实证）；⑥省市数据补全（江苏 7→13 市全量，全国 19 省全地级市，城市下拉 allow-create 可手填县市）+ 工厂「联系人电话」从只读派生改可直接填写（写回首行联系人）。schema 红线一已验：升级✓/diff 空（45表/709列）✓/幂等✓；生产探针：89 字符部位+克重落库 ✓（探针样衣已删）。api jest 327 / web vitest **182**（+11）/ 双 tsc 0 错；**7 条反馈（#28-34）已逐条回复并标已处理**。详见「最近变更」首条。

> 前一条：**分码材料「各码尺寸」已发版上线（commit `478ea63`，ecs+GitHub 均已推送，health 全绿）**——拉链/织带等分码材料每个尺码尺寸不同（S码拉链50cm/M码52cm），此前只能记一个「门幅/尺寸」。`order_material` + `size_specs` JSON 列（`{"S":"50","M":"52"}`，空=各码同尺寸，存量零影响）：①订单编辑「分码出量」表新增**可编辑「尺寸」列**（占位=门幅值，不填即通用）；②合同两条生成路径都把各码尺寸带进合同行 **size 列 `S(50)`**（后端 generateFromOrder + web 从订单带入选材对话框，同公式同口径）；③生产通知单分码材料下带「各码尺寸：S=50 / M=52」子行。schema 红线一已验：hotfix 升级✓/与 init.sql 集合 diff 空（45表/708列）✓/幂等✓，生产库列已就位✓。api jest **327**（+UT-CON-24）/ web vitest 180 / 双 tsc 0 错；本地真栈全链冒烟：建单→size_specs 落库✓→拆单生成合同→合同行 `S(50)/M(52)/L/XL(58×0.8)` ✓。详见「最近变更」首条。

> 前一条：**表单本地草稿功能已发版上线（commit `bad8469`，ecs+GitHub 均已推送，health 全绿）**——解决用户「填了半天数据保存报错/误关页面就白填」：新 `utils/formDraft.ts`（输入防抖 800ms 自动写 localStorage、按路由分 key、打开页面检测到草稿弹窗询问恢复/丢弃、保存成功清除并停止写回防复活、卸载兜底写一次、hooks 支持合同 form+terms 双对象），已接入**样衣/报价/订单/合同**四张大单编辑页。web vitest **180**（+9）、vue-tsc 0 错；**Playwright 真机冒烟 6/6**：输入→自动落盘→刷新→弹恢复窗→两字段回填→保存成功草稿清除。纯前端零接口零 schema。详见「最近变更」首条。

> 前一条：**7-27 导出件 1 条生产报错 + 9 条用户反馈全部修复（commit `b819503` 已推 GitHub；⚠️发版卡在 hotfix 步）**——报错：`PUT /samples/:id` 500 `Incorrect decimal value: '' for column 'qty'`（17 次，材料行数值空串直写 DECIMAL，`buildMaterials` 统一 num 归一 qty/refPrice/actualUsage/supplierId/sortOrder）。反馈落地：①中间商全链可空（样衣新建/编辑 + 客户表单「买家必须关联中间商」前后端校验一并放宽，直接客户选最终买家即可，后端 `middlemanId ?? customerId ?? buyerId`）；②材料「复制行」按钮（勾选整行复制）；③材料表格导入大补强：映射字段 5→15（补门幅/成份/供应商/参考价等，门幅导不进来的根因）、表头前 5 行自动识别（Grace「共 16 行暂无数据」根因：首行是大标题）、xlsx 多工作表自动挑最像材料表的一张、.xls 给「另存为 .xlsx」明确提示；④字典下拉占位提示可手填（原本就支持回车创建，用户没发现）。daisy 的「附件在线预览」经核实 PDF/xlsx/图片本就能预览，只有 .xls 老格式受技术限制只能下载（已提示）。api jest 326 / web vitest 171（+4）/ 双 tsc 全绿；本机无真库未跑 deep-test。**发版状况：服务器执行 hotfix-schema.sql 失败（该文件自 7-20 未变、上次发版成功，原因待查；失败点在换静态+重启之前，生产仍在旧版无损）。已推 `infra/scripts/diagnose-hotfix.sh` 排查脚本 + deploy.sh 失败时露出真实 SQL 报错（此前被 >/dev/null 吞掉）。**详见「最近变更」首条。

> 前一条：**SUPERVISOR（主管）权限视同 ADMIN（用户拍板），已发版上线（commit `60de941`，ecs+GitHub 均已推送，health 全绿）**——`isAdminRole()` 收口在 `@i9/types`：RolesGuard 凡放行 ADMIN 的端点主管均可访问（基础资料增删改/财务域/系统域/各类删除一次全覆盖）、机密客户行级过滤放行、菜单恒全量 17 项；移除「主管限指派 5 角色/不能碰管理账号」防线（最后管理员保护+不能停自己兜底保留并对主管同样生效）；web `hasRole(ADMIN)` 对主管为真，约 20 处 isAdmin 按钮自动解锁。无 schema 变更、存量账号无需数据迁移（menu_keys 被恒全量覆盖）。生产探针：主管访问管理员专属 `/error-logs` 200 ✓（此前 403）。api jest 326 / web vitest 167 / 双 tsc 全绿。详见「最近变更」首条。

> 前一条：**新 3 条用户反馈已全部实现并发版上线（commit `89c79c2`，ecs+GitHub 均已推送，health 全绿）**——①报价编辑改最终客户/中间商后**名称快照联动刷新**（此前只改 id 不改快照列，列表恒显旧名）；②报价**撤回调整**：`PATCH /quotes/:id/revert`——已报价直接回客户调整（清审批）；已成单须关联订单全为草稿，草稿订单随报价一并软删（非草稿报出单号拦截，可先在订单侧撤回）；列表「撤回调整」按钮带二次确认；③材料合同从订单带入对**分码/分色材料按订单尺码矩阵自动拆行**（与后端 generateFromOrder 同公式，整数类单位向上取整），选材对话框显示拆行预览+拆分标记。api jest 326（+7）/ web vitest 166 / 双 tsc 0 错；本地真栈全链冒烟全过；生产探针 revert 路由就位 ✓；**3 条反馈（#16-18）已在系统标「已处理」并逐条回复**。详见「最近变更」首条。

> 前一条：**6 条用户反馈已全部实现并发版上线（commit `9db91e2`，ecs+GitHub 均已推送，health 全绿）**——①订单「已下单」可**撤回**回草稿改（`PATCH /orders/:id/revert`，CONFIRMED 限定、清审批、已生成合同起不可撤；删草稿单时报价无他单引用自动解锁回「已报价」）；②材料合同从订单带入改**勾选选材**（不再全部带出）+ 数量默认 **单耗×订单件数** 可改；③新建报价默认汇率 6.5/利润率 10%；④材料类表格全局 `v-keynav` 键盘 ↑↓←→ 单元格导航（报价明细/费用/样衣材料/合同材料/订单矩阵/订单材料 6 张）；⑤样衣材料支持**上传工艺单电子表格**（.xlsx/.csv，列映射自适应+预览，兼容工厂自有格式）；⑥报价明细行 ↑↓ 调序。api jest 319（+4）/ web vitest 166（+13）/ 双 tsc 0 错；本地真栈冒烟 6 步全过；生产探针 revert 路由就位 ✓；**6 条反馈（#10-15）已在系统标「已处理」并逐条回复**。详见「最近变更」首条。

> 前一条：**修生产 500——建样衣材料「安排日期」空串写 DATE 列**（报错记录抓到 30 次，`QueryFailedError: Incorrect date value: '' for column 'arrange_date'`）。`sample.service.ts` 三处空串→NULL 归一化（材料 `arrange_date`、创建/更新 `ship_sample_date`），纯代码改动无 schema 变更。api jest 315 全过 + tsc 0 错；本机无真库未跑 deep-test（改动为空值归一，风险低）。**待发版**。

前一条：**账号管理三增强已发版上线（commit `29291b5`，ecs 已推，health 全绿）**——SUPERVISOR 可管账号（限指派 5 种非管理角色，服务端 403 防线）、新角色「船务 SHIPPING」、账号级菜单权限（`sys_user.menu_keys`，NULL=按角色默认；侧栏+路由双拦截，下次登录生效）。生产实测：主管登录响应带 menu_keys ✓、账号管理列表 200 ✓、主管建 ADMIN 被 403 ✓、sys_user 枚举含 SHIPPING+menu_keys 列就位（hotfix 自动升级）✓。GitHub origin 已补推 ✓。详见「最近变更」首条。

前一条：`docs/BUG排查报告-2026-07-19.md` 53 条（8 高危 / 12 中危 / 33 低危）**全量修复并已发版上线**（commit `04c3c74`，GitHub+ecs 均已推送，health 全绿）。对账存量数据兼容同步生效：代码事务内自愈 + hotfix 幂等回填——生产实测回填正确：真实单 DZ-...-003 持有批次，两张 ¥1 测试草稿（DZ-...-001/002）被正确识别为双重申领、提交时将报出占用方拦截，**建议业务在 UI 确认后删除这两张测试草稿**。api tsc 0 错 / jest 306 全过；web vue-tsc 0 错 / vitest 147 全过；portal vue-tsc 0 错。**红线二已闭环**：2026-07-20 对生产真库实跑 `deep-test.sh` —— 371 通过 / 121 失败，逐条归因后确认**零真回归**（失败全为脚本过时/历史 fixture 失效/本次安全加固的预期拦截 + 429 限流）；deep-test 残留测试数据已清理（软删 124 张测试单据、硬删 11 个 `sup_dt*` 账号与 120 行孤儿子表，真实数据零触碰，复核 remaining_live=0）。运行日志归档 `docs/deep-test-2026-07-20.log`。详见「最近变更」首条。

准出审查所有**可短平快做的 P0/P1/P2 + 两个新功能 + 大项「合同两类编辑页」已全部完成、真栈验证、并进 `main`**。已交付:阶段0(列表响应契约/编辑页size/客户银行子表/首建撞S001 + macOS免Docker本地栈+Playwright)、阶段1(上传XSS/审批绕过/超付加锁/分页钳制/结算双重扣减/发票唯一索引)、审计补口(样衣状态机守卫+编辑丢字段/订单状态机/报价门槛A7/报价权限F1/工厂阻删/登录提示/菜单角色门控/Dashboard计数/脱敏显示/批量删确认)、**新功能**(用户反馈悬浮件+管理+HTML导出;系统报错自动记录去重+上下文+HTML导出)、**门户对账→开票→付款闭环**、**合同两类编辑页**(设计稿04 v1.3:材料/加工全字段编辑页+条款模板+价格包含项+担保人+落款盖章+从订单带入+补料/复制+PATCH编辑接口+E5锁定+两套PDF模板;schema +17列走红线一真库验证:升级✓/diff空✓/幂等✓;浏览器E2E 20/20)。
大项②**订单尺码PO矩阵+Excel导入+分色分码**也已完成:PO列矩阵(行=款·色·码,列=PO号/目的地/收货人,TOTAL QTY/各PO合计/总计回填大货总数)+总搭配汇总+CSV模板导入(4步:模板→上传→校验异常行→确认入库)+材料分色/分码出量预览+合同生成按矩阵拆行(分色→各色行/分码→各码行,UT-CON-21~23)+**修复订单编辑从不落库matrix_data的存量洞**;浏览器E2E 14/14。
大项③**门户「我要对账」步**也已完成(设计稿05 v2.2 §C 四步顺序锁定补全):发货批次业务审批(web合同详情通过/驳回)→门户H5勾选已审批批次自动算(合同单价×发货数量)生成DZ单直推PENDING→业务复核确认(已有)→RECONCILED解锁开票;批次被对账占用防重复、删单释放;发货步补快递公司/单号/附件。contract_shipment +7列(走生成器)。真栈全链E2E:推送→盖章→发货→B2拒未审批→批批次→对账250→拒重复→确认→RECONCILED→开票→自动付款申请✓。顺带修:**订单编辑保存清零材料单价**(web不传unit_price,合同价格上游丢失)。
大项④**付款申请账期/到期日+分批付款+结算搜款号**也已完成(设计稿06 v1.1/v1.4):payment_request +账期/到期日/已付总额(创建时从合同带入,应付列表逾期高亮);新表 payment_record 分批付款(方式/日期/金额/水单/备注),多次付款自动累计已付/未付,悲观锁防并发超付,余额=0 整单转已付清并联动对账单 PAID;web 付款弹窗改「财务付款(可分批)」含历史记录表;结算新建改搜款号选订单(选中按款号自动聚合成本)。真栈E2E:批准→付100(余150)→超付200被拦→付150转PAID→再付被拒→记录留痕→对账单PAID✓。
大项⑤**客户机密授权体系**也已完成(设计稿01 v1.3:客户属机密单据):新表 customer_grant(客户×用户,查看/修改两级);行级可见=创建人+被授权人+管理员,覆盖 列表/详情(未授权404防探测)/下拉(下游选客户同样不可见);新建自动为创建人登记权限;批量授权(多客户×多用户,仅管理员,POST /customers/grants)+单客户授权清单/撤销;GET /auth/users 选人端点(仅管理员);web 列表🔒机密列+批量授权对话框+行级「授权」管理。真栈E2E 8步:未授权不可见→授权仅查看(改名403)→升级可改→撤销复隐→非管理员授权被拒→自建自见✓。
**报价行级过滤已补齐**(设计稿「非授权用户在报价中不可见该客户」):quote findAll/findOne 对非管理员按可见客户集过滤——中间商(customer_id)或最终买家(buyer_id)任一指向不可见客户即整单隐藏,详情 404 防探测;真栈E2E:未授权(列表隐/详情404)→授权可见→admin全量✓。合同列表不含客户列(天然不泄露);订单/样衣列表仅显示中间商名称快照(不含联系方式/条款等核心机密),如需彻底遮蔽可按 quote 同法接入(已知边界)。
**审计清单至此全部完成**——所有设计稿大项(合同编辑页/订单矩阵/门户对账/付款分批/客户机密+报价行级)均已交付并真栈验证。
**基建待办三连清(2026-07-10)**:①Redis 硬单点消除——sys_sequence DB 兜底发号(挂了降速不阻断/恢复自动追平切回,真栈杀 Redis 实测五号连续无重号);②GitHub Actions 合并即发版 workflow 模板就位于 infra/ci/deploy.yml(~~待用户复制到 .github/workflows/ 并配 secrets~~ **2026-08-03 用户拍板不做,模板仅留档**);③backup.sh 补上传文件打包(uploads_*.tar.gz)。剩余基建:migration 体系(gen-column-sync 已等效缓解)/deep-test CI 门禁/异地备份+HTTPS+告警(需服务器侧凭据与操作)。

## 系统当前活动状态

| 组件 | 状态 |
|---|---|
| 业务功能 | 8 单据 + 基础资料 + 审计补口 14 项，已上线 |
| 数据库结构 | 生产已用 `upgrade-db.sh` 补齐；`deploy.sh` 已内置幂等升级 |
| 发版 | ✅ 一键 `bash infra/scripts/deploy.sh`（备份+结构升级+就绪自愈+回滚提示）|
| Redis | ✅ 目标 ECS 上**已运行**（`health.sh` 兼容 Docker/原生两种探测）|
| 测试 | 真机 `deep-test.sh` 493/0；API 单测 183/183 |

## 立即待办（按优先级）

0.2 ~~🟠 合同/报价/样衣三张导出的数字仍是文本~~ **已收口（2026-08-27）**：三张全部改数值单元格，新增 `numOr()`（转不动保原文，样衣自由文本列的历史脏值不丢）；样衣的 `bodyRow` 学会认数值格。变异 4 次全红。

0. **🟠 deep-test.sh 脚本适配**（恢复红线二门禁）：2026-07-20 真库运行暴露脚本与现行代码脱节（硬编码 supplier1 账号、fixture 缺 categories 必填字段、订单手动推进自旧提交 3077f42 被禁、P3 起自动开通门户账号、FINANCE 审批、假 PNG 被 magic-bytes 拦截等），需逐集群适配：fixture 补 categories、门户改现场开通账号、报价授权对齐 customer_grant、对账传 contract_id、订单断言适配下游事件、上传改真 PNG。适配完成前 deep-test 结果不可作发版门禁。
0.5 **🔴 8-04 反馈诊断的剩余 ~60 条隐患**（完整清单 `docs/反馈诊断-2026-08-04.md`，2026-08-05 已修 11 项）。按优先级：
   - **根因级**：`Partial<Dto>` 而非 `PartialType` 导致 **5 个编辑端点整条跳过 ValidationPipe**（客户/报价/订单/样衣/工厂）——同一行脏数据「新建 400、编辑 500」的根源，也是 7-27 那次 500 的真因。**改动面大、whitelist 有回归风险，需专门一轮 + 真库验证**。
   - **会挡住干活**：全站关联下拉只取前 100 条且不可手填（生产客户已 103 条、订单 110 条，超出的选不到）；订单新建后端要 `customer_id` 而前端根本没有客户选择项（不走报价就建不了订单）；付款申请要用户手填「对账单ID」但对账模块全程不显示该 ID。
   - **会出错数据**：订单用料核算/报价明细的数值列手填非数字 → `NaN` → JSON 序列化成 `null` **静默丢值**（不报错，用户以为存进去了）；批量生成材料合同时把订单外销币种继承给国内采购合同（USD 合同装人民币金额）。
   - **同类未清**：合同编辑页币种仍硬编码 CNY/USD、合同/门户多处金额写死 `¥`；`DictSelect` 传 placeholder 会盖掉「回车创建新值」的唯一提示（订单页 4 个下拉中招）。
   - ⚠️ 该清单的**对抗验证轮因额度中断未跑完**，动手前请自行核对行号与结论。
1. **🟠 测试基建**：merge-ship 的 UI 真实路径补 Playwright 用例（当前自动化零覆盖）；portal 包建立 vitest 基建；L9 彻底化需上传台账表（schema 变更，评估后排期）。
2. ~~🟡 用户操作·启用合并即发版~~ **用户拍板：不做（2026-08-03）**，从待办移除、别再提。模板 `infra/ci/deploy.yml` 留在仓库里（不删，日后想启用照它走即可），但**不再视为待办**。**须知其后果**：发版继续走开发机 `deploy-local.sh`，而它只 `git push ecs main`（SSH 直推服务器）、**不推 GitHub**——所以**每次发完版都要单独 `git push origin main`**，否则 GitHub 持续落后（8-03 一天内连续三次发版都落后过）。这是既定工作方式、不是疏忽，接手的人照做即可。
3. 🟡 剩余基建：正式 migration 体系（`gen-column-sync.py` 已等效缓解）；`deep-test.sh` CI 门禁；异地备份推送（OSS/rclone）+ HTTPS + 告警（需服务器侧凭据）。
4. ~~🟡 **AUTO 段只补列、不补索引**（2026-08-03 真库 diff 实证）：生产库缺 5 个 `init.sql` 里有的索引~~ **已解决（2026-08-03，待发版）**：`gen-column-sync.py` 加索引同步段，AUTO 段现生成 77 条 `_i9_add_index`/`_i9_add_unique`；认不出的约束行改为报错退出防复发。服务器临时库已验（升级前正缺那 5 个 → 升级后索引 diff 空 → 幂等 ✓）。详见「最近变更」首条。
5. 🟡 **`_i9_sync_col` 只比类型、看不见可空性/默认值漂移**（2026-08-03 索引验证顺带发现）：`customer.grade` 生产是 `NOT NULL DEFAULT 'B'`、HEAD `init.sql` 是 `DEFAULT NULL`，因两边 `COLUMN_TYPE` 同为 `enum('A','B','C')` 而被跳过。当前**不报错**（DTO 里 `grade` 是 `@IsOptional()`，不传就走库里默认值 `'B'`）；风险仅在显式传 `grade: null` 时会撞 `Column 'grade' cannot be null`。修法是让 `_i9_sync_col` 也比 `IS_NULLABLE`/`COLUMN_DEFAULT`，但那会让存量库上一批列开始被 MODIFY，**需先在临时库跑一遍看影响面**再定，别顺手改。
6. ~~🟡 **`rollback.sh` 会在服务器上重新构建**~~ **已处理（2026-08-03）**，但结论与最初的猜测不同：①**「开 `--skip-build` 通道」是错的**——服务器上的 dist 正是要滚掉的那一版，跳过构建 = 代码回了、跑的还是坏产物，等于没回滚且毫无提示，比 OOM 更糟。构建在回滚里是**必需**的。②原待办的 OOM 前提已过时：真机核实服务器现有 **4G swapfile**（`swapon --show`，仅用 117M），7-16「无 swap」的条件不再成立，风险从「OOM 杀 mysqld」降为「构建期挤 swap、拖慢 mysqld」。现改为**内存 preflight**（可用+swap < 2048MB 即中止，并给出「开发机 reset+force-with-lease+deploy-local.sh」替代路径与加 swap 命令）。③**顺带修掉一个更实在的隐患**：原来 `git checkout <commit>` 会把服务器打成 detached HEAD，而 `receive.denyCurrentBranch=updateInstead` 只在「推的分支正是检出分支」时更新工作树——detached 之下，下次 `deploy-local.sh` 的 `git push ecs main` 只挪 ref、工作树纹丝不动：新产物照常 rsync+重启，源码树却停在回滚版，`deploy.sh` 读到的还是旧 `hotfix-schema.sql`，版本错配且无任何提示。改为停在原分支上 `git reset --hard <target>`（不动未跟踪文件，之后发版仍是正常快进）。
7. ~~🟡 **其余脚本里同款 `cmd | grep -q` + `pipefail` 竞态**~~ **已处理（2026-08-03）**：`health.sh` nginx 探活（改为自读 `%{http_code}`，顺带去掉 CLAUDE.md 明令不用的 `curl -f`，失败时把实际状态码带进输出）、`health.sh` redis 容器探测（新 `has_line()`）、`clean-db.sh` MySQL 容器探测（误判会误 die）、`upgrade-db.sh` pm2 探测（改纯 bash 子串匹配）。四处后果都比 deploy.sh 轻（误报 / 误 die / 探测失准，不会静默跳过结构升级），但同一个坑，一并清掉。

**近期已完成**：审计清单全清 ✅；生产缺列事故根治（结构自动同步）✅；Redis 单点消除（DB 兜底发号）✅；backup.sh 含上传文件 ✅；一键 `deploy.sh` ✅。

## 本次会话关键决策（用户已拍板）

- 币种/汇率主数据字典 **不做**；本司主体档案 **做**；出口发票子模块 + 多汇率收汇 **不做**（需求未写）。
- 字段级角色脱敏 **按推荐走**；⑧大货生产 / ⑨船务资料模块 **暂缓**。
- 验收审计：**小+中项全做（14 项）、大项缓做**——已完成。
- **GitHub Actions「合并即发版」不做（2026-08-03）**：模板 `infra/ci/deploy.yml` 留档但不启用，已从「立即待办」移除。发版维持「开发机 `deploy-local.sh`」一条路；因其只推 `ecs` 不推 GitHub，**每次发版后单独 `git push origin main`** 属既定流程、不是遗漏。

## 如何接手工作（速查）

- **先读**：本文件 →`CLAUDE.md`（红线）→`docs/项目记忆.md`（全貌）。
- **开发分支**：`claude/code-pull-7brvqy`；同时推 `main`。
- **推送凭据**（2026-08-03 更正并复核）：GitHub 用 **PAT**（账号密码 2021 起已不收）。文档原写「凭据在 `.env.local`」是错的——该文件被 `.gitignore` 忽略、从未随仓库分发，**开发机上根本不存在**，照着找会扑空。实际走 `credential.helper=osxkeychain`：**钥匙串现已存有条目**（`srvr=github.com` / `acct=zhangang12`，2026-08-03 由用户手动 push 一次后写入），因此 `git push origin main` 现在可非交互直接推。换机器/PAT 过期后需再交互输一次；SSH 未配公钥（`ssh -T git@github.com` → publickey denied），想免密可另配 SSH key 并改 remote。**凭据切勿入库、勿写进任何文档、勿拼进 URL**（会进 shell 历史与进程表）。注意：`deploy-local.sh` 只 `git push ecs main`（SSH，走服务器），**不推 GitHub**——每次发完版仍需单独 `git push origin main`，否则 GitHub 持续落后（8-03 连续三次发版都落后过）。
- **发版**：`bash infra/scripts/deploy.sh`（一条命令）。回滚：`rollback.sh <commit>`。体检：`health.sh`。
- **回滚会在服务器上重新构建**（必需：服务器 dist 正是要滚掉的那版，跳过构建等于没回滚）。脚本已带内存 preflight，不够时中止并给「开发机构建 + rsync」的替代路径。
- **真机测**（改了 DB/集成必做）：容器内 `apt-get install -y mariadb-server redis-server` 起隔离实例 → HEAD `init.sql` 建库 → `NODE_ENV=production` 起 `node dist/main.js` → `bash infra/scripts/deep-test.sh`（应 0 失败）。
- **改 schema**：delta 同时进 `init.sql` + `hotfix-schema.sql`，并用真库验证（红线一）。

## 最近变更（新→旧，保留最近若干条）

- （本次·**#120 B改良版落地**）`fix(web)` 把「里子早就分色、面子看不出」补齐：系统采购量列带分组明细（无组时明说按整单）、分色行颜色格换「自动分色」标记（手打外号对不上矩阵真名）、同名拆分行保存前防呆（`duplicateSplitGroups`，用订单 73 真实数据做 fixture）、合同页 `splitLinesOf` 补 BY_BOTH + 默认量改取已核算采购量。算法/后端/历史数据全零改动。web **562**（+7）；变异 3 全红。**待办**：订单 73 外科清理 + 回复 daisy 并问「分色不同供应商/单价」判据。
- （本次·**#121 展开面板输入框失焦**）`fix(web)` 材料表加 `:row-key`（WeakMap 发号，不污染行数据）。根因在 element-plus 源码逐行核实：deep watch → setData → updateExpandRows，无 rowKey 分支直接清空 expandRows。新增守卫 `expand-rowkey-guard.spec.ts`（展开面板含 v-model 必须有 row-key，变异实测有效）。web vitest **555**（+2）。**#120 未动**：采购量分色口径与船期均待用户拍板（多签 20.2 万的三张合同还是草稿，不急但别拖）。**零后端改动、零 DB 结构变更**。
- （本次·**#119 付款申请显示申请人**）`feat(api,web)` `created_by` 一直在存、只是没带出来：`enrichNames()` 加一次批量查账号（真名优先、退账号名、查不到给 null 不伪造），付款申请列表与工厂账单导出各加「申请人」列。单测 UT-PR-NAME-4 抓出「历史行 created_by 为空时该键缺失（undefined≠null）」并已修；构建 tsc 抓出测试漏传导出时间戳参数（vitest 放行、build 失败）。api jest **479**（+4）/ web **553**（+4）；变异 6 次全红。**零 DB 结构变更**。
- （本次·**导入弹窗不再列草稿报价**）`fix(web)` error_log 实证有人对着「必被 A7 拒」的草稿报价重试 10 次（同一订单，无重复建单——上一轮的顺序设计兑现）。弹窗改列 `importableQuotes`（QUOTED/ADJUSTING/ORDERED，与后端同口径）并带状态字样；「关联报价单」下拉保持不过滤（老订单会挂任意状态报价）。web 549 全绿。**零后端改动**。
- （本次·**#118 生产通知单按 PO 转置 + 材料行复制**）`fix(web)` factory 打印的数量搭配改「每 PO 一行、尺码作列」（`matrixPivotRows`，fixture 用 daisy 样张真实数字核到分毫不差；空组合不出行、洗标号空列不占位；船期列因 PO 维度无此数据未做、涉及 schema 等拍板）；订单材料明细加行内「复制」，**剥掉 id 与已订标记**（id 是行主键、后端原地更新，带着复制＝两行写同一条记录）。web vitest **549**（+8）；变异 4 次全红。**零后端改动、零 DB 结构变更**。
- （本次·**#116/#117 新建订单没有「从报价导入」**）`fix(web)` 按钮原条件 `!readonly && editId`——新建页没有 editId 按钮就消失，预期的「先存草稿再导入」流程无任何提示，Grace 误判成「系统不稳定」。现新建页也显示；点导入时先按所选报价静默建草稿（报价详情取 `customer_id`），**先跳到草稿编辑页再调后端导入**（顺序保证导入被拒时重试不重复建单）；映射仍只走后端那一份（`quote_item_id`/`quote_synced_at`/`buildMaterials`），前端不复制。web 541 全绿。**零后端改动、零 DB 结构变更**。
- （本次·**#115 收口：三张导出改真数值**）`fix(web)` 合同/报价/样衣导出的数量与金额全部改数值单元格（数量 General、金额 2 位、单价/耗用 4 位；款号颜色仍文本）。新增 docExcel 公共出口 **`numOr()`**——能转就转、转不了保留原文，护住样衣自由文本列的历史脏值（「3条」不许被丢成空格）；样衣手搭的 `bodyRow` 改为识别 `{num,fmt}` 数值格。测试从断文本改为断 `numericCells()`（testKit 新增），**变异 4 次全红**，其中 1 次靠「按出现次数钉」才抓住（合计退文本时被 kv 同值数值格顶包）。web vitest **541**（+3）。**零后端改动、零 DB 结构变更**。
- （本次·**深度审查「界面让填、保存静默丢弃」**）`fix(web)` 写机械检测器扫「可编辑字段 vs 保存载荷」，**先修检测器自身两处缺陷**（`buildDto` 正则不认带逗号的返回类型、看不懂 `...rest` 展开）把噪音从 21 降到 2，两处均为误报（`readonly` 展示字段、`clean()` 整行透传）。改从「保存载荷里 `.filter()` 丢行」角度扫，查出客户联系人/银行/快递、出口发票款项；新增的**源码守卫又当场抓出工厂联系人、订单材料行**。这一类累计 **7 处 / 9 张子表**已全部改为点名行号提示。新增 `silent-drop-guard.spec.ts` 钉死复发（实测删掉提示即变红）。web vitest **538**（+10）；变异 3 次如期变红。**零后端改动、零 DB 结构变更**。
- （本次·**8-26 反馈 #112~#115**）`fix(web)` **#115** 订单导出的数字是文本、Excel 求和为 0——`docExcel` 为保住款号把所有单元格钉成文本，数值唯一出口是 `numCell()`，而四个单据导出器一次都没用过；已把订单的数量/耗用/单价/金额包成数值（款号颜色仍文本、空值仍空单元格），**合同/报价/样衣三张同问题未修，已进待办 0.2**。**#113** 拆分选「颜色+尺码」时界面让填的各码尺寸**保存即丢**（`buildDto` 条件写成只发 `BY_SIZE`，界面却按 `hasSizeDim` 显示），改用 `hasSizeDim`；**本周第三例「界面让填、保存静默丢弃」**。**#114** 经真库核实是两件事叠加：那 4 样料**根本没生成过合同**；且门户状态筛选精确匹配当前状态，选「已盖章」会漏掉已走到「出货中」的——已把选项文案改为「已推送（待盖章）」「已盖章（待发货）」，只动文案。**#112** 非缺陷（发票登记三处入口）。web vitest **528**（+7）/ api 475；变异 5 次如期变红。**零后端改动、零 DB 结构变更**。
- （本次·**举一反三：三类问题各扫一遍**）`fix(web)` 以生产真库列名为基准扫描：**字段名对不上**类别处无残留（导出器集合名与各详情接口返回逐一对照全部正确，`qtyStats` 等 3 个候选经核实为接口派生或死表达式）；**fixture 臆想**类命中项均为嵌套键/入参非该表的误报；**校验不点行号**类**查出更严重的一种并已修**——报价与样衣保存时 `filter((i) => i.itemName)` 会把「填了内容但没填品名」的行**静默丢弃**且提示保存成功。`goodsLineCheck.ts` → `lineCheck.ts` 并泛化，新增 `halfFilledRows()`/`halfFilledMessage()`，点名行号 + 说明会丢数据；判定排除有默认值的列（报价 `lossRate`）与空数组（样衣 `colorGroups`）。web vitest **521**（+8）；变异 3 次如期变红。**零后端改动、零 DB 结构变更**。
- （本次·**8-25 反馈 #109~#111**）`fix(web)` **#109/#110 是我上周加的订单导出自己的 bug**：字段名与接口对不上——搭配数据在 `matrix.matrix_data`（我读了 `detail.matrix`）、`order_date` 这列不存在（是 `make_date`）、大货总数是 `qty_total` 不是 `total_qty`，于是导出来配比是空表、抬头几格是 0/空，且**不报错**。打印侧一直正确，是加导出时抄漏。**测试之所以全绿，是因为 fixture 按我臆想的形状写**，已按 `order_main` 真实列名重写并补 UT-ORD-X11b；新断言最初用 `toContain('500')` 扫全串又被「12.5000」蒙混，已改为按字段名精确取值。**#111** 货物明细校验原来只抛不带行号的通用提示，新增 `utils/goodsLineCheck.ts` 点名到行、带出填错的值，并把**空行**单独提示为「勾选后点『— 删除』」。web vitest **513**（+11）；变异 4 次如期变红。**零后端改动、零 DB 结构变更**。
- （本次·**预付款收口到一条路**）`feat(api,web,types)` 系统里两个同名「预付款」互不相通，且付款申请冲抵只认 `prepayment` 表——对账里那个记了也抵不掉。按用户拍板停用对账的 `PREPAY` 子类型：新增 `RECONCILE_SUBTYPE_OPTIONS` 供前后端共用，DTO 改 `@IsIn` 后**新建不再收 PREPAY**，枚举与标签保留（「预付款（已停用）」）仅供历史单据显示，页面上手写的两份标签表收口到共享那份。删掉 `prepayment` 的 2 笔测试数据（留底 `_bk_prepay_20260822`）。api jest **470**（+5）/ web 501 / portal 19 / 四包构建 + 三处 tsc 全绿；变异 2 次如期变红。**零 DB 结构变更**（`sub_type` 是 VARCHAR，不是 DB 枚举）。**未做**：那笔 30,420（`DZ-费用-008`）**没有迁**——查明细发现它是带发票（13% 税、税额 3954.6）的加工费，不是预付款，迁过去会丢发票并凭空产生可冲抵余额；已请用户确认改为纠正子类型为「费用」。
- （本次·**#108 修复 8-19 改错地方的那一轮**）`feat(web)` 靠 nginx 日志定位：发版后带 `keyword` 的样衣请求**一次都没有**，说明远程搜索没被触发；她用的是**报价列表页**「从样衣建报价」，而我改的是**报价编辑页**「从样衣导入」。抽 `utils/remoteOptions.ts` 统一口径，**五处**选样衣/选报价的下拉全部改远程搜索；新增源码守卫 `remote-select-guard.spec.ts`（选样衣/选报价的 `el-select` 必须带独立 `remote`），**守卫当场又抓出订单页「关联报价单」这第五处**。生产条数：样衣 175 / 报价 117 已超 100，订单 27 / 工厂 69 / 客户 10 未超。web vitest **501**（+11）；变异 4 次，2 次第一遍没红（守卫的 `\bremote\b` 会命中 `:remote-method`；另一次是变异模式没匹配上），均已修正后变红。**后端零改动、零 DB 结构变更**。
- （本次·**8-19 反馈 #106、#107**）`feat(web)` **#107** 报价页「从样衣导入」的样衣下拉由「拉前 100 条 + 本地过滤」改为**后端远程搜索**（`remote-method` → `keyword`）——生产样衣已 172 条，第 101 条之后的款号在那个框里永远搜不到，而样衣管理页走后端搜索所以能搜到；后端未改。**#107 同类隐患仍在**（客户/订单等下拉也是前 100 条口径，见待办 0.5）。**#106** 新增 `utils/orderExcel.ts` 与订单页「导出Excel」下拉——订单原先只有「打印」，是四类单据里唯一没有 Excel 导出的；**沿用打印的三套脱敏口径**（对客无成本与供应商且不出用料区／对工厂无客户与价格／内部全量），文件名带口径。web vitest **490**（+11）/ api 465 / portal 19 / 四包构建 + 三处 tsc 全绿；**变异测试 6 次**全部如期变红——其中一次**第一遍没抓住**：把「对工厂不给供应商」改成恒给，测试竟全绿，因为原用例只覆盖了对客口径（而对客根本不出用料区），已补 UT-ORD-X11 钉住「给工厂那份不能带供应商与成本」。**零 DB 结构变更、零后端改动**。
- （本次·**反馈全量查询按账号开通**）`feat(api,web)` 新增 `MenuGuard` + `@MenuAccess()`，把账号级菜单权限（`sys_user.menu_keys`）从「只管前端」接到**接口层**，口径复用 `resolveMenuKeys`（ADMIN/主管恒全量）。`GET /feedbacks`、`GET /feedbacks/export` 由 `@Roles(ADMIN)` 改为 `@MenuAccess('feedbacks')`；`PATCH :id/handled`（回复/结案）**仍限 ADMIN/主管**，前端对只读账号不渲染操作列。`JwtStrategy` 补取 `menu_keys`（同一次查询，改完菜单立刻生效）；路由 `/feedbacks` 的 `meta.admin` 改为 `meta.menu`。**主管本来就有权限**（SUPERVISOR 视同 ADMIN），无需改动；King 是 BUSINESS，已在生产把他的账号菜单配成「业务默认 + feedbacks」。api jest 465（+9）；变异测试 4 次全部如期变红。**零 DB 结构变更**（只写了一条账号的 `menu_keys` 数据）。
- （本次·**8-14 反馈 #103~#105**）`feat(api,web)` **#103** `findOne()` 合成第一轮时，数量/寄样日期按「版师填的 → 业务填的」兜底（`piece_count ?? sample_qty`、`ship_sample_date || material_ship_date`）——原先只带单号和尺码，业务建单时另两格永远空着，只能照抄一遍；**#104** 新增 `latestShipDate()`，顶层「寄样日期」为空时用**最后一轮**的兜底（列表那一列读的就是顶层字段，两边从来没打通），人填的优先、各轮都没填就保持空，并把顶层寄样日期改成可清空；**#105** 尺码数量搭配模板由 CSV 改发**真 .xlsx**（数量写成数字、表头冻结），上传放开到 `.xlsx/.csv/.txt`，读取抽到 `utils/sheetGrid.ts`：xlsx 取第一个工作表、csv 解出替换字符**自动换 GBK 重读**（中文 Windows「另存为 CSV」默认 GBK，按 UTF-8 读会整片乱码而界面只报「款号为空」）、`.xls` 给可操作提示；`parseCsv` 上移到该工具，死掉的 `csvCell` 已删。api jest **456**（+9）/ web vitest **479**（+11）/ portal 19 / 四包构建 + 三处 tsc 全绿；**变异测试 9 次**全部如期变红。**零 DB 结构变更**。
- （本次·**8-13 反馈 #93~#102 一次清完**）`feat(api,web)` 详见上方「一句话现状」。要点：**#96** 材料寄出日期改手填（原先写死当天＝每条都错）并补回 `update()` 里被静默丢弃的单号/日期映射；**#95** 新增 `PATCH /samples/:id/undo-ship`（仅 SHIPPED→SAMPLING、清寄样日期、记 `UNDO_SHIP`）；**#97/#99/#100** 合同货物明细加「复制行 / 复制选中 / 往上插入」，复制**连 `order_material_id` 一起带**（订单「已订」绿标的凭据，订单侧 GROUP BY 不会重复计数）；**#101** 报价明细拖拽排序，**不引 sortablejs**（首屏包刚压到 480KB），原生拖放 + **仅手柄可拖**（整行可拖会让单元格里选不中文字）；**#102** 单位改可搜索下拉 + `allow-create`，报价与合同两处同步。**顺带修两处数据丢失**：合同的拉头/拉齿/码带能填能存却**读不回来**、保存也没往后端发（只有「从订单生成」写得进去），`order_material_id` 同样没回传——等于每保存一次就把订单侧「已订」洗一遍。逻辑抽到 `utils/rowOps.ts` / `utils/tableRowDrag.ts` 并单测。api jest **447**（+7）/ web vitest **468**（+21）/ 四包构建 + 三处 tsc 全绿；**变异测试 10 次**全部如期变红。**零 DB 结构变更**（只加 DTO 字段），红线一本轮不适用。
- （本次·对账逐品名明细 + 草稿可改）`feat(all)` **#80 对账明细不要平均价**（King）：对账行是**按发货批次**存的、单价取批次锁价——一个批次含多个品名时那就是加权平均。**生产实证** `DZ-MNA263M525-001`：显示 9941 × 15.3130，而批次里真实是 4 条不同单价的料（23.80/6.60/24.80/6.60），合计 152226.20 分毫不差。逐品名数据一直在 `contract_shipment_item` 里，只是没带出来。后端详情为每个批次挂上 `items`；前端把出货明细**摊成逐品名行**（批次汇总行保留、其下缩进列出）。**两种都兼容**：门户「按物料行填写实发数」是可选的，全库 16 个批次只有 4 个有逐行数据，没有的仍按整批一行，不会因为摊开而弄丢。**#83 对账草稿可改**（ZYT）：与付款草稿当时同一个毛病——只有 建/提交/复核/退回/删除，**没有编辑端点**，删除又限管理员。新增 `PATCH /reconciliations/:id`：**仅 DRAFT**、业务限本人创建、**只放开非结构性字段**（发票号/发票金额/税率/说明），改发票金额与税率时**同步重算发票差额与税额**（不留旧值）；**批次与费用行不在此列**——它们决定金额还牵扯批次占用释放，改一处等于把创建逻辑再走一遍，建错了删掉重建更稳，这条已写进弹窗提示。删除无权时也给禁用按钮 + 说明。api jest **387**（+5）/ web 374 / tsc 全绿，变异测试（不重算差额、去掉归属校验）2 条断言变红。**另两条经查是「怎么操作」不是 bug**：#79 那张是**已推送**的补料合同故整表锁定（页面本就有「撤销推送后可改」的提示），尺码列是自由文本、可直接填 `M-170/92A`；#73 目的地是 `allow-create` 字典选择框，打错的值会写进**共享字典**，要到「字典维护」里删（默认仅管理员/主管可见该菜单）。
- （本次·**#75 材料合同可以不关联订单**·**动了 DB 结构，红线一已走完**）`feat(api,web)` King：「材料合同能否不关联订单？需要一些公司挂卡或者销样面料之类的，是没有订单的」。`contract.order_id` 由 `NOT NULL` 放开为可空；**只放开材料合同**——加工合同的数量取自订单大货数、补料合同挂在母合同+订单上，这两类没订单无从建起，服务层显式拦下并说明原因。**三个坑**：①**`_i9_sync_col` 抓不到这次的改动**——它只比 `information_schema.COLUMN_TYPE`，而 COLUMN_TYPE 里**不含 NULL/NOT NULL**，`bigint`→`bigint` 在它眼里毫无差异，AUTO 段不会 MODIFY，存量库会一直停在 NOT NULL，新代码不填 order_id 就是 `Field 'order_id' doesn't have a default value` 建单必 500；故在 hotfix **手工段**补 `_i9_modify_col('contract','order_id','BIGINT NULL')`。（同类隐患 `customer.grade` 仍在待办；根治要改 `_i9_sync_col` 的比较口径，但那会让 677 列首次执行时全量 MODIFY，风险另评估。）②**TypeORM 里 `where: { order_id: undefined }` 等于不加条件**，会把**全库**订单材料捞回来拼进合同——无订单合同正好走这条路，加了 `&& dto.order_id` 判空。③落库 `dto.order_id ?? null`，并让「材料明细不能为空」的文案分支（无订单时再说"该订单无可带出的用料"是句听不懂的话）。前端：材料合同的「关联订单」改为非必填、可清空并给出「未关联订单：明细需手工填写」的说明，款号在无订单时也不再强制（挂卡/销样本就没款号）。**红线一真库验证**：拿今晚 20:18 生产备份复刻存量库 → 应用 hotfix → 与 HEAD `init.sql` 全新装机**逐列/逐索引比对**（`contract.order_id` 由 `bigint NO` 变 `bigint YES`、与装机一致；索引差异为空；列差异里剩下的 27 条**全是本次之前就有的漂移**：`customer.grade` 可空性、`contract.account_period_days` 默认值 45↔90、以及 quotation/sample_garment 等老重构留下的"存量有装机无"孤儿列，均非本次引入）→ **二次执行结构指纹一致、两轮 0 报错**。**红线二端到端**：同一份临时库上跑真 API，实测①无订单材料合同建成且库里 `order_id IS NULL`②加工合同不带订单被拒并给出可读原因③不填明细的提示已换成人话④带订单的材料合同照旧能建（老路没走坏）⑤合同列表读得回来，运行期 0 条 SQL 报错；临时库/授权/临时文件全部回收。api jest **401**（+5）/ web vitest 391 / portal 19 全绿；**变异测试** 4 次（去掉加工必填订单的拦截、自动带料不判空、落库不做 `?? null`、文案不分支）全部如期变红。
- （本次·**#87 材料合同发货地址取错了对象**）Grace：「材料应该发这个工厂地址，为啥协议出来还是公司地址，供应商说系统上不显示工厂地址」，附了供应商的微信原话「没显示收货地址」。**先把对象搞清楚**：材料合同的甲方（供方）就是那家材料供应商，他是**发货方**；他问的是「布该送到哪」——答案是做这件衣服的**加工厂**，不是他自己。而原来前端那行默认值取的正是 `selectedFactory.address`——**材料合同的 `factory_id` 恰恰就是材料供应商**，等于「把布发到卖布的人那儿」，取错了对象（老板一度也读成"供应商自己的地址"，正因为代码就是这么写的；已用 Grace 原话＋字段自带提示＋供应商微信三条证据当面确认为加工厂地址）。**而且时机也不对**：那个默认只在「手动切换工厂」时触发，材料合同基本都是从订单一键生成的、打开时不触发——生产实证 **24 张材料合同里 22 张这栏是空的（92%）**，打印模板照常印一个「—」，供应商只能回来问。改法三件：①后端 `resolveShipToAddress()`——同订单的加工合同 → 它的工厂 → 工厂地址，**找不到就留空，绝不拿别的地址凑数**（填错地址比留空危险得多）；②**加工合同往往比材料合同后建**（先订料、后定加工厂），所以建加工合同时**回头补齐**同订单下仍为空的材料合同；③前端删掉那个取错对象的 watcher，规则条与占位文案改成写明来源与「该订单还没建加工合同时填不出来」。**存量试算**（生产副本）：只有 **2 张**补得上，**19 张补不上**——因为全库 9 个有材料合同的订单里只有 3 个有加工合同；Grace 那张单（O-20260722-003）**根本没有加工合同**，所以光改代码她那栏还是空的，必须靠界面把原因说出来，这也是本次特意加文案的原因。api jest **435**（+4）/ web vitest 443 / portal 19；**变异测试 3 次**全部如期变红。**同轮修了一个脆弱测试**：`UT-CON-15` 的 `findOne` 用的是按调用顺序排的 `mockResolvedValueOnce` 链，事务里**多一次查询就全乱套**（本次加了「查该款加工合同」后当场挂掉）——改成按实体分发。
- （本次·**前端错误上报上线一天的收成：抓到 1 个真 bug，也暴露了上报器自己的 3 个问题**）拉「系统报错」页，5 条前端上报里：**①真 bug——主管改不了付款草稿**。`payment.service.ts` 里手写了 `user.role === UserRole.ADMIN || FINANCE`，**漏掉 SUPERVISOR**；而前端 `hasRole(ADMIN)` 与控制器的 `RolesGuard` 都按「主管视同 ADMIN」放行（2026-07-22 拍板，`isAdminRole`），于是**按钮看得见、点下去 403**（日志里主管点了 3 次）。改用 `isAdminRole`；对账那处虽然写对了但同样是手写，一并统一。**全库排查过，只有这两处手写 ADMIN 比较。②上报器噪音太大**：`ResizeObserver loop …` 一天报了 12 条、占错误表一半——那是浏览器自己的告警不是错误（观察回调里改布局、通知推到下一帧，规范上合法，Element Plus 的表格/下拉几乎必触发）；另有「Request failed with status code 403/400」从 **Vue 通道**漏进来（`unhandledrejection` 那条早就挡了，`errorHandler` 这条忘了挡）。两类都加进降噪。**③上报请求撞 401 会把人踢到登录页**——`http.post` 走的是同一个 axios 实例，401 触发全局跳转；一个后台旁路请求把正在干活的人踹出去是本末倒置。给上报请求打 `telemetry` 标记，拦截器见到就不跳。**④后端只记 5xx，400 全无痕迹**——King 建结算单连点两次 400、事后只能靠猜，正是因为 400 不入库。改成 **5xx 或 400 都记**（401/403 仍不记：正常的登录态与权限噪音，量大且无信息量）。400 是「系统明确拒绝了用户」，恰恰最该留下原因。api jest **431**（+2）/ web vitest **443**（+3）/ portal 19；**变异测试 4 次**（把主管漏回去、把业务也放开、去掉降噪、去掉遥测标记）全部如期变红。
- （本次·**侧栏折叠按钮的提示盖住标签栏**）用户截图：鼠标停在左上角折叠按钮上，「收起菜单」的深色提示框正压在标签栏第一个页签上，看着像界面坏了。原因是用的**原生 `title`**——浏览器一律把它画在元素**正下方**，而这个按钮下面紧挨着就是标签栏，必然重叠且**位置完全不可控**。改用 `el-tooltip` 放右侧（`placement="right"`、400ms 延迟），位置可控、样式与全站一致、不挡任何可点的东西；原 `title` 换成 `aria-label` 以免读屏丢失。**同类写法在别处见到也照改**：图标按钮上的原生 title 只要下方紧跟着内容区就会撞。
- （本次·**#85 打印行高与列宽可调**）YSM：「打印面的行高不能调整吗？一个字一行，很浪费纸，另外可以自行调整是最好」。**先查出「一个字一行」不是行高的问题，是列宽预算的问题**：A4 竖版去掉 14mm 页边距后正文宽约 **680px**，默认要印 11 列，其中品名、备注不设宽度靠剩余空间撑——而固定列合计 **600px**，只剩 80px 给这两列平分、每列 40px，一个汉字在 12px 字号下约占 12px，**三个字就换行**，长品名于是被压成一条竖线。三件事一起做才有效：①**收紧默认列宽**（部位 64→48、颜色 90→70、成份 90→70、尺寸 80→60 等），固定列合计 600→**488px**，给品名+备注留出 **192px（每列 96px）**；②**行高可调**（0~10px 单元格内边距，0 是合法值＝最省纸，**不能用 `|| 4` 兜底否则 0 会被吃掉**）；③**列宽逐列可调** + `table-layout: fixed`。**注意 fixed 的作用别写错**：它本身不解决「一个字一行」，它解决的是「调了不生效」——auto 布局下浏览器会按内容重新分配、用户设的宽度经常被无视，"能自己调"就成了空话。操作台里还加了**超宽告警**：各列宽度合计超过纸宽时提示超了多少 px，并给出可操作的三条路（改横版／去列／调窄某列）。测试里加了一条**预算守卫**——直接断言"默认布局下自适应列每列 ≥80px"，把默认列宽调回原来那套就会红，防止日后有人随手加列/加宽又把这个坑挖回来。web vitest **440**（+9）；**变异测试 6 次**全部如期变红（行高用 `||` 兜底、不钳越界、去掉 fixed、忽略用户列宽、行高写死、默认列宽调回旧值）。另修一个自己引入的错误：`pad`/`widths` 声明写在了使用之后（TDZ），单测直接报 `Cannot access 'widths' before initialization`——**这正是"只写不跑"会漏掉的那类**。
- （本次·**#84 粘贴供应商列 + #92 付款申请可自行登记发票**，后者含 schema 变更）**①#84**（daisy：「供应商这栏希望可以复制粘贴，不需要一个个去重复找再点」）——页面本来就有「批量设置供应商」，但那只解决"几十行同一个供应商"；她的场景是**每行不同、名单在 Excel 里**。新增「📋 粘贴供应商列」：读剪贴板（生产是 HTTP，`navigator.clipboard` 不可用，走既有的手工粘贴兜底框）→ 按工厂名逐行匹配 → 从**第一个勾选行**（没勾就第一行）往下填，与 Excel「点一个格子往下贴」的习惯一致。匹配口径单独抽成 `utils/supplierPaste.ts` 并单测，因为它是全流程最容易出错的一环——**认错一家工厂，材料就订到别人那儿去了，界面上完全看不出异常**：完全同名优先（库里有「苏州鑫研服饰」和「苏州鑫研服饰有限公司」两条时，精确输入直接命中对应那条）；写少了字导致两家都沾边时返回 AMBIGUOUS **不猜**；对不上的行**原样保留不清空**并逐条报出来（静默跳过会让人以为都贴上了，等生成合同才发现缺供应商）；粘的行数超过材料行数也明说。**②#92**（King：「非合同付款可以我们自己上传发票吗？」）——`payment_request` 表**没有发票字段**，合同付款的发票挂在对账单上、非合同付款无处可放。加 `invoice_no VARCHAR(100)` + `invoice_url VARCHAR(500)`，**走红线一全流程**：改 `init.sql` → `gen-column-sync.py` 重生成 → 真库验证。前端在付款申请建单/改草稿里加发票号与附件（走 FileUpload，不是贴 URL），列表加一列「发票」可点开看。**不按类型隐藏**：合同付款也允许填——"对账时没票、付款时补票"确实存在，藏掉只会让业务绕路。**红线一验证结果**：以生产备份建存量库 → 应用 hotfix → 两列正确加上；与全新装机库做**四向 diff**——「全新有/存量缺」**为空**（这才是会炸 Unknown column 的方向）、类型不一致**为空**、缺表**为空**；另有 25 列只存在于存量库，是早期版本从 `init.sql` 删掉但升级脚本只增不减留下的**历史遗留，与本次无关**（第一次跑 diff 只看了前 20 行、差点把它误读成本次引入的问题，故补了分方向的复核）。二次执行 hotfix 幂等、0 报错；真 API 建了一张非合同付款单，发票号与附件正确落库、列表接口能带出。api jest **429**（+4）/ web vitest **431**（+12）/ portal 19 / 四包构建 + 三处 tsc 全绿、`deep-test.sh` 语法通过；**变异测试 7 次**全部如期变红——其中两次**第一遍没抓住**：`supplierPaste` 的空名守卫其实是死代码（已删并改成防脏数据的真守卫），`updatePaymentRequest` 的"没传就保留原值"分支因为用例总是传了值而没走到（已补不传的用例）。
- （本次·**一批确定性反馈：#89 / #91 / #90 / #86+#88**）**①#89 材料标绿失效**（King：「下过合同的材料应该标绿…咋还是不行呢」）——**不是绿标坏了**。查生产：订单 43 的 7 张材料合同分别建于 15:03/15:23/15:46/16:07/16:25/16:37/16:45，而「按供应商拆单」是**一个事务里一次性生成**、时间几乎相同，所以那是**一张张手工建的**；手工建的合同前端根本没有「这一行来自订单哪一行」的信息，`contract_material.order_material_id` 全 NULL，绿标认的正是这个字段。修法：合同**挂了订单**时按**品名**把行认回去（颜色/尺码在合同侧可能被拆开、订单侧是一行，所以不能进匹配键），同名多行按顺序一一对上，**订单上没有的料不猜**——标错行会让业务以为某个料已经订了而漏订；顺带把**最终采购量**补上（同一条反馈里的「带入实际订单数量」），且**只补空值不覆盖人工填过的数**。新建与编辑两条路都接了。**②#91 新建结算单选了款号却没有对账金额**——聚合本来就有而且是对的，只是它发生在**保存那一刻**，弹窗里什么都不显示；而弹窗顶上就写着「选择订单后系统自动聚合…可在下方人工覆盖」，业务照着这句话等结果、等到一片空白，只能理解成坏了（生产实证：该款号有 6 张已确认/已付款对账单合计 3.7 万）。新增只读接口 `GET /settlements/cost-preview`，把**同一份聚合结果**提前显示出来，**不改任何计算口径**；已确认未付的照旧单列「不计入」，一条对账都没有时给一句话说明为什么是空的。**③#90 结算货币符号可选**：`settlement.currency` 本来就有、只是界面把 `$` 写死了 7 处；新建时可选币种（默认跟订单走），详情/收汇/发票金额按单据币种显示符号。注：`invoice_amount_usd`/`receipt_usd` 列名带 usd 是历史命名，存的是本单币种金额，**只改显示不动表结构**。**④#86+#88 对账加费用行**（Grace：现货供应商的运费加在哪；King：增加「添加费用」一行）——昨天做的扣款明细本来就支持正负两向，只是文案写死成"扣款"；改成「费用 / 扣款调整」：**加钱填正数**（运费/版费/打样费）、**减钱填负数**（打折/次品退货），导出合计文案也跟着正负变。**红线二走了真库**：临时库跑真 API 实测——#89 手工建合同认回订单第 330 行、临时辅料保持 NULL 没乱猜、最终采购量从 0 补成 123、订单详情 `contracted=true` 并带出合同号；#91 预览在订单 35 上返回 1 条聚合行且「已确认未付」正确地不计入成本；0 条 SQL 报错，临时库已回收。api jest **425**（+12）/ web vitest **419**（+2）/ portal 19 / 四包构建 + 三处 tsc 全绿；**变异测试 6 次**全部如期变红。**同轮修了测试桩的一个漏洞**：合同模块 18 处 manager 桩缺 `find`，事务里新增查询后集体炸掉——补齐而不是把生产代码改成"没有 find 就跳过"。
- （本次·**#74 对账扣款明细 + 财务默认可见客户管理**，两条都由业务微信拍板）**①#74**（King：已确认合同要打折/次品退货怎么办）业务定的口径是「**合同保持原样不动**，在对账环节加一类扣款明细（可填负数，带事由和照片/说明附件），对账金额＝发货金额−扣款，后面的付款和结算跟着这个走」，JXH 确认。实现要点：**不建表**——`reconciliation_expense_item` 本来就是「事由+金额+款号+附件」四件套，扣款要的字段一个不差，复用它就**不涉及存量库结构升级**（红线一本轮不适用）；金额**带符号存**，扣款填负数，界面填什么、库里存什么、导出显示什么三处一致，不做正负转换（业务原话就是「可填负数」）；`total_amount = 发货金额 + Σ调整`，税额与发票差额自然跟着扣后金额算；**扣光或扣成负数直接拦**——放过去后面付款申请的超付闸门、结算毛利全跟着错且很难回头查；详情查询原来写的是「只在 NO_CONTRACT 下查明细」，**合同对账挂了扣款根本看不见**，已改成只排除工时类型。前端：创建弹窗加扣款行（事由/金额/款号/**照片附件走 FileUpload**，不是让人贴 URL），并**当场显示金额构成**「发货 − 扣款 ＝ 对账」——只给一个总额业务对不出为什么少了一笔；导出同理，合同对账里这张子表标题改叫「扣款明细」而不是「费用明细」（把一列 -500 标成"费用"，看的人会以为是花出去的钱）。**②财务默认可见客户管理**：`ROLE_DEFAULT_MENUS[FINANCE]` 补 `customers`（微信原话「直接给财务开发客户权限，其他角色还是默认需要主管配置」，Steve 确认）。查过生产：qiao 的菜单是主管一项项配的、本来就带 customers，所以**这次改的是以后新建财务账号的默认值**，存量账号不受影响（`resolveMenuKeys` 对显式配置过的账号只认配置）。**红线二走了真库**：生产备份复刻临时库确认 `amount` 是 `decimal(15,4)` **有符号**（UNSIGNED 的话负数根本存不进去，mock 永远测不出），直插 -500.0000 原样读回，再用真 API 拉一张真实的合同对账单详情、确认扣款能带出来，运行期 0 条 SQL 报错，临时库已回收。api jest **413**（+7）/ web vitest **417**（+9，新增 `reconciliationExcel.spec.ts`）/ portal 19 / 四包构建 + 三处 tsc 全绿；**变异测试 7 次**（扣款不计入金额、扣款取绝对值、去掉扣负拦截、明细不落库、详情又只查无合同、财务默认去掉 customers、把全部菜单给财务）全部如期变红。
- （本次·**白屏续：去掉 `<transition mode="out-in">` + 补前端错误上报**）用户截图复现：URL 是 `/export-invoices`、**侧栏高亮、面包屑、标签页全都正常切过去了，只有内容区是空的**。这推翻了上一条的判断——骨架那条修的是**整页加载**的白屏，而这次布局明明在，说明路由跳转是**成功**的（不成功不会有那个标签页），组件也确实加载到了（Vue Router 会等 `import()` 完成才算导航成功），问题出在**渲染**这一步。**嫌疑锁定 `MainLayout` 里的 `<transition name="page" mode="out-in">`**：旧页先淡出 180ms、再淡入新页；**在这 180ms 内再点一次菜单**，Vue 会把待入场的组件丢掉，结果就是路由/标签页/面包屑都对、内容区一直空着，且**没有任何网络错误**——正好解释「经常打开菜单、或切换菜单就白屏」（连续点菜单触发）。过渡纯属装饰，去掉零功能损失、消掉一整类问题；**`:key="$route.fullPath"` 必须留**（防「编辑样衣 #5 切 #7」时复用实例、表单串单据）。**同轮补上前端错误上报**——今晚为这一个问题连错两次方向（先当 chunk 404、日志里那几天根本没有 404；再当首屏骨架、用户截图证明布局是好的），根因是 `error_log` 只收后端异常、**前端崩了服务器一无所知**，只能靠翻 nginx 日志倒推。新增 `POST /error-logs/client`（**方法级 `@Roles` 覆盖类级 ADMIN**，否则最需要证据的业务同事一条也报不上来），前端捕获 Vue 渲染错误 / 未捕获异常 / 未处理 Promise 拒绝 / 路由错误，带上出错路由与**构建标识**（用来判断用户是不是跑在旧版本上）。刻意克制：**不报**接口 4xx（后端自己记过，重复上报会淹掉错误表）、不报 chunk 失败（有专门的换版本逻辑）、同错 30 秒一次、单页最多 5 条、上报自身绝不抛错。**必须挂在 `app.mount()` 之前**，否则「进页面直接白屏」这种首次渲染就挂掉的情况反而抓不到。**红线二走了真库**：生产备份复刻临时库 → 真 API（3002 端口）→ 用**业务角色**打一条上报，HTTP 204、真落库、字段齐（`method=CLIENT` / `error_type=FE_VUE` / build_id 进 `req_input` / username 有），同一条报 3 次只 count+1 不新增行，运行期 0 条 SQL 报错，临时库与授权已回收。api jest 406 / web vitest **408**（+8）/ portal 19 / 构建全绿；**变异测试 5 次**（去掉 30 秒去重、去掉单页上限、接口错误也报、chunk 也报、顶掉原有 errorHandler）全部如期变红。
- （本次·**白屏的真正原因：`<div id="app">` 是空的**）`fix(web,portal)` 用户报「经常打开菜单、或切换菜单就白屏，刚刚也是」。**先查日志再动手**：nginx 里 8-11、8-12 **一条 assets 404 都没有**（历史白屏 8-04/05/08/10 全都伴随 chunk 404，这次没有），说明不是取不到文件。顺着日志找到那个有登录态的 IP，看到**两个标签页每 3 分钟在拉 `version.json`**——今晚加的版本检测正常工作，发现发版后会在下次切页做**整页跳转**。而 `index.html` 里 `<div id="app"></div>` **完全是空的**：整页加载时 JS 没跑起来之前屏幕上什么都没有，首屏要下 480KB，冷缓存下就是好几秒纯白。**整页加载并不罕见**——登录、刷新、发版后换版本、chunk 恢复全都走它，所以「经常」。修法：①`index.html` 里放一屏**加载骨架**（纯内联 CSS，跟 HTML 一起到达即可见，Vue mount 时自动清掉，不需要额外代码配合），10 秒还没被顶掉就显示「加载超时，点此重新加载」——那正是"入口 JS 自己都取不到"的情形；②补 `unhandledrejection` 兜底：`router.onError` 只管**路由级**懒加载，而生产 404 过的 `CsvImportDialog-*.js`、`sampleExcel-*.js`、`contractExcel-*.js`、`AiToolsView-*.js` 都是**组件内 `import()`**，失败时路由完全不知情、表现是「点导入/导出没反应」；现在统一走 `recoverFromChunkError()`，**只有确认真发过版才刷**（否则真网络故障会被刷成无限循环）。门户同样处理（手机+移动网络，白屏只会更长）。web vitest **400**（+3）/ portal 19 / 构建全绿；变异测试 2 次（把普通业务错误也当成 chunk 失败、不判版本就刷）均如期变红。
- （本次·**清生产前端报错 + 流畅度优化**）`fix(web,api)` 用户要求「查生产日志，把前端报错都解决，并优化流畅度，别影响业务连续性」。**先查后修**：`error_log` 表只有 3 条且是 7 月的（那是后端错误表），API journal 近 7 天 0 条 ERROR，30 次重启全是部署——**问题全在前端与静态资源**，证据来自 nginx access.log 归并。修了 6 处：**①401 风暴（最大一处）**——一页同时打 7~8 个请求，token 一过期**同时**401，日志实证 8 个列表接口各 17~18 次挤在同一刻，而拦截器对每一条都执行一次 `location.href='/login'`；加「只跳一次」标志，并把当前地址带成 `?redirect=`（`LoginView` 本来就在读这个 query，此前一直没人传），做到一半被踢出去的人重新登录能回原页。**②`/settlements/stats` 403 ×19**——该接口是 `@Roles(ADMIN,FINANCE)`，但结算页无条件调用，别的角色每次进页面白挨一个 403；改成先判角色。**③反馈附件 403 ×8**——反馈图直接用裸地址渲染，落在 `private/` 的那张一律 403（生产上 55 条附件里有 1 条是 private，有人对着它点了 8 次都打不开）；改走 `signedUrl`。**④favicon.ico 404 ×58**——两个站点都没有这个文件，浏览器隐式请求必 404；补上（**注意别直接拷 720×720 的 datex-mark，那是 176KB，比不修还糟**，缩到 64px＝4KB）。**⑤上传文件没有缓存头**——`res.sendFile` 默认 `max-age=0`，合同/样衣材料明细一页几十行、每行一张照片，每次进页面逐张回源到 Node（生产单张最大 1.9MB、48 张超 500KB）；公共文件给 `public, max-age=31536000, immutable`（文件名是 uuid，内容不会变），**private 只给 `private, max-age=300`**——给 public 等于把签名保护废掉，让共享代理把敏感附件缓存下来发给别人。**⑥nginx gzip 压缩级别**——`gzip_comp_level` **默认是 1**（压得很浅），显式设成 6：elementPlus 那个包 394KB→338KB、主 CSS 69KB→50KB，**一句配置省 76KB**，纯配置零应用风险（停在 6 不上 9：9 只再省 0.7KB 但 CPU 明显更贵；产物又是 immutable 长缓存，一个客户端只压一次）。同轮纠正一个**缓存口径错误**：`location ~* \.(js|css|png|...)$ { expires 1y; immutable; }` 把 `/datex-mark.png`、`/favicon.ico` 这种**固定文件名**也标成了 immutable——哪天换 logo 老用户一年都看不到新的（浏览器根本不会来问）。改成 immutable 只给 `/assets/`（文件名带内容哈希），根目录固定名图标只缓存一周。**⑦品牌图按显示尺寸重出**——`datex-mark` 720×720/176KB 实际只显示 34~74px，`datex-logo` 1180×1300/85KB 只显示 132px 宽；重出后登录页首屏图片 **261KB → 47KB（-82%）**。**首屏冷加载实测合计：775KB → 480KB（-38%）**；剩下的 480KB 里 **elementPlus 一个包就占 338KB**，要再降只能做按需引入——那要动全站组件解析，风险和收益都大，**本轮按「不影响业务连续性」没做，留给你拍板**。另加 `el-image lazy`（合同/样衣材料明细、反馈列表）与「标签页在后台不轮询反馈未读」（那条一分钟一次，是全站请求量最大的接口之一）。**没修的两个如实记账**：`PUT /samples/123`、`PUT /quotes/21|52` 各几次 400，最后一次是 8-06、之后多次发版未再出现，**无法复现故不动**；`/uploads/file` 空 `p=` 的 400 ×9（referer 是报价查看页）**未定位到来源**，已确认各处渲染都有 `v-if` 守卫、库里也没有坏值。api jest **406**（+5）/ web vitest **397**（+6，新增 `http-401.spec.ts`）/ portal 19 / 四包构建 + 三处 tsc 全绿；**变异测试 5 次**（去掉 401 去重、去掉 redirect 回跳、登录页也跳转、不发缓存头、private 也给 public 缓存）全部如期变红——其中「去掉 401 去重」**第一次没被抓住**：断言写的是「跳了几个不同地址」，而重复跳的是同一个字符串，改成数赋值次数才抓得住。
- （本次·**#82 按工厂导出该公司所有账单**）`feat(api,web)` qiao：「可以按工厂名称，下载EXCEL文件，拉出这个公司的所有账单吗」。付款管理页顶部加「导出工厂账单」，选工厂（区间可选，不选＝全部），一份 xlsx 出齐**付款申请 / 实付记录 / 预付款 / 对账单**四张明细 + 汇总。三个当时想清楚的点：①**新开 `GET /payments/factory-statement` 而不是前端循环调列表**——列表分页上限 100，且「某张申请下的实付记录」只有按 ID 取的接口，前端拼账单就是 N+1；今天全库才 5 张申请看不出来，一家供应商做满一年就是几百张。②**金额必须是真数字**：本层给所有单元格钉了 `numFmt='@'`（否则款号 I27.230.03929 被 Excel 截成 27.23），代价是金额也成文本、选中一列不显示求和、`=SUM()` 得 0——而这份表存在的意义就是拿去二次加工。故给 `docExcel` 加了 `NumCell`（`numCell()`），**只在 xlsx 路径写成真数字**，HTML 路径与既有五条导出链路一个字不动。③**合计口径写在标签里**：「应付合计（已批准+已付款）」——草稿/待审批不构成欠款，混进去会把应付虚高；已驳回的行明细里照列但不进合计，且该行「应付/已付/未付」**留空而不是写 0**（写 0 会被读成已结清）。**红线二按规矩走了真库**：拿今晚 20:18 的生产备份复刻临时库 `i9_stmt_check`、用本次产物起 3001 端口真 API，5 家有往来的工厂逐一与直算 SQL 对齐（申请/应付/已付三列全一致），另测不存在的工厂→404、缺 factory_id→400、前端清空筛选发的空串日期→正常、无往来工厂→空账单；运行期 0 条 SQL 报错；验完临时库与临时授权一并回收。另外把 `customsDocs.ts` 文件头一句**错注释**改了：源文件写的是「工厂给的采购合同 PO」，实际是**客户给的**（标题叫《采购合同》但抬头是晋江必迪斯 BDS、致南京达泰，是客户的采购＝我们的销售订单；发票 ISSUED TO＝BDS、受益人＝南京达泰同样印证），写成「工厂给的」会把整件事读到上游合同侧去。api jest **396**（+9）/ web vitest **391**（+11）/ portal 19 / 四包构建 + 三处 tsc 全绿；**变异测试** 9 次（后端 4：驳回进合计、应付含草稿、区间不带时分秒、actual_pay 空当 0；前端 5：金额退回文本、数字格式钉成 @、驳回行照写应付、水单导裸链接、未填金额写 0）全部如期变红。
- （本次·**白屏的正解：让浏览器发现发版并主动换新版本**）`fix(web,portal)` 用户指出上一条的方向错了：「不应该是不删掉旧的静态资源，而是让浏览器发现已经发版了，去请求新的静态资源。」**确实错了**——保留旧 chunk 只是让白屏不发生，代价是**用户可以一直跑在旧版本上**：今天修好的问题他明天照样遇到、还会再报一次，等于把问题藏起来。改法：构建时生成 `BUILD_ID`，同时写进产物根目录的 `version.json` 和运行时常量 `__BUILD_ID__`（vite `define` + `generateBundle` 里 `emitFile`）；前端每 3 分钟、以及**每次标签页切回前台**时拉一次 `version.json`（`?t=` 时间戳 + `cache:'no-store'` 双保险，否则永远读到旧值），发现标识变了就 ①弹一条**不自动消失**的「系统已更新」（可点立即刷新）②打标记，**下一次切换页面时用整页跳转代替前端路由**——那是天然的安全时机，不会把正在填的单子刷没。守卫里必须 `return false` 终止前端路由，否则会先按旧 chunk 名去加载、还是白屏。`deploy-local.sh` 随之**恢复 `rsync --delete`**，不再留旧文件、不再有 14 天清理。`router.onError` 的整页重载兜底保留，防的是「恰好在两次轮询之间发版、用户又正好点了进某一页」这个竞态窗口；门户同样受影响（供应商多是手机长期挂着标签页，最容易停在旧版本上），故 web/portal 各配一份（两包之间没有共享运行时代码，`types` 只放类型，改一处记得改另一处）。另修 `deploy-local.sh` 一个老坑：四个构建都写成 `pnpm ... >/dev/null`，失败时 pnpm 的报错走 stdout 被吞掉、`set -e` 又当场退出（连 `die` 都来不及跑），屏幕上只剩「构建管理后台...」**一个字的原因都没有**——本次就是这么静默死了一次；改成 `build_pkg` 落临时文件、成功丢弃失败原样打出。web vitest **380**（+6，新增 `versionCheck.spec.ts`）/ portal vitest 19 / 四包构建 + 三处 tsc 全绿；**变异测试**把守卫的 `return false` 改成 `true`、去掉 `no-store`+时间戳、去掉 `pendingUpdate` 标记，三次都如期变红。
- （本次·**白屏事故根因：发版删掉了旧 chunk**）`fix(web/ops)` 两位用户同时报「系统太不稳定」「返回工作台一片白」（#77 Nina / #78 EVA），后端报错表却一条都没有。**证据在 nginx**：`assets/CsvImportDialog-*.js`、`QuoteListView-*.js`、`ExportInvoiceView-*.js` 等一串 **404**。根因：路由是懒加载、chunk 用内容哈希命名，每次发版换一批新名字，而 `deploy-local.sh` 用 `rsync -az --delete` 把旧文件清掉了；**用户已经开着的页面**手里是旧 index.html，点进某个路由时仍按旧名字请求 → 404 → 动态 import 失败 → **整页白屏**，且请求根本没到后端（所以 error_log 一片空白，极易误判成"偶发"）。当天发了 8 次版，两人先后中招。**当时的做法已被下一条推翻**（① 保留旧 assets 只增不删 —— 见下条「白屏的正解」）；仍然保留的是②`router.onError` 捕获动态导入失败后**自动整页重载一次**到目标地址，用 `sessionStorage` 打标记确保只重载一次（否则新版本真有问题会变成无限刷新），成功进入任意页面即清标记。**遗留**：旧 chunk 本地也已被覆盖、补不回来，**当前已白屏的用户需手动刷新一次**，之后由这两道防线接管。**#81 Excel 导入弹窗**（EVA：「出现这个弹框，但没有下载选项。鼠标也是不可操作的状态」）：文案是 `文件超过 15MB，请下载后查看`——那句是给**附件预览**写的（`sheetPreview` 注释：预览而已免得卡死），导入路径复用 `parseXlsx` 连文案一起继承了，在"上传"场景里说不通。改为导入侧**按 file.size 提前拦**并给可操作的说法（报出实际大小 + 「删掉多余工作表/图片后另存，或把明细单独存为 .csv」），且**出错时弹窗只留「关闭」**，不再摆两个按不动的按钮。web vitest 374 / tsc 全绿。
- （本次·对账「待复核」找不到复核入口）`fix(web)` 微信群里问「这个待复核从哪复核」。**先查线上**：`DZ-MNA263F525-001` 现已是 `CONFIRMED`（截图后已有人复核），批次也是 `APPROVED` 后才被对账占用——**流程没错乱，纯粹是找不到入口**。原因：「复核确认/整单退回」限 `ADMIN/SUPERVISOR`（前后端一致），非主管看到的操作列只有「详情/导出Excel」，**既没有入口、也不知道该找谁**。这与 8-10 付款那条（「付款付了多少填在哪儿」）是**同一类问题**：能力存在但被角色隐藏且无任何说明。照同样办法处理：无权时也把按钮露出来但禁用，并用 tooltip 说明该找谁（「复核确认需要主管或管理员操作」）；草稿态的「提交复核」同理。web vitest 374 / tsc 全绿；零后端改动。
- （本次·门户合同导出补「数量搭配」矩阵）`feat(portal)` **第一条来自供应商门户的反馈**（s136，8-10 19:12）：附图要求合同 Excel 改成某版式——**图与 8-08 老板给的那张是同一张**，即 8-09 已实现的真实合同版式。逐项比对后确认差的是**末尾那张「数量搭配」表**：`contractExcel.ts` 里 0 处（产物里搜到「数量搭配」是门户详情页的，不是导出文件）。数据不用新增接口——门户详情本就返回 `orderDetail.size_matrix`，详情页已在用同一份。导出末尾补一张「款/色/洗标号/码 × 各PO + 合计」表，无矩阵数据时不印空表。portal vitest **19**（+4，含变异测试：不拼矩阵则 2 条断言变红）/ api 382 / web 374 / tsc 全绿。**顺带确认门户反馈泡泡确实在用**：未读接口被 7 个供应商设备调用 276 次、状态码全是 200/304，此前 0 条只是没人点开过。
- （本次·**驳回发货批次不冲回累计已发**——生产已中招）`fix(api)` 起因是 #71（King：「供应商发货后，数字不对的话，需要能金额调整」，截图里他在门户填了 **本次实发 = -46** 想冲掉多发的量）。顺着查出两层：①负数其实被后端挡着（`if (!(qty > 0))`），但报错是「请填写本次实发数量」——他明明填了，只是填了负数，这提示等于没说；②**真正的坑**：`approveShipment` 驳回批次时只改 `approval_status`，**不动 `contract.shipped_qty`**，累计已发只增不减。**生产实证**：`HT-20260810-001` 唯一那个批次已被驳回，合同上却仍挂着累计已发 **1895**（未驳回批次合计为 0）——数量差额/超发闸门因此全部失真，用户只能想歪招。修法：新增 `recalcShippedQty()`，审批**通过或驳回后都按「未驳回批次」整体重算**；**用重算而不是加减**，因为重算是幂等的，同一批次反复审批/驳回都不会越算越歪（专门有用例，且做了变异测试——去掉重算后 4 条断言变红）。负数的报错也改成明确指路：「不能是负数；发多了请联系业务驳回该批次，驳回后累计已发会自动扣回，再重发正确数量」。**遗留数据需人工确认**：本次代码修复只在**下一次审批/驳回动作发生时**纠正该合同，`HT-20260810-001` 眼下仍显示 1895（应为 0）——**未擅自改生产数据**，已在汇报中列出精确的更正 SQL 待拍板。api jest **382**（+5）；零 schema 改动。
- （本次·8-10 又两条：发票点开变下载 + 非合同付款草稿改不了）`fix/feat(all)` **#70 发票要能直接点开**（King）：**先验后端再改前端**——实测 `/uploads/file` 对 PDF 返回的就是 `Content-Type: application/pdf` + `Content-Disposition: inline`（直连 API 与过 nginx 都查了），**后端没毛病**。真原因有两条：①开新标签页后是否内联显示**完全由浏览器决定**，国内常见的 360/QQ 浏览器、以及关掉内置 PDF 阅读器的 Chrome 都会直接落成下载；②`openFile` 是 `await` 拿到签名链接之后才 `window.open`，**已脱离用户手势、还会被弹窗拦截**。故新增 `components/FilePreviewDialog.vue`，在自己页面里用 `<iframe>`(PDF)/`<img>`(图) 渲染，显示与否不再看浏览器脸色；组件内部换短时签名链接（供应商上传一律落 `private/`）。**举一反三**：水单在**付款、结算、出口发票**三处也是同一条 `openFile` 路子，一并换掉。**#69 非合同付款草稿可以调整吗**（King）：查出一个更严重的缺口——付款申请从来只有「建/提交/审批/付款/删除」，**根本没有编辑端点**；而提交是 `ADMIN/FINANCE`、删除是 `ADMIN`，**创建却放行 BUSINESS**（后端注释写明「业务可发起无合同付款」），于是业务建完草稿**自己动不了任何一步，卡死**。新增 `PATCH /payments/requests/:id`：**仅 DRAFT**（已提交的金额是审批依据、已付款的会让「已付 vs 申请」勾稽断掉）、**业务限本人创建**（不动既有角色矩阵，提交/审批/付款仍是财务/管理员）、**改单同样过冲抵预付的余额闸门**（不能靠改单绕开创建时的校验）。前端草稿行加「编辑」（复用新建弹窗，标题切换，关闭时清编辑态否则下次「新建」会变成改上一条）；**无权提交时也把原因说出来**（禁用按钮 + 「提交审批需财务或管理员」），别让草稿看着像死胡同。**前端不假装判断归属**：auth store 只存 role 不存 user id，故统一放出入口由后端把关，越权时它返回的文案本身就是说明。验证：api jest **377**（+8）/ web vitest 374 / portal 15 / 三处 tsc 全绿，并做**变异测试**（去掉草稿闸门与归属校验，2 条断言变红）；**零 schema 改动**。
- （本次·8-10 两条反馈：订单标绿漏了一整条路径 + 付款入口找不到）`fix/feat(all)` **#67 订单材料标绿**（King：「有一部分还是没绿」）：**查生产比他说的更严重**——订单 36 唯一那张合同的 4 条材料行 `order_material_id` **全是 NULL**，即这张合同带来的**一条都不会绿**。根因：合同编辑页的**「从订单带入」拼行时没带 `order_material_id`**（`c.raw.id` 就在手边却没用），而只有走「生成合同」的 `expandMaterialLines` 才会写。所以凡是走这个入口建的合同，订单侧永远标不了「已订」。三处补齐：分色/分码支、不拆分支、以及**载入既有合同时也要保留**（否则「打开合同→保存」会把已有的绿底洗掉）。**#67 附带需求：合同供应商回填订单**——新增 `backfillOrderSupplier`，建合同后把工厂名写回有溯源的订单材料行；**只填空的，绝不覆盖**：订单上的供应商是业务自己选的、且**决定了合同怎么拆单**，被反向改掉会让下次重新生成拆成另一个样且无人知道（这条专门有用例并做了变异验证）。**#68 付款**（qiao：「付款付了多少填在哪儿？」+ 贴水单 + 已付清/部分付款标色）：**先查清再动手——分批付款、水单上传（支持拖拽/Ctrl+V）、付款记录表其实早就有**，问题是「付款」按钮要 `approval_status === 'APPROVED'` 才出现，而她那两条都在「待审批」，那一列只剩「导出Excel」，看不出功能存在、也看不出为什么。故改为**待审批时也把入口露出来但禁用，并用 tooltip 说明「需先批准，批准后可登记金额并上传水单」**；另加行着色：已付清浅绿、部分付款浅橙，**按已付/应付金额判定而不是审批状态**（钱付清了但审批流没流转到 PAID 的情况财务照样要看得出来），并在表格上方加图例说明判定口径。验证：api jest **369**（+5）/ web vitest 374 / portal 15 / 三处 tsc 全绿；**零 schema 改动**。
- （本次·8-10 两条反馈：报价直接导 Excel + 颜色按色组分列）`feat(web)` **#66 颜色按色组分列**（Grace，附了工厂真实工艺单两张图）：工厂要横着看「每个色组下这条辅料用什么颜色」，而我们导出/打印把所有色组用逗号并进一格（"粉色，砖红"），工厂得自己数第几个逗号对应第几个色。样衣**导出 Excel** 与**打印**的「颜色」列现在按色组摊成「颜色一/颜色二/…」，列数 = 该样衣材料里最多的色组数；**只有一个色组时保持单列**，不给单色样衣平白加空列；用户在打印操作台里关掉了颜色列则不自作主张加回来。**口径沿用既有行为**：色组落库只有 `colors` 一个逗号串，样衣编辑页本来就是按 `[，,]` 还原色组的（`splitColors`），新增的 `utils/colorGroups.ts` 与它同一套拆法——**不是新发明的口径**。已知代价（已写进注释）：单个色组内部若自带逗号会被拆成两列，与编辑页表现一致；真要根治得把色组结构化落库，那是另一件事。**#65 报价明细直接导 Excel**（Grace：老系统里已有很多款报价，想直接导进来，不必先建样衣再从样衣导入）：报价页加「📥 Excel 导入」，走既有的「靠表头关键词自适应」机制而不是固定模板——她手上的老表列序五花八门，要求对齐模板等于没解决问题。为此把 `sheetImport` 的 `guessMapping`/`rowsToMaterials`/`parseSheetFile` **参数化字段表（默认仍是 `MATERIAL_FIELDS`，老调用方零影响）**，新增 `QUOTE_ITEM_FIELDS`；导入前弹预览，显示识别到的行数、**每个字段对应第几列**、前 5 行内容，再由用户选「追加」还是「替换」；没填损耗% 的行用顶部「统一损耗%」兜底，与粘贴路径同口径。**验证**：web vitest **374**（+14），并做**变异测试**——把色组摊开关掉、把字段表参数改回写死，5 条断言变红。api jest 364 / portal vitest 15 / 三处 tsc 全绿；**零 schema 改动**。
- （本次·**方向键在表格里悄悄改数量**——用户录屏实证的数据损坏）`fix(web)` 用户发来一段手机录屏（拍屏幕）。逐帧比对锁定现象：焦点从 3XL 往上走到 XL 的过程中，**沿途每一格「数量」都被 +1**（3XL 125→126、2XL 440→441），全程无任何提示。**根因**：`utils/tableKeynav.ts` 的注释白纸黑字写着「数字输入框不再增减数值」，但监听是用**默认的冒泡阶段**挂在表格容器上的，而 `el-input-number` 把方向键处理绑在内层 `<input>` 自己身上——**目标阶段先跑**。等事件冒到容器时数值早已被改，此时再 `preventDefault()` 毫无作用。另外 `preventDefault()` 只在「找到目标格」时才调，**首行按 ↑ / 末行按 ↓ 连拦都不拦**。**改法**：`addEventListener(..., true)` 改捕获阶段，并抽出 `swallow()` 同时做 `preventDefault + stopPropagation`——事件在抵达 input 之前就被截住，加减逻辑根本不会执行；上下键**一律吞掉（含边界不移动时）**，左右键仍只在真的换格时才截，光标在文本中间照常编辑。**影响面**：挂 `v-keynav` 的四张表里，`ContractEditView`（8 处 `el-input-number`，含合同材料的数量/单价）与 `QuoteEditView`（1 处）受影响；`OrderEditView`/`SampleEditView` 用的是普通 `el-input`，只是焦点不移不改值。**为什么原有 5 条用例没发现**：它们造的是**普通 `<input>`**——压根不会自增，「值有没有被改」在那些用例里根本不可观测（典型的空转覆盖）。新增 6 条，造了一个**会自增的输入框**来模拟 `el-input-number`，逐条复现录屏里的 125→126 / 440→441；并做**变异测试**：把监听改回冒泡阶段后 5 条断言以完全相同的方式变红（值差 ±1）。web vitest **360**（+6）、tsc 全绿。
- （本次·拉链三件套贯通全链 + 门户合同 Excel 改真实合同版式 + 清掉一个 NUL 字节）`feat/fix(all)` 用户给了**实物采购合同截图**，要门户导出照那个版式印。**先查清可行性再动手**：截图里的「拉头/拉齿/码带」三列，合同链路上**根本没有**——`code_band/puller` 只存在于**样衣材料**，`拉齿` 连样衣都没有，报价/订单/合同全都没带下来（生产真实数据佐证：`spec` 存的是门幅数字、`color` 全空）。用户拍板「把三列一直带到合同」，于是按红线一给**四张表加 10 列**（`sample_material.zipper_teeth`；`quotation_item`/`order_material`/`contract_material` 各加 puller/zipper_teeth/code_band），并打通**三跳带出**：样衣→报价（`buildItems`）、报价→订单（`buildMaterials` 与导入路径两处，少改一处值就在落库时被丢掉）、订单→合同（`expandMaterialLines` 的 base，分色分码展开的每一行都带）。四处录入界面各补列。**真库验证**：这三类列 2→12、二次执行幂等、与全新装机库逐列一致（`sample_material`/`order_material`/`contract_material` 完全一致；`quotation_item` 仅剩 `created_at`/`loss_price` 两个**生产历史遗留列**，init.sql 早不声明、与本次无关），并实测**老数据插入新列为 NULL 不报错**。门户 `contractExcel` 的货物表改为真实合同版式：「第一条　货物」标题、品名跨其配色行 `rowspan` 合并、款号/交货期限整列只写一次、末尾总价行附**中文大写金额**（新增 `rmbUpper`）。**写这版时被自己的用例抓出三个真错**：①`rmbUpper(10001)` 读成「壹万壹元」——万/亿分组的补零漏了，重写为「组内按权位、组间看首位是否为 0 补读一个零」；②`rmbUpper(null)` 给出「零元整」，而合同上「未填」与「零元」是两回事，改为空串；③新加的总价行在全表金额都没填时印出 `0.00`，**撞上了门户导出早就有的那条「未填≠金额为零」守卫**，改为有金额才印。**另清掉一处自己埋的雷**：`contract.service.ts` 里 BY_BOTH 的分组键曾误写成裸 NUL 字节（`${color}\x00${size}`），自 `20ed09b` 起已随两次发版进入生产——**功能无害**（该 key 只在函数内分组、从不外露，测试与线上都正常），但**整个文件被 git/grep 判成二进制，diff 与全文搜索全废**；改为 `JSON.stringify([color,size])`，顺带消除「颜色本身含空格（导入色号形如「10 White 增白色」）时拼串有歧义」的隐患，前端 `splitKeyOf` 同步，并把**分组键与展示文案拆开**（`key` 用于分组、`label` 用于「颜色+尺码」列显示，否则会把 JSON 串印给用户）。api jest **364** / web vitest **354** / portal vitest **15**（+9）/ 三处 tsc 全绿。
- （本次·供应商门户加反馈泡泡）`feat(portal/api)` 用户反馈 2026-08-08：「供应商门户没有反馈问题小泡泡，要加」。**不是照搬 PC 那份就完事**——两个坑：①`/feedbacks` 整体挂 `@Roles(...INTERNAL_ROLES)`，供应商令牌调过去一律 403，故新开 `portal/feedbacks`（`JwtAuthGuard + SupplierGuard`）；②**内部用户与供应商账号是两套各自独立的自增 ID**，而 `feedback` 只按 `user_id` 过滤「我的反馈/未读数/标记已读」——直接接进来会串号：内部用户#5 能看到供应商账号#5 的反馈，甚至把对方的回复标成已读。故按红线一给 `feedback` 加 `user_type ENUM('INTERNAL','SUPPLIER') DEFAULT 'INTERNAL'`，service 的 create/mine/unreadCount/markRead 全部带上它（门户控制器**每处显式传 SUPPLIER**，不依赖默认值，少传一次就漏一处）。**真库验证**：生产原本无此列 → hotfix 补上 → 二次执行幂等 → 与全新装机库 `feedback` 表逐列**完全一致**；并实测**不带 user_type 插入的老数据默认落 INTERNAL**，现有反馈仍归原提交人。**顺带**：`idx_user` 本想扩成 `(user_id,user_type,deleted)`，但 `_i9_add_index` 只增不改、存量库补不上会留结构漂移，且原索引已够用，**已撤回**。管理端列表与 HTML 导出给供应商来源加了标识。门户侧新增 `components/FeedbackBubble.vue`（Vant；泡泡避开 TabBar 与 iPhone 安全区、未登录/登录页自动隐藏、打开面板即消红点、图片走 uploadApi 上传后带 URL 提交），挂在 `App.vue` 根组件上，各页面不用各自引。**验证**：新增 `feedback.service.spec.ts` 7 条并做**变异测试**——把隔离条件去掉后 5 条断言全红，证明这层网真能挡住串号。api jest **364**（+7）/ web vitest 354 / portal vitest 6 / 三处 tsc 全绿。
- （本次·8-08 三条反馈：发货分色码对比 / 发票可见 + 一键生成付款申请 / 付款带款号）`feat/fix(all)` **#62 发货对比（按色/码）**：合同行在「分色/分码/颜色+尺码」生成时本就是拆开的，实发按批次物料行 `material_id` 归集，于是合同详情抽屉新增一张「订单数 / 已发数 / 差额 / 进度」表。**关键取舍**：门户发货时「按物料行填写实发数」是可选的，只填总量的批次没有 items、**无法归到具体色码**；若把它们忽略，某个色码会显示成「一件没发」，比不显示更误导——所以单独统计并提示「另有 N 来自 X 个未按行填报的批次」，并告诉用户让供应商打开按行填报即可精确。部分填报（行合计<批次总量）的差额同样计入。**#63 发票在哪看**：查明 `invoice_url` **存了、导出 Excel 也标注了，但 PC 界面上从来没有任何入口**——King 直接问「供应商上传发票后，在哪里看到呀？」。对账列表「含发票」列与详情抽屉各补一个入口，走 `openFile` 换短时签名链接（供应商上传一律落 `private/`，裸 URL 点开必 403）。**#63 按发票生成付款申请**：对账单已确认的行加「生成付款申请」，跳付款页并**自动开弹窗预填**（对账单/工厂/金额/款号），金额优先取发票金额（他要的是"按发票"），没票才退对账金额；真正的校验仍在后端（状态闸门/工厂一致性/累计超付）。**#64 付款申请关联款号**：`related_style_no` 此前只有手填，合同类付款一律为空，结算按款号归集自然一条也捞不出来；改为建单时从对账单 `style_no` 带出（手填优先）。**又踩到一次列宽**：来源 `reconciliation.style_no` 是 `varchar(200)`，目标 `related_style_no` 只有 `varchar(60)`——直接拷会 `Data too long`，截断则按款号归集直接失配、等于白做；按红线一加宽到 200（init.sql → `gen-column-sync.py` → **真库验证**：以生产当前结构建临时库→应用 hotfix→与全新装机库逐列/逐索引比对，`related_style_no` 零差异、二次执行幂等、索引完全一致，临时库已清理）。**验证过程中顺带查实一条既有漂移**：`customer.grade` 生产是 `null=NO`、init.sql 是 `null=YES`，属早已登记的 `_i9_sync_col` 可空性漂移，**与本次无关**（列存在、类型一致，仅可空性）。api jest **357**（+5）/ web vitest **343** / 三处 tsc 全绿。
- （本次·新增【AI工具集】菜单 + 场景 1「清关单据生成」，一份 PO 出齐三份清关件）`feat(web)` **AI 落地性专项，纯前端、不落库、不接单据链路**。
  新增 `utils/customsDocs.ts`（解析 PO / 装箱 / 三份工作簿）、`views/aitools/AiToolsView.vue`（场景卡片栅格，加场景=加一条 `scenes`+一条路由）、`views/aitools/CustomsDocsView.vue`（上传→选表→装箱参数→抬头→三页签结果，可单份导出也可一键出三份）；`@i9/types` `MENU_REGISTRY` 加 `ai-tools`（**纯代码改动，`menu_keys` 是既有列，不涉 schema**），`ROLE_DEFAULT_MENUS` 给 **SHIPPING 默认带上**（清关三件套本就是船务的活）。
  **解析**：不认死模板，靠**表头关键词**在前 30 行里找命中最多的一行（真 PO 表头在第 14 行，前面全是抬头/条款）；`HS CODE` 那列在有的 PO 表**表头是空的**（RSIN 就没写），按「夹在性别与尺码之间」的位置补，不然整列丢空；`TOTAL ORDER`/条款/空行按「款号或尺码为空、件数≤0」滤掉。
  **三份的关系**：箱单吃装箱结果、发票吃 PO 明细原样、装柜计划吃装箱结果按款号汇总——同源所以件数必然一致，界面上有互校指示灯。口径细节见上「一句话现状」。
  **导出**：exceljs 动态 import（不进主包）。箱单/发票按定单号分表（`<PO> PL` / `<PO> INV`），装柜计划一张总表；条码与 10 位商编强制文本，否则 Excel 转科学计数法；体积保留 **8 位小数**——单箱 CBM 0.0815625 是 7 位，截到 6 位整柜体积会跟客户对不上。
  **验证**：`utils/__tests__/customsDocs.spec.ts` **36 例**；web vitest 324 / api jest 352 / 四包构建 / 三处 tsc 全绿；真实 PO 实跑 + 三份产物用 openpyxl 逐格比对真实件（发票合计与 PO 的 TOTAL ORDER 分毫不差；装柜计划小计行号与合并区与真实件完全一致）+ 浏览器端走通全链路。
  **过程可见性（同轮追加）**：`SheetScan` 带出**原始行 + 表头行 + 列映射 + 跳过行归类**，界面据此做「列识别核对」抽屉（可手改列映射并当场重算）；`PoLine.srcRow` / `CartonLine.srcQty·seg` 让每一行都能回溯到源文件第几行、属于整箱段还是尾数段；新增 `inspectLines`（源数据体检，分级 + 写明影响哪份单）、`reconcile`（PO/箱单/装柜三口径逐款对账）、`explainRow`（装箱算式转人话）、`fillHsByStyle`（**跨定单补 HS，冲突一律不补只报警**）。
  **两个必须记住的坑**：①`SheetScan` 里带着整表原文，交给 `ref()` 会被深度代理成几万个 reactive 数组，**页面直接卡死且不报错**——必须 `markRaw`；②exceljs 读**整行合并单元格**会把同一句话在每列都返回一遍，条款行因此伪装成「有款号有尺码、只是数量不对」，跳过行归类须按「整行去重只剩一个值」特判。
  **装箱规格按款号（同轮追加）**：`specByStyle` 取代 `netByStyle`，每箱件数/箱规/净重逐款可覆盖；`loadStyleSpecs`/`saveStyleSpecs`/`toSpecFile`/`parseSpecFile` 管本地保存与导出导入（`normalizeSpecs` 滤掉空壳与非正数）。**第三个坑**：预设里的净重含义随 `netBasis` 变，口径错会让重量虚高近一个数量级——文件必须记 `netBasis`，没记就弹窗问，另有「平均单件净重」哨兵兜底。
- （本次·打印改成「排版操作台」，替掉上一版的列设置弹窗）`feat(web)` 用户反馈：上一版把「打印」和「列设置」拆成两个按钮**体验不好**，要求「点打印就进一个能自己设计页面元素的操作台」。**改法**：`samplePrint.ts` 整份改成**布局驱动**——抽出 `buildSampleHtml(detail, layout, autoPrint)`，区块取舍与顺序（基本信息/材料明细/寄样跟踪/寄样轮次/成衣备注/照片）、基本信息印哪些字段与顺序、材料明细印哪些列与顺序、纸张竖横、字号，全部由 `PrintLayout` 决定；新增 `components/PrintDesigner.vue`：左边元素面板（**原生 HTML5 拖拽排序**，不引第三方）、右边 **iframe 实时预览**，页脚「保存并打印」。**关键约束**：预览与真打印**共用同一个 `buildSampleHtml`**，只差 `autoPrint` 一个开关——两边一旦分叉就成了所见非所得，专门有用例逐字比对两份 HTML。`utils/printLayout.ts` 存 localStorage 并做**版本兼容**：以默认值为骨架合并，存档里没有的新区块自动补上（不会因为配置旧就少印一块）；纸张/字号越界、非字符串数组、JSON 坏掉一律退默认；并**迁移上一版只存列的旧键**（`i9.printCols.*`），用户刚配过的不白配，「恢复默认」时两个键一起清（否则下次又被迁回来）。上一版的 `PrintColsDialog.vue` / `printCols.ts` 已删除。**验证**：`samplePrint.spec.ts` 24 条（列/字段/区块的取舍与顺序、横版改 `@page` 方向且基本信息转三列、空区块不印空标题、弹窗被拦截给可操作提示、预览与打印同源、存档兼容与旧键迁移）；web vitest **309**、tsc 全绿。**未做**：只覆盖样衣制作单，报价/合同/订单的打印仍是写死的列（`printLayout` 的 docKey 已按单据分开，要扩照这套即可）。
- （本次·样衣打印列可自选与排序）`feat(web)` 用户反馈 2026-08-07：「打印排版能直接在系统调吗，打出来没有成一行，各列位置希望可以自己调，比如部位拉到品名后面。成分码带克重这种可以不用打印出来，占格子」。**11 列铺在 A4 竖版上，品名/备注必然折行，一条材料印成两行**。用户拍板口径：**①偏好每人各自记住（不入库）②靠砍列解决折行，不改横版、不缩字号**。做法：`samplePrint.ts` 把列抽成 `SAMPLE_MAT_COLS`（15 个可选列，含原来没印的拉链长度/拉头/实际耗用/供应商），`printSample(detail, colKeys?)` 按传入顺序出表头与单元格；`utils/printCols.ts` 存 localStorage（`i9.printCols.<单据>`）；`components/PrintColsDialog.vue` 勾选 + ↑↓ 排序 + 恢复默认，样衣列表与编辑页各接一个入口，**保存即打印**（省一次点击）。**健壮性**：认不出的 key 忽略（列改名/删除后旧配置仍可用）、全无效退回默认（绝不产出没有列的表）、localStorage 存坏了当没配过；未配置时**沿用改造前那 11 列**，老用户不会发现单据变样。另加 `td.c/td.r` 的 `white-space:nowrap`——门幅/数量/克重这类短值不再被挤折行，长文本仍可换行。**验证**：新增 `samplePrint.spec.ts` 14 条（表头顺序、砍列后单元格同步减少不错位、空明细 colspan 跟着列数走、坏配置兜底）；web vitest **278**。写用例时被自己的正则坑了一下：`<th[^>]*>` 会把 `<thead>` 也匹配进去（`<th` + `ead` + `>`），已改成 `<th(?:\s[^>]*)?>`。**注**：目前只做了样衣制作单；报价/合同/订单的打印仍是写死的列，要扩就照这套（`printCols` 的 docKey 已按单据分开）。
- （本次·线上报错 `Cannot read properties of undefined (reading 'readText')`）`fix(web)` **根因**：`navigator.clipboard` **只在安全上下文（HTTPS/localhost）存在**，而生产 nginx 只监听 80、是纯 HTTP，于是整个对象是 undefined。报价明细点「Excel 粘贴」当场抛异常。**这类问题在开发机永远复现不了**（localhost 算安全上下文），一上生产必挂，极易被当成偶发。**举一反三扫出同类两处**：客户列表、工厂列表的「复制当前页」用 `navigator.clipboard.writeText`，在 HTTP 下同样抛，而且是在 `.then` 之前就抛、**连失败提示都没有**。**改法**：新增 `utils/clipboard.ts` 收口读写两条路——读：能读就读，读不到弹一个多行框让用户自己 Ctrl+V（浏览器对用户亲手按 Ctrl+V 从不设限，任何环境都通），取消时抛 `CLIPBOARD_CANCELLED` 让调用方静默返回而不是报「粘贴失败」；写：退回 `document.execCommand('copy')`（已废弃但全浏览器支持且不要求安全上下文），失败返回 false 由调用方提示，绝不抛到界面上。全站已无直连 `navigator.clipboard` 的写法。**验证**：新增 `clipboard.spec.ts` 9 条，关键是把「没有 clipboard 的环境」在测试里造出来（`vi.stubGlobal('navigator', {})`），否则这条 bug 在 jsdom 里同样测不出来；web vitest **264**。**根治要配 HTTPS**（运维 P0 待办里本就有），配好后自动走原生路径、不再弹兜底框。
- （本次·8-07 五条反馈全部实现）`feat/fix(all)` **#57 合同模糊搜索**：`contract.findAll` 关键词从「只匹配合同号」扩到「合同号 / 关联款号 / 供应商名」。供应商名不在 contract 表（只有 `factory_id`），先按名字查一遍工厂取 id 再并进 OR 分支——不必把 `findAndCount` 改写成 QueryBuilder，分页/排序既有行为零改动；工厂无命中时**不加空的 IN 分支**（`IN ()` 会把结果清零），且每个 OR 分支都带上 `...base`，否则「按类型筛完再搜」会漏筛（这两条都有用例，且做过变异验证）。前端 placeholder 改「合同号 / 供应商 / 款号」。
  **#59 客户PO号自动带入**：矩阵 PO 列 → 客户PO号，Excel 导入后自动带、字段旁另有「从 PO 列带入」按钮。**踩到并绕开一次事故**：`customer_po` 原为 `VARCHAR(50)`，King 截图里 6 个 PO 拼起来约 90 字符，直接实现即 `Data too long`→保存 500。**按红线一加宽到 255**：改 `init.sql` → 跑 `gen-column-sync.py` 重生成 AUTO 段（diff 极小，仅此列）→ **真库验证**（以生产当前结构建临时库 `i9_verify` → 应用 `hotfix-schema.sql` → 与 `init.sql` 全新装机库 `i9_fresh` 用 information_schema **逐列 + 逐索引**比对：索引完全一致，列差异只剩 `quotation_item`/`sample_garment` 几个**生产历史遗留列**（init.sql 早不声明、幂等升级只增不减，与本次无关），`customer_po` 无差异；二次执行幂等；临时库与临时文件已清理）。带入逻辑**装不下就整个 PO 不带并提示还剩几个**，绝不把单号截一半。
  **#58 上传慢 + 粘贴**（Grace）：**先测再改**——往生产传 2MB 实测 2.9 秒（≈665KB/s），后端 `FileService` 只是 multer 落盘、无图像处理，故瓶颈是**整张手机原图（3-10MB，上限 20MB）走用户上行带宽**。新增 `utils/imageCompress.ts`：长边 >2000 才缩、>1.5MB 才压、只认 jpeg/png/webp（gif 不压否则动图只剩一帧；PDF/Excel 绝不碰）、压完更大或 canvas 不可用**一律退回原图**（压缩是锦上添花，不能让本来能传的传不上去）、输出改 `.jpg` 与 magic-byte 判定对齐。粘贴那半：UI 早写着「聚焦后 Ctrl+V」，只有反馈弹窗开了 `globalPaste`——正是她对比的那个；改为所有实例进 `pasteStack`，document 单监听按**后挂载优先**派发、本框满了让给下一个，于是弹窗一开自然接管、关掉还给页面表单，不会「粘贴到反馈弹窗、图却进了样衣表单」；只接文件型剪贴板内容，纯文本粘贴不受影响。
  **#61 统一损耗%**（daisy，承接 #44 让其重提的口径）：报价明细工具条加「统一损耗%」，改动即刷全表，新增/Excel 粘贴的行也用它（此前粘贴处硬编码 3，统一值形同虚设）；**保留每行的损耗%列**——拉链/织带与面料损耗本就可能不同；打开既有报价时各行不一致就留空、不假装统一。
  **#60 颜色×尺码同时拆**（King）：`split_mode` 增 `BY_BOTH`。**故意不叫 `BY_COLOR_SIZE`**——该列是 `VARCHAR(10)`，13 字符会 `Data too long`（CLAUDE.md「组合值超列宽」同类），用例里直接断言了长度上限。后端 `expandMaterialLines` 按「颜色 空格 尺码」成组，**颜色或尺码缺一个就跳过该矩阵行**；`size_specs` 按**纯尺码**取（用组合键「黑色 S」会查不到、尺寸带不进合同——这条专门有用例并做了变异验证）；`qty_source` 写「采购量·分色分码」（8 字符，`qty_source` 是 `varchar(20)`，已核）。前端 `splitPreview` 与后端**共用同一套分组口径**，否则预览与真出的合同对不上；「尺寸」列改按 `srow.size` 存取。
  **验证**：api jest **352**（+14）/ web vitest **255**（+13）/ 四包构建 + web·api·portal 三处 tsc 全绿；变异测试确认新断言非空转。
- （本次·合同/报价导出件里的图片不显示，同 8-06 样衣那条）`fix(web)` **根因**：`docExcel` 的 `.xls` 是 HTML 工作表，图片只能内联成 `<img src="data:image/...;base64,...">`，而 **Excel 打开 HTML 工作表时不渲染 data: URI 图片**（文件里有、界面没有、无报错）——这是格式层面的限制，调参数解决不了。**改法**：公共层加**第二个渲染出口 `exportDocXlsx()`**（exceljs，动态 import，主包不受影响），吃同一份 `Block[]`，新增 `imgCell()` 图片单元格；`contractExcel.ts`（材料照片列）与 `quoteExcel.ts`（款图，**举一反三扫出来的同类未修**）改走这条，文件名同步 `.xls→.xlsx`。**取证方式（不是只跑 mock 单测）**：scp 生产 `/data/uploads` 里最大的真实照片（2480×3508 / 1.7MB）跑真实导出链路落盘 → `unzip` 核对 `[Content_Types].xml` 有 `jpeg` 声明、`sheet1.xml.rels → drawing1.xml → media/image1.jpeg` 关系链完整、`oneCellAnchor` 落在 col=12(照片列 M)/row=24(材料行 25) **无错位** → macOS QuickLook 独立渲染**肉眼确认图显示出来**。**由此发现两个单测发现不了的缺陷并一并修掉**：①原实现把 `ext` 定死 96×72，竖图被拉扁——新增 `imageSize()` 从字节解原始宽高（PNG IHDR / JPEG SOFn（跳过 0xC4 DHT 这类假 SOF）/ GIF 头；只解前 96KB；纯字节不依赖 DOM，浏览器与 jsdom 一致），再**等比缩进方框**（合同材料照 110、款图 240）；②标题/表头行是「自动行高」，各渲染器算的高度不同，图会上飘压住表头——现钉死标题/表头行高，数据行保持自动（否则长备注 wrap 被切）。**顺带清账**：`toDataUrl`/`splitDataUrl` 由 `sampleExcel` 上移到公共层（此前是公共层 import 具体单据，依赖方向反了）；`downloadXls` 改为延后 10s revoke（立刻 revoke 在部分移动浏览器上会让下载拿不到数据，门户那份早有此注释）；`val()` 兜住图片单元格误走 HTML 路径时不写出 `[object Object]`；工作表名过滤 Excel 非法字符（补料号形如 `补料-母合同号-序号`，一旦混进 `/ : [ ]` 会让 `addWorksheet` 直接抛错、整份导出失败）。**验证**：web vitest **242**（+22）——新增 `contractExcel.spec.ts`（11 条）、`docExcel.spec.ts` 扩到 20 条、导出测试夹具统一收进 `__tests__/xlsxTestKit.ts`（读回 xlsx 断言真实产物，不再断言中间字符串）；并做**变异测试**：把 `contractExcel` 改回旧的 data-URI 写法，确认两条关键断言真的变红（`expected +0 to be 1`），证明这层网不是摆设。tsc 全绿；**零 schema 改动、零后端改动**。**明确未做**：门户合同导出仍不含照片（移动端包不值得为一列照片引 ~1MB exceljs，且白名单口径「屏幕上没有的文件里也不该有」）；对账/付款/结算无图片列，继续走 `.xls`（要迁只需换个出口函数，区块声明不动）。
- （本次·回滚路径加固 + `grep -q` 竞态清账）`fix(ops)` **① `rollback.sh` detached HEAD（真隐患，原待办没提）**：原来 `git checkout "$TARGET"` 让服务器脱离分支，而服务器配 `receive.denyCurrentBranch=updateInstead`——该配置**只在「推的分支正是当前检出的分支」时才更新工作树**。detached 之下下次 `deploy-local.sh` 的 `git push ecs main` 是合法的 ref 更新（分支没被检出，不受 deny 保护）但**工作树不动**：dist 新产物照常 rsync 上去、API 照常重启，源码树却停在回滚版——`deploy.sh` 从旧源码树读 `hotfix-schema.sql` 做结构升级，全程无一句告警。改为 `BRANCH=$(git symbolic-ref --short -q HEAD)`，在分支上就 `git reset --hard "$TARGET_FULL"`（只挪分支指针+工作树，不碰未跟踪的 `.env.production`），后续发版仍是正常快进；确实处于 detached 时保留 checkout 但打警告提示复位。**② 没有采纳待办里「开 `--skip-build` 通道」的建议**——服务器 dist 正是要滚掉的那版，跳过构建后代码回了、跑的还是坏产物，**等于没回滚且毫无提示**，比 OOM 后果更糟；构建在回滚场景是必需的，已在脚本头注释写清原因防后人再图快。**③ 原待办的 OOM 前提已过时**：待办援引 7-16 事故的「1.8G 无 swap」，但真机 `swapon --show` 核实现有 **4G swapfile（仅用 117M）**，条件不再成立，风险从「OOM 杀 mysqld」降为「构建挤 swap 拖慢 mysqld」。故不禁止构建，改为**内存 preflight**：`MemAvailable + SwapTotal < 2048MB`（可 `MIN_BUILD_MEM_MB` 覆盖）即 `die`，并给出两条替代路径（开发机 `reset --hard` + `push --force-with-lease` + `deploy-local.sh`；或 `fallocate` 加 swap 后重跑）。另补两个小守卫：目标 commit 用 `--verify "<t>^{commit}"` 校验（原来 `rev-parse` 对 tree/blob 也会成功）、目标==当前版直接拒绝。**④ 剩余 4 处 `cmd | grep -q` + `pipefail` 竞态清账**（同 deploy.sh 那个坑，后果轻得多）：`health.sh` nginx 探活改为自读 `%{http_code}` 判断（顺带去掉 CLAUDE.md 明令不用的 `curl -f`——这里本就靠状态码自判，`-f` 只会在 4xx 时让 curl 非零退出反而干扰；失败时把实际码带进输出便于排查）、`health.sh` redis 容器探测走新 `has_line()`、`clean-db.sh` MySQL 容器探测（误判会**误 die**）、`upgrade-db.sh` pm2 探测改纯 bash 子串匹配。**⑤ 更正 HANDOFF「如何接手」里的不实记载**：原写「推送凭据在 `.env.local`」，实测该文件在开发机上**不存在**（`.gitignore` 忽略了它，从未随仓库分发），照着找必然扑空；已改为记录实情——GitHub 只收 PAT、keychain 里无 github.com 条目、SSH 未配公钥，`git push origin main` 会交互式索要凭据，且**`deploy-local.sh` 只 `git push ecs main` 不推 GitHub**（今天两次发版后 GitHub 都落后，需手动补推）。**验证**：4 脚本 `bash -n` 通过；**新 `health.sh` 真机实跑**（只读）六项输出与改前逐项一致、全绿 exit 0；`rollback.sh` 真机取 `symbolic-ref` = `main`（确认走 reset 分支而非 detach 分支）、内存实测 867+4095=4962MB 判「放行」，再用 `MIN_BUILD_MEM_MB=99999999` 反向实测**闸门在确认提示/`git reset`/部署之前就 `die`（exit 1）**、事后核对服务器仍 `6de9954` on `main` 未被触碰。**未做**：没在生产实跑真回滚（要把线上退回旧版再滚回来，收益不抵风险）；回滚的执行段（reset→`deploy.sh --skip-pull`）沿用既有路径、逻辑未改，本次改动集中在「怎么切版本」与「切之前拦不拦」。**无业务代码改动**，未跑四包构建与单测。
- （本次·发版失败没有回滚路径：回滚提示指向出问题那一版）`fix(ops)` **问题**：`deploy.sh` 第 53 行 `PREV_COMMIT=$(git rev-parse --short HEAD)` 记「发版前 commit」，`die()` 据此打印 `rollback.sh $PREV_COMMIT`。但默认发版路径是开发机 `deploy-local.sh`——**① `git push ecs main`（第 54 行）→ ② ssh 调 `deploy.sh --skip-pull`（第 65 行）**，服务器 `receive.denyCurrentBranch=updateInstead`（真机 `git config` 已核实），push 一到工作树当场更新到新 commit，等 `deploy.sh` 再读 HEAD 已经是新版本。实证 8-03 两次发版：`部署成功 commit=54ca4c7 (上一版 54ca4c7)`、`commit=bb737b3 (上一版 bb737b3)`。后果：**中途失败时打印的回滚命令把你回滚到出问题的那一版**（`rollback.sh` 走 `git checkout <commit>` 重新部署，目标给错就是白回），等于没有回滚路径。**改法**：①**`deploy-local.sh` push 前先取**——`SRV_PREV=$(ssh $REMOTE "git -C $APP_DIR rev-parse --short HEAD")`（校验 `^[0-9a-f]{6,40}$`；取不到只 warn 不阻断发版），以 `PREV_COMMIT=$SRV_PREV` 前缀传给远端 `deploy.sh`，收尾行也带上「(上一版 xxx)」。②**`deploy.sh` 加状态文件 `$STATE_DIR/last-deployed-commit`**（默认 `/var/lib/i9`，**跑完整条部署才写**）+ `resolve_prev_commit()` 按 **①上次成功部署记录 →②传入值 →③未 `--skip-pull` 时的当前 HEAD →④`HEAD@{1}`** 取值：候选必须在本仓库真实存在（过期/传错的记录不打一条跑不通的命令），`--skip-pull` 下候选等于当前 HEAD 即判定无效继续下找（覆盖"失败后不重推直接重跑"）。**记录优先于传入值是有意的**——上次发版中途失败时，传入值和 HEAD 都指向那个失败版本，只有记录仍指着最后一个真跑起来过的版本；`rollback.sh`（`git checkout` 后调 `deploy.sh --skip-pull`）同理受益。四个来源都拿不到就打「未能确定发版前的 commit……请先 git log 挑目标」，不再打假命令。③**补 `trap ... ERR`**：`rsync`/`systemctl restart`/`pnpm` 这些没包 `die` 的命令失败时此前被 `set -e` 直接掐掉、连一行回滚提示都没有（"任一步失败都会打印回滚命令"并不成立），现统一打「部署在第 N 行中断（exit=N）」+ 回滚路径，退出码原样透传。④顺带：`LOG_FILE`/`WEB_ROOT`/`PORTAL_ROOT` 改为可 env 覆盖（沙箱验证需要，生产默认值不变）；日志目录 `mkdir` 提到 ENV 检查之前（否则首次装机时第一条 `die` 的 `tee` 写不进日志）。**验证**：搭本地「开发机 + 服务器」沙箱——两个真 git 仓库（服务器侧配 `receive.denyCurrentBranch=updateInstead` 复刻真机），ssh/rsync/pnpm/docker/systemctl/curl 全打桩，**不碰任何真机**——9 项断言全过：**R1** 用修复前的 `deploy.sh` **复现原 bug**（提示指向刚推的新 commit）；**R2** 首次发版（无历史记录）+ 故意让结构升级 SQL 失败 → 提示 = 发版前那一版 ✓；**R3** 失败后重跑（无传值、HEAD 已是新版）走 reflog 仍拿到发版前那版 ✓、跑通后落盘记录 ✓；**R4** 下一次发版失败 → 取上次成功部署的版本、不是刚推的新版 ✓；**R5** 让 `rsync` 静态文件失败 → ERR 兜底仍打回滚路径、退出码 12 透传 ✓；**R6** 服务器直接发版 / CI 路径（不带 `--skip-pull`）→ 取拉码前的 HEAD ✓。另单测「四个来源都没有」→ 打「未能确定」而非假命令 ✓。**未在生产实跑失败演练**：那要故意让线上发版失败，收益不抵风险；真机只做只读核实（`receive.denyCurrentBranch=updateInstead`、bash 4.4、push 会写 `HEAD@{1}` reflog）。**验证过程顺带揪出一个更凶的隐患并一并修了**：`deploy.sh` 判断容器存在用的是 `docker ps -a --format '{{.Names}}' | grep -q "^i9_mysql$"`——`grep -q` 一命中就退出，`docker` 还没写完就吃 **SIGPIPE**，`set -o pipefail` 下整条管道返回 141，`if` 判成假 → **静默跳过整个数据库结构升级**、然后照常重启 API，只留一条「未发现 i9_mysql 容器」warn。这正是**红线一**最怕的「发版后 Unknown column 全线报错」，而且现场只看得到一行 warn。沙箱里真随机踩到过一次（第一遍 R6 挂了、单独重跑 3/3 又好，就是它）。改为 `has_container()`：先把 `docker ps` 输出落进变量，再用纯 bash `[[ $'\n'"$names"$'\n' == *…* ]]` 匹配，没有管道就没有竞态；Redis 那两处同款写法一并换掉。R8 专测这条：把桩 `docker` 的两个容器名分两次写出（放大竞态）——修复前 100% 判成「未发现容器」跳过升级，修复后照常进结构升级。**同类写法还在 `health.sh:35/61`、`clean-db.sh:42`、`upgrade-db.sh:95`**（后果轻得多：误报 redis ✗ / 误 die / pm2 探测失准），已登记待办、本次未动。沙箱脚本未入库（在会话临时目录），需要可再要。**无业务代码改动**（只动两个发版脚本），故未跑四包构建与单测；两脚本 `bash -n` 通过。
- （本次·AUTO 段补上索引同步，「立即待办 4」清账）`fix(ops)` **问题**：`gen-column-sync.py` 的 `parse_tables()` 把建表体里所有约束行（`CONSTRAINT_PREFIX` 命中即 `continue`）直接跳过，只生成建表和补列 → `init.sql` 里声明的索引对已上线的存量库**永远补不上**；8-03 上一条改动新增 `contract_material.idx_order_material` 就是靠手工在 hotfix 手工段写 `_i9_add_index` 才补上的，同一个漏洞对以后每个新增索引都成立。**改法**：①`parse_tables()` 改为「不以反引号开头的行 = 约束行」再分流（已核对全部 45 张表，约束行只有 `PRIMARY KEY`/`KEY`/`UNIQUE KEY` 三种）——`PRIMARY KEY` 跳过（随建表产生，也不能用 `ADD INDEX` 补），`KEY`→`_i9_add_index`、`UNIQUE KEY`→`_i9_add_unique`，多列索引**按 `init.sql` 原列序**输出（列序决定能否命中，不可重排）；②索引段排在**所有表的列都补齐之后**——新增列上的索引若在补列前跑，`ADD INDEX` 会直接报错中断发版；③**认不出的约束行改为报错退出**（前缀长度 `col(20)` / `DESC` / `FULLTEXT` / `SPATIAL` / 外键这些 `ADD INDEX` 拼法表达不了的形式），不再静默丢弃——这次的坑根源就是"静默跳过"，宁可发版前失败。产物从 45 表/666 列 → 45 表/666 列/**77 索引**（60 普通 + 17 唯一），生成器重跑产物一致（幂等）；与 `init.sql` 声明的索引集合做程序化比对 **77↔77 完全一致、不缺不多**；解析器的收/拒行为逐形式测过（普通/UNIQUE/多列带空格/`INDEX` 关键字 → 通过；前缀长度/FULLTEXT/DESC/外键 → 拦截退出）。**红线一验证**（本机无 MySQL/Docker，改在服务器临时库；生产库全程只被 `mysqldump --no-data` 读结构、未写入，临时库跑完已 DROP）：`i9_verify_old`=生产当前结构基线 / `i9_verify_new`=全新 `init.sql` → **升级前** information_schema 索引 diff 实测**正是那 5 个缺口**（`customer.idx_type`、`factory.idx_type`、`feedback.idx_user`、`reconciliation.idx_style_no`、`settlement.idx_style_no`，与 8-03 首次实证完全吻合）→ **应用 hotfix**（退出码 0、零报错、`_i9_add_unique` 零重复值告警）→ **升级后索引缺口为空、反向也无多余索引**、列缺口除下述历史漂移外为空 → **二次执行 hotfix 幂等**（退出码 0、列与索引结构零变化）。**顺带发现一处既有漂移（非本次引入，未修）**：`customer.grade` 生产为 `NOT NULL DEFAULT 'B'`、HEAD `init.sql` 为 `DEFAULT NULL`，因 `_i9_sync_col` 只比 `COLUMN_TYPE`（两边同为 `enum('A','B','C')`）而看不见可空性/默认值差异；当前不报错（DTO `grade` 是 `@IsOptional()`，不传即用库默认值），只有显式传 `grade: null` 才会撞 `cannot be null`，已登记为「立即待办 5」。另核实：hotfix 直接灌**全空库**会在第 97 行 `_i9_add_col('factory','extra_types',…)` 报 `Table doesn't exist` 而中止——这是「枚举语义迁移」手工块的既有性质（手工块假定表已存在，且全新装机本就走 `init.sql`，不走 hotfix），发生在 AUTO 段之前，与本次改动无关。**无代码改动**（只动生成器脚本 + 其产物 SQL，不涉及 TS/实体/接口），故未跑四包构建与单测；`init.sql` 本身未改，本次不新增任何索引，只是让已声明的索引真正落到存量库。
- （本次·8-03 两条反馈：合同类型文案 + 订单标出已生成合同的材料）`feat(order/contract)` ①**类型文案统一「材料合同」**（King，页面 `/contracts`，截图圈的是类型列）：枚举值 `MATERIAL` 不动，只改展示。代码里本就分裂——`contractExcel.ts`、`ContractEditView`、`ContractPicker` 早叫「材料」，`web/ContractListView`（筛选 el-option + typeLabel）与 `portal/ContractListView`、`portal/ContractDetailView` 还叫「面料合同」，4 处改齐；`contractPrint.ts` 的「原料/辅料购销协议」「委托加工合同」是法律文书标题，保留。②**订单里标出已生成合同的材料**（King，页面 `/orders/32/edit`：「已经生成合同的材料，能否在订单里标注一下颜色？未标颜色表示没有订」）。用户拍板要**精确到行**，故加真关联而非按品名猜：`contract_material.order_material_id`（+`idx_order_material`，init.sql 与 hotfix 同步，AUTO 段已重生成）。`expandMaterialLines` 的 base 写入 `om.id`——分色/分码展开出的多行合同明细都指回同一条订单材料；`contract.update` 的明细是「整表删了重建」，加 `prevSrc`（品名|颜色|尺码 → 旧行 order_material_id）接力，否则编辑一次合同就把订单侧标记冲没；`order.findOne` 增一条原生 SQL（JOIN contract 过滤 `deleted=0`，GROUP BY 折算分色多行）按行返回 `contracted` + `contracts[{id,contract_no}]`；`OrderEditView` 材料表 `row-class-name` 上浅绿底 + 品名列 `已订` 标签（tooltip 显示合同号）——底色之外给标签，不让色弱用户只能靠颜色分辨。**连带根治一个会让新外键立刻失效的隐患**：`order.update()` 原本 `delete({order_id}) + 全量重插`，每保存一次订单材料行 ID 全变（草稿单生成合同后仍可编辑，这条路径真实存在）→ `order_material_id` 悬空、标记全丢。改为 reconcile：`CreateOrderMaterialDto` 加可选 `id`，前端 load/buildDto 原样带回，后端带 id 原地更新、只删本次未提交的行，并加**越权守卫**（传别单的行 ID 一律当新增，杜绝把别单材料改绑过来）。存量合同由 hotfix 手工段一次性回填（「同订单 + 同品名 + 合同供应商=订单材料供应商」，多行同值取 MIN(id) 保证确定性，只填 NULL 行故幂等可随发版重放；挂「待定供应商」占位工厂的匹配不上保持 NULL=显示未标记）。**验证**：api jest **332**（新增 UT-ORD-21/22/23、UT-CON-33/34）/ web vitest 194 / types+api+web+portal 四包构建全绿。**红线一**：本机无 MySQL/MariaDB/Docker，改在服务器建临时库验证（生产库全程只被 `mysqldump --no-data` 读结构）——以**生产当前结构**为老基线 → 应用 hotfix → 新列✓/`idx_order_material`✓ → 与全新 `init.sql` 用 information_schema 做列+索引完整性 diff（差异项全部为本次改动之外的历史漂移：生产多出的孤儿列、以及 init.sql 有而生产缺的 5 个索引 `customer.idx_type`/`factory.idx_type`/`feedback.idx_user`/`reconciliation.idx_style_no`/`settlement.idx_style_no`——AUTO 段只补列不补索引，属既有缺口，已登记待办）→ 二次执行 diff 为空（幂等✓）→ 临时库已 DROP。另在真库用样本数据实跑了 `findOne` 的新 SQL（分色两行折算成一条✓、已删合同的行被排除✓、`order_material_id IS NULL` 的手工行不炸✓）与回填 SQL（101/102→11、103→12，无匹配保持 NULL✓）。**未做**：`deep-test.sh` 端到端未跑——它会往库里写测试单据，生产库不宜跑；`order.update` 的 reconcile 在真 MySQL 上只验了 SQL 语义、未走完整 API 链路，发版后需实际改存一次订单确认「已订」标记不丢。
- （本次·门户打印合同 + 工厂联系人补列）`feat(portal/factory)` ①**门户打印合同**（供应商微信截图：「添加一个打印按钮吧，合同需要能打印纸质盖章了再上传盖章照片」；用户确认「电脑登门户要能打印一张合同出来」）：新 `portal/src/utils/contractPrint.ts`（与 PC 端 `contractPrint.ts` 同版式：材料/加工两套模板、甲乙方按类型、terms_json 优先模板兜底、担保条款、双方电子章贴图、A4 @page），`ContractDetailView` 吸底操作区加「🖨 打印合同（纸质盖章用）」按钮（全状态可见），未盖章时落款提示打印后拍照回传「纸质盖章照片」——与既有纸质盖章上传流程闭环。后端 `portal.service.getContract` 补查 `factory`（按 token factory_id 取，select id/name/address/contact_name/contact_phone/seal_url）与 `company`（默认主体优先，select 抬头/地址/开户行/章图），随详情一次返回；`PortalModule` 注册 Factory/CompanyProfile 两实体，门户 spec 补两仓储 provider。②**工厂联系人子表补列**（Grace 反馈 7-28「新建工厂电话也填写不了」）：设计稿联系人子表 7 列，实现只有 姓名/手机号码/电子邮件/备注 4 列——补「部门/职务/电话号码(座机)」三列（badge 改 7 列）；`factory_contact` 实体/DTO/`contact_phone` 首行回填（phone||mobile）链路本就在，纯前端漏列，零后端改动。同反馈的「江苏城市不全」远端 f9e8053 已修（全国 34 省级补全+城市 allow-create），本地重复修改已弃、stash 合入仅留本列改动。验证：api jest 全量过（portal 20/20）；portal vue-tsc 0（portal 无 vitest 用例）；web 工厂单测 4/4。**无 schema 变更，不涉及红线一**；本机无真库未跑 deep-test（纯查询+前端改动，风险低）。
- （本次·导入预览色组分列）`fix(sample-import)` Helen 反馈（7-30）：导入对话框预览把 颜色一/颜色二 合并显示在一列「颜色还是合并在一列里」。①预览表格**色组动态分列**——`sheetMaxColorGroups` 按各行最大色组数自动出列（色组1/色组2…），每列独立显示；②`rowsToMaterials` 的 extraColorCols 拼接**取消去重**——源表两列同值也保留两列结构（她的工艺单就是 颜色一/颜色二 两列，去重会把 色组二 抹掉；注释写明清真原因）。新增 3 例单测（分列拼接/同值保留/空列不悬空）。验证：vue-tsc 0 / vitest **194** 全绿；Playwright 实测上传含 颜色一/颜色二 的 xlsx，预览表头「色组1 | 色组2」且值正确分列 ✓。
- （本次·导入解析「举一反三」）`fix(import)` 针对 7-29 工厂导入全空行做全站同类排查，新公共解析器 `utils/parseTable.ts`（CSV/TSV/分号自适应 + 引号包裹内嵌分隔符/换行安全 + BOM/\r 处理 + 首行表头识别），统一收口 4 处自写 split 的实现：①`CsvImportDialog`（5 模块导入共用）；②报价明细 Excel 粘贴 `pasteRows.ts`（旧按行 split，遇单元格内嵌换行必断行）；③报价/订单历史导入 `split(/\t|,/)`（名称含逗号即错列；改位置映射+首行像表头自动跳过）；④工厂导入**省市后缀归一**（normProvince/normCity 列表驱动匹配「广东省→广东、东莞市→东莞」，注释明确不写死后缀——吉林市/吉林省并存防误杀）。新增 `parseTable.spec.ts` 9 例（TSV/引号逗号/内嵌换行/转义引号/分号/BOM/松散引号/表头映射/跳表头）。验证：vue-tsc 0 / vitest **191** 全绿；Playwright 实测 Helen 原数据 TSV 粘贴全列解析 ✓ 通过。
- （本次·7-29 三条反馈）`fix(import/sample/contract)` ①**工厂导入全空行根治**（Helen「厂商名称填了怎么导入不了」）：`CsvImportDialog` 只认逗号文本——Excel 直接复制是 Tab 分隔（粘贴进一格→表头对不上→全部列空+厂商名称必填）、上传 .xlsx 是二进制（readAsText 出乱码）。共享组件升级：**xlsx 走 exceljs 动态解析（.xls 明示另存）+ Tab/逗号自适应**，工厂/客户/样衣/报价/订单五处导入同时受益；模板说明同步。真机实测 Helen 原数据 TSV 粘贴「通过 1 行」✓。②**色组分列**（Helen「色组一/二分开导，一款四五个颜色」）：样衣材料颜色改**色组动态多列**——`colorGroups` 数组为编辑源（每色组一列、＋色组扩列、逐列删），`colors` 逗号串同步存储（零 schema 改动，buildDto 剥离辅助数组防 whitelist 400）；载入/复制行/表格导入三处同步分列。③**合同日期语义**（Grace「发货日和交货期限为啥不一样」）：本就是两个字段——最后发货日=整单出货截止日、交货期限=每行材料应交日期（默认订单交期−45 天通常更早），「最后发货日」加 tooltip 说明。验证：vue-tsc 0 / vitest **182** 全绿；Playwright 实测 TSV 导入通过 ✓。
- （本次·「举一反三」系统性扫查：四类模式 20+ 项隐患全清）`fix(all)` 用户「这个系统的 bug 太多了，排查是否有类似问题」。三路扫查（API 长度+空值 / web 下拉+批量+派生 / 打印导出嵌图）+生产报错记录核查（3 条历史 500 均已修未复发），修复：①**空串/null 写列 500**——contract.service update 加 NON_NULL 守卫（账期/比例/币种/工厂 el-input-number 清空给 null 裸写 NOT NULL 列 500）；export-invoice create `invoice_date || null`、addReceipt 日期必填拦截+汇率归一；payment addRecord pay_date 必填+pay_method `|| 'BANK'`；settlement refundReceived 排除空串（「留空按预估」被 `+''` 清 0 矛盾）；sample update 主档 sampleQty/patternmakerId ''→null。②**字段长度**——颜色链 `quotation_item.color(50)/order_material.color(100)/contract_material.color(50)` + `contract_material.spec(100)` 全放宽 VARCHAR(200)（7-28 双色组合并把长度顶到，>50 即 Data too long 500，扫查agent 实证该放大）；reconciliation expense_name DTO 补 @MaxLength(200)。③**下拉/批量/派生**——regions.ts 补全 34 省级行政区全地级市+省份下拉 allow-create；工厂联系人姓名与电话同款 computed setter 可写；报价外销币种/订单币种统一改 DictSelect currency（原 5 项/2 项硬编码且 RMB/CNY 口径不一，汇率由字典 @item 直接带出）；订单材料加批量设置供应商/单位/损耗%（与样衣同款）；表格导入供应商按名称匹配工厂库回填 supplierId（不再显示空白）+对话框明示图片不导入。④**打印导出**——samplePrint 寄样多轮兜底+轮次明细表（多轮样衣单号/日期不再「—」）；quotePrint 补贸易国别/结汇方式/价格条款/备注说明+列表/批量打印带款图；quoteExcel 款图 base64 内联+补 4 字段；orderPrint PO 列头补收货人+佣金（内部单）+附件档案区（彩稿等图片嵌入/非图文件名清单）；contractPrint/contractExcel 材料照片列（导出转 base64 内联）；对账发票/付款水单/结算水单/合同纸质章 4 处敏感附件导出裸 URL 死链统一改 `sensitiveMark`「🔒系统内查看」标注。**验证**：api tsc 0 / jest **327**；web vue-tsc 0 / vitest **182**；红线一真库：hotfix 升级✓/与 init.sql 集合 diff 空✓/幂等✓；**真栈冒烟 3/3**：发票无日期 null 入库✓、合同 PATCH null 账期保值 90✓、55 字符颜色落库✓（冒烟数据已清理）。**遗留（如实登记，未动）**：PUT 端 `Partial<X>` 使 ValidationPipe 失效的架构根因（本次按字段点修；全量改 PartialType 有白名单回归风险，需专门评估）；款号下游列宽 50/60→100 对齐（客户款号>50 罕见）；订单单据级 Excel 导出（大功能另立）；样衣类别字典化；对账出货明细 shipment_id 裸数字输入改下拉（E1，需新后端端点，下批做）。
- （本次·7-28 导出件 7 条反馈全量落地）`feat(sample/factory/print)` ①**材料克重列**（daisy）：`sample_material.gram_weight` VARCHAR(30)，编辑表/样衣制作单打印/导出 Excel/表格导入映射（克重|平方克|gsm）全链贯通；材料行 ↑↓ 调序（sort_order 按数组序落库）。②**打印嵌图+基本信息**（daisy）：制作单加样衣尺码/数量、底部嵌样衣照片图稿（image1/2/3 逗号多图）；导出 Excel 照片抓图转 base64 内联（>2MB/失败退回链接不炸）。③**批量设置供应商**（Helen）：材料表勾选行 → 工具栏选供应商 →「应用到所选」一次写入。④**两组颜色合并导入**（Helen）：颜色一/颜色二多列自动合并去重（rowsToMaterials extraColorCols），克重进映射字段（15→16）。⑤**部位 >50 字符保存失败根治**（Helen 实证 89 字符）：`sample_material/quotation_item/order_material` 三表 part 放宽 VARCHAR(200) + 三处 DTO @MaxLength(200)（材料行样衣→报价→订单全链一致，防下游再次截断）。⑥**省市数据补全**（Grace）：江苏 7→13 市、全国 19 省全地级市（regions.ts），城市下拉 allow-create 可手填县市；**工厂联系人电话**从 readonly 派生改可写（写回首行联系人，无联系人自动补行）。**schema 红线一**：init.sql+gen-column-sync 重生成，真库升级✓/与 init.sql 集合 diff 空（45表/709列）✓/幂等✓。**验证**：api tsc 0 / jest **327**；web vue-tsc 0 / vitest **182**（+11：导出克重+照片内联/失败退回）；真栈 89 字符部位+克重落库 ✓；Playwright 实测江苏 13 市齐全 ✓（冒烟数据已清理）。
- （本次·分码材料「各码尺寸」）`feat(order/contract)` 用户截图反馈「拉链要分码，但每个码尺寸不同，只能填一个尺寸」+拍板方案（size 列 `S(50)`、自由文本）。①**schema**：`order_material` + `size_specs` JSON 可空列（`{"S":"50","M":"52"}`；空=各码同尺寸，存量零影响；init.sql+gen-column-sync 重生成，红线一真库验证：升级✓/diff空✓/幂等✓）。②**订单编辑页**：「分码出量」表新增可编辑「尺寸」列（仅 BY_SIZE，占位=材料行门幅/尺寸，不填即通用；只读态显示文本），保存/载入两端透传（仅 BY_SIZE 且有值才写）。③**合同两条生成路径同口径带尺寸**：后端 `generateFromOrder` 的 `expandMaterialLines` 分码行 `size=S(50)`（无尺寸码回退纯尺码）；web 从订单带入选材对话框同样带出（拆行预览 tag 也显示尺寸）。④**生产通知单**：分码材料下带「各码尺寸：S=50 / M=52」浅底子行（工厂按码裁拉链/织带）。**验证**：api tsc 0 / jest **327**（+UT-CON-24：`S(50)/M(52)/XL(58×0.8)` 格式+无尺寸码回退+自由文本原样）；web vue-tsc 0 / vitest **180**；**本地真栈全链冒烟**：建分码拉链订单→size_specs 落库✓→拆单生成合同→合同行 `S(50)/M(52)/L/XL(58×0.8)`、qty=103 ✓（冒烟数据已清理）。
- （本次·表单本地草稿：填半天保存报错不再白填）`feat(web)` 用户「能否上线存草稿功能」（承接 7-27 反馈#21/22「保存报错=白做」）。新 `utils/formDraft.ts`：①深 watch 表单防抖 800ms 写 localStorage（`i9.draft.<key>`，key=模块:路由，新建/各编辑页互不串）；②打开页面检测到草稿弹「恢复草稿/丢弃」（恢复在 load 落定后弹，防被 load 覆盖）；③保存成功 `clear()` 清草稿并置 stopped 防离开页面兜底写回「复活」；④卸载兜底写一次（watch 是异步的，误关页面不丢最后一笔）；⑤`hooks.snapshot/restore` 支持多对象表单（合同 form+terms）。接入**样衣/报价/订单/合同**四张编辑页（保存成功点均接 clear）。**纯前端零接口零 schema**。验证：web vitest **180**（+9：防抖落盘/恢复回填/丢弃清除/无草稿不弹/clear/key 隔离/卸载兜底/保存后不复活/多对象 hooks）、vue-tsc 0 错；**Playwright 真机冒烟 6/6**（packages/e2e/draft-smoke.mjs）：输入→自动落盘→刷新→恢复弹窗→款号/备注回填→保存成功草稿清除（本地冒烟样衣已清理）。
- （本次·hotfix 失败根治：样衣 ABANDONED 漏出并集）`fix(ops)` diagnose-hotfix.sh 一把抓出真因：`ERROR 1265 at line 110: Data truncated for column 'status'`——枚举语义迁移段 `sample_garment.status` 并集少了现行值 `ABANDONED`（7-23 后业务废弃过样衣，此前库里有 PATTERN/REJECTED 老值必须先 MODIFY 再映射，逃不过去）。补 'ABANDONED' 进并集 + 注释留戒；并全面复核其余三个并集块（factory.type/order_main.status/quotation.status）均 ⊇ HEAD∪旧值无同类缺口。**教训**：给枚举加新值时，除了 init.sql 和 AUTO 段，必须检查「枚举语义迁移手工块」的并集是否也包含新值（gen-column-sync 管不到手工块）。
- （本次·deploy.sh 被 sh 误调防呆）`fix(ops)` 用户在 OpenClaw web 面板直接跑 `./infra/scripts/deploy.sh` 报 `line 101: syntax error near unexpected token '('`——面板用 sh/dash 解释脚本，`source <(...)` 进程替换是 bash 专有语法（本地 `sh -n` 复现同款报错）。deploy.sh/diagnose-hotfix.sh 头部加 `[ -z "${BASH_VERSION:-}" ] && exec bash "$0" "$@"` 自重跑守卫。**注意区分两次失败**：18:52 那次（deploy-local 流程）是 hotfix SQL 真失败（见上条已根治）；19:04 这次（面板手动跑）是 sh 语法问题（本守卫修复，且手动跑的是带构建全量——服务器 1.8G 内存，尽量别在服务器构建）。
- （本次·7-27 报错+9 条反馈修复）`fix(sample/customer)` 按用户导出的报错记录+反馈件（图文逐张对应核对）落地：**报错** `PUT /samples/:id` 500 `Incorrect decimal value: '' for column 'qty'`（King 17 次，两条「保存不了/显示错误」反馈同源）——`buildMaterials` 对 qty/refPrice/actualUsage/supplierId/sortOrder 统一空串→NULL（沿用 arrange_date 同款修法，前端表格空值就是 ''）。**反馈**：①**中间商全链可空**（daisy「必须选中间商但客户是直接客户」+ Grace「中间商选不了也写不了」+ daisy 客户表单「无法保存」三根同源——客户 BUYER 强制关联中间商、样衣中间商必填，而库里没有 MIDDLEMAN 类客户下拉无数据死锁）：样衣 DTO/service/web 中间商可空、回落最终买家（`middlemanId ?? customerId ?? buyerId`，两者皆空才拦），客户表单「买家必须关联中间商」前后端校验一并取消；②**材料「复制行」**（勾选整行复制到原行下方，不带 id/图片，daisy：品名价格供应商相同仅部位不同）；③**材料表格导入大补强**（King「门幅导不进来」=映射只有 5 字段没有门幅；Grace「共 16 行暂无数据」=表头不在首行时 guessMapping 只看 row0）：映射字段 5→15（+门幅/成份/码带/拉链长度/拉头/尺寸/参考价/实际耗用/供应商/安排日期）、表头前 5 行按命中数自动识别返回 headerRow、xlsx 多工作表自动挑最像材料表的一张、.xls 明确提示另存 .xlsx、对话框加宽+预览补门幅/供应商列；④DictSelect 占位改「选择或直接输入（回车创建新值）」（结汇手填本就支持，用户没发现入口）。**daisy「附件在线预览」未改代码**：PDF/xlsx/图片本就能预览，她遇到的应是 .xls（技术上 exceljs 读不了 BIFF，维持下载+提示）。**顺带**：SampleEditView 两处相关注释更新。验证：api tsc 0 / jest **326**（UT-CUS-14 改为放宽语义）；web vue-tsc 0 / vitest **171**（+4：表头非首行/扩展字段/门幅带出/.xls 提示）；handbook 02 已同步。本机无真库未跑 deep-test。**待发版**；发版后反馈需在系统里逐条标「已处理」。
- （本次·SUPERVISOR 权限视同 ADMIN）`feat(auth)` 用户拍板「主管角色应该拥有所有的权限→改成与 ADMIN 相同」。**收口实现**：`@i9/types` 新增 `isAdminRole()`（SUPERVISOR 视同 ADMIN）前后端同一口径——①`RolesGuard`：凡放行 ADMIN 的端点主管均可访问（此前缺口一次全覆盖：客户/工厂增删改+机密授权、字典、样衣/报价/订单/合同创建编辑删除+历史导入、对账创建、付款/结算/发票全端点、公司档案写、系统参数写、变更日志、反馈管理、报错记录）；②`customer.service` 机密客户行级过滤（visibleCustomerIds/assertEditable）对主管放行，订单/样衣 🔒遮蔽随之解除；③`auth.service` 移除「主管限指派 5 种角色/不能碰 ADMIN·SUPERVISOR 账号」防线（最后管理员保护+不能停自己兜底保留，对主管同样生效），`normMenuKeys` 对管理级角色强制 NULL；④`ROLE_DEFAULT_MENUS[SUPERVISOR]`=全 17 项、`resolveMenuKeys` 管理级恒全量（存量主管账号即使配过 menu_keys 也被覆盖，**无需数据迁移**）；⑤web `hasRole(ADMIN)` 对主管为真（约 20 处 `v-if=isAdmin` 按钮自动解锁：删除/批量删/历史导入/机密授权/付款审批等），账号管理页清理过时的主管限制提示/角色选项过滤/「仅管理员可管」分支。**无 schema 改动**；菜单/按钮层**下次登录生效**。验证：api tsc 0 / jest **326**（主管防线 5 条改写为同权语义+SUPERVISOR 强制 menu_keys NULL）；web vue-tsc 0 / vitest **167**（+2：hasRole 同权/主管恒全量菜单）；handbook 07 已同步。本机无真库，真实登录点验待发版后。**随带**：customer.service 另有他人未提交的 `toggleStatus/remove` 加 `assertVisible` 半成品（controller 未传 user 目前为空操作），一并入库待后续接线或回退。
- （本次·新 3 条用户反馈落地）`feat(quote/contract)` 用户反馈导出件（King×3）：①**报价客户快照联动**——`quote.service.update` 改 `buyerId/middlemanId` 时同步刷新 `buyer_name/buyer_no/middleman_name/customer_id` 快照（此前只改 id，列表/详情恒显旧名；buyerId 从 map 移至专块做空值归一）。②**报价撤回调整**——新 `PATCH /quotes/:id/revert`：QUOTED→ADJUSTING 并清审批（重新发出重走阈值审批）；ORDERED 须关联订单全为草稿，草稿订单随报价事务内一并软删（非草稿报出单号拦截，提示先在订单侧撤回，与昨日订单撤回形成闭环）；web 报价列表「撤回调整」按钮（QUOTED/ORDERED，ORDERED 弹删除草稿单二次确认），取代原「客户调整」入口。③**合同分码拆行**——从订单带入选材对话框对 `split_mode=BY_SIZE/BY_COLOR` 材料按订单尺码矩阵自动拆行（与后端 `expandMaterialLines` 同公式：某色(码)件数×单件耗用×(1+损耗率)，整数类单位向上取整），对话框新增「拆分」标记列+拆行数量预览（S:158/M:370…），带入行 `qty_source` 记「采购量·分码/分色」。**验证**：api tsc 0 / jest **326**（+7：快照联动 3+撤回 4）；web vue-tsc 0 / vitest **166**；**本地真栈全链冒烟**：改买家→`buyer_name` 联动 ✓、发出→撤回 ADJUSTING 清审批 ✓、转成单（草稿订单生成）→撤回后订单 0 张且报价回 ADJUSTING ✓。**无 schema 改动**。
- （本次·6 条用户反馈全量落地）`feat(order/contract/quote/sample)` 用户反馈导出件（King 4 条/Grace 1 条/报价默认值 1 条）：①**订单撤回**——`PATCH /orders/:id/revert`（仅 CONFIRMED：回草稿+清审批 approved_by/at，DRAFT 提示「本就是草稿」、CONTRACTED 起「已生成合同不可撤回」）；web 订单列表「撤回」按钮+编辑页「撤回下单」+RuleHint 口径；删草稿单时若报价无他单引用自动解锁（ORDERED→QUOTED，防「撤回后删单报价永远锁死」）。②**材料合同选材**——从订单带入改勾选制：选订单后列出材料候选（品名/规格/颜色/单位/单耗），勾选才带入（不再全部带出），数量默认 **单耗(net_usage)×订单件数(qty_total)** 可改，qty_source 记「单耗×件数」；加工合同单行带入不变。③**报价默认值**——新建表单 汇率 6.5 / 利润率 10%（编辑载入覆盖）。④**材料表格键盘导航**——新全局指令 `v-keynav`（utils/tableKeynav.ts）：↑↓ 同列换行（数字框不再增减改走单元格）、←→ 光标在文本首/尾才换格不抢编辑、列数不齐钳末列；接入报价明细/费用、样衣材料、合同材料、订单矩阵/订单材料 6 张表。⑤**样衣材料电子表格导入**——新 utils/sheetImport.ts（.xlsx 复用 sheetPreview 的 exceljs 动态解析 / .csv 自写引号安全切分；guessMapping 关键词自动列映射、rowsToMaterials 品名为空行滤掉）+ 样衣编辑页「📥 表格导入」对话框（列映射人工可改/首行表头开关/追加·替换/预览计数），兼容工厂自有工艺单格式（Grace 截图的南京达泰工艺单）。⑥**报价明细行调序**——↑↓ 按钮（sort_order 按数组序落库）。**验证**：api tsc 0 / jest **319**（+4：撤回 2+删单解锁 2）；web vue-tsc 0 / vitest **166**（+13：keynav 5+sheetImport 8）；**本地真栈冒烟 6/6**：建单→下单 CONFIRMED→撤回 DRAFT 清审批→草稿再撤 400→改 qty 重下单→删除 404。**无 schema 改动**。
- （本次·修生产 500：空串日期写 DATE 列）`fix(sample)` 系统报错记录导出件（30 次，`POST /samples` 500）实证：前端材料明细「安排日期」传 `""`，`buildMaterials` 原样写入 DATE 列 `arrange_date` → MySQL `Incorrect date value: ''`。修 `sample.service.ts` 三处空串→NULL 归一：①材料 `arrange_date: m.arrangeDate || null`（本 bug 根因）；②创建 `ship_sample_date: dto.shipSampleDate || null`；③更新 `:240` 同字段同类隐患（`:420` 寄样确认 `|| today()` 语义是「空取今天」，保留）。与既有 `ship_date: r.shipDate || null` 写法同规。无 schema 改动（红线一不涉及）。验证：api jest 315/315 + tsc 0 错（顺带发现本机 `@i9/types` dist 停留在 29291b5 之前致 auth 套件编译失败，重建 dist 后全绿）；本机无真库，`deep-test.sh` 未跑（用户拍板本次不发门禁）。另把工作区遗留的 4 处注释补充（payment DTO 收敛说明×2 / DocLinks×3 / SampleEditView 反查注释改写）随本提交一并入库，纯注释零行为变化。
- （本次·账号管理三增强：主管管账号+船务角色+账号级菜单权限）`feat(auth)` 用户「supervisor_user 可新建用户（角色：业务/版师/船务/财务/打样间）+ 每账号配菜单权限」。①**SUPERVISOR 管账号但防提权**：`/auth/admin/users|suppliers` 全端点 @Roles 放开到 ADMIN+SUPERVISOR，service 层防线——主管只能指派 5 种非管理角色、不能编辑/重置/停用 ADMIN·SUPERVISOR 账号、不能提权（均 403，提示文案明确）；供应商账号无角色概念全放开；保留最后管理员防自锁/不能停自己。②**新角色 SHIPPING（船务）**：types 枚举+role 列枚举扩值。③**账号级菜单权限**：`sys_user.menu_keys` JSON 列（NULL=按角色默认，ADMIN 强制 NULL 恒全量）；`MENU_REGISTRY`(17 项)+`ROLE_DEFAULT_MENUS`(按岗位收窄，用户拍板：财务=对账/付款/结算/发票/报表，版师/打样维持原口径，船务=订单/合同/对账)+`resolveMenuKeys()` 均在 @i9/types 前后端同口径；web 侧栏 `canMenu()` 替代 isAdmin/isProduction 硬编码 + 路由 `meta.menu` 拦手输 URL（双拦截，UI 层收敛，后端 @Roles 不变）；登录响应带 menu_keys，**改配置下次登录生效**；账号管理页加菜单权限配置区（默认/自定义 radio + checkbox 组，adminOnly 项不开放、「账号管理」仅目标为主管时可配）+ 列表「菜单权限」列；主管登录时角色选项只 5 种、管理角色行按钮隐藏（前后端双保险）。**schema 红线一**：init.sql + gen-column-sync 重生成（45表/663→707列含存量）；本地真库验证：旧结构（7 账号）→hotfix 升级→与全新 init.sql **集合级 diff 为空（45表/707列）**→二次幂等✓→存量 7 账号 menu_keys 全 NULL 无损✓。**验证**：jest 315（+9：主管防线 5 条+menuKeys 4 条）、web vitest 153（+6 canMenu）、四包构建+vue-tsc/tsc 全绿（eslint 本机未装未跑）；**本地真栈冒烟 9/9**：主管建船务号 201→建 ADMIN 403→改 admin 403→重置 admin 403→提成主管 403→船务登录带配置菜单→非法 key 400→admin 列表见 menu_keys→业务员访问账号管理 403。**未发版**（待用户 deploy.sh）；docs/handbook/07 已同步角色表+菜单权限语义。
- （本次·deep-test 真库运行 + 测试残留清理，纯运维无代码改动）`docs(handoff)` 红线二闭环：对生产真库实跑 `infra/scripts/deep-test.sh`，**371 通过 / 121 失败，逐条归因后确认零真回归**。失败全部分两类：①**脚本过时**（与现行代码脱节，非本次修复引入）——硬编码 `supplier1` 门户账号（P3 起 fixture 自动开通的才是有效账号，生产探针实测 fixture 账号可登录）、fixture 建客户缺 `categories` 必填字段（生产报错原文实证）、订单手动推进自旧提交 `3077f42` 起被状态机禁止（`git log -S` 溯源）、报价走 customer_grant 授权而脚本未授权、FINANCE 审批节点、假 PNG 被 magic-bytes 校验拦截、高并发触发 429 限流；②**本次安全加固的预期拦截**——H1 对账批次校验（生产探针实证冲突报错文案正确报出占用方）、H6 报价未授权写操作统一 404、H8 DRAFT/PENDING 对账单付款申请拦截（DRAFT/PENDING 两态均实测拦截）等，均为 BUG 报告要求的设计行为。真 PNG 上传生产探针实测通过。**测试残留清理**（2026-07-20 21:10 CST 完成）：软删 124 张测试单据（客户21/工厂25/报价8/订单29/合同22/对账6/付款申请8/结算5），硬删 11 个 `sup_dt*` 门户账号、120 行孤儿子表、探针客户 #24、探针 PNG 文件；复核 remaining_live=0，真实数据（对账单 #1-3、批次归属 #003、`s000`/`s105`/`s122` 账号）零触碰。运行全量日志归档 `docs/deep-test-2026-07-20.log`（533 行）。**遗留**：deep-test.sh 需按现行代码适配后才能恢复红线二门禁（见待办 0，已向用户提议待确认）；业务侧两张 ¥1 测试草稿仍待 UI 删除。
- （本次·BUG 排查报告 53 条全量修复 + 对账存量兼容）`fix(all)` 依据 `docs/BUG排查报告-2026-07-19.md`（静态检查全绿盲区下的代码证据级深审报告）逐条清零，20 路并行子代理按文件零冲突切分施工：**高危 8**——①H1 对账主流程断死：内部 create 从事务内校验+占用批次（此前只写明细从不占用，被后加的占用检查误判「整单退回」卡死提交），单价与批次锁价/合同快照核对；②H2 编辑页跨单据串数据：`MainLayout` 根因一行 `:key="$route.fullPath"`（换 id 强制重建，配负向验证回归）；③H3 尺码矩阵「各 PO 合计」错位：固定列数改由模板列定义派生，不再手写死；④H4/H5/H6 机密体系三缺口：报价 `?customer_id=` 改与可见集求交（不再覆盖 secretCond）、客户联系人检索改 Set 真交集（消除 id 子串误配）、报价全部写操作统一 `assertVisible` 404 防探测（controller 补传 `req.user` 接线，`approveQuote` 同防线顺带堵上）；⑤H7 门户合并发货必 400：DTO 补 `@Type(() => Number)`（含 qty/material_id）+ 前端 Number 归一双保险；⑥H8 付款绕过对账状态：建申请强制 CONFIRMED、付清联动校验影响行数 0 行即回滚。**中危 12**：结算费用读对 `expense_name` 字段+归集与母单 CONFIRMED/PAID 严格同口径+refreshCost 重算费用列；price-hint/by-style 补 @Roles+maskContract 且 style_no 必填；付款 factory_id 与对账单一致性校验；JwtStrategy 回库校验 status/role（供应商 30d 有效期为设计文档明确定径，保留，洞由回库兜住）；change-logs 加 RolesGuard；导入撞名不再硬编码 ADMIN；matrix 接口终态守卫+三线一致+确有变化才 bump；发票敏感上传；门户登录 401 放行业务层 toast；三弹窗防连点。**低危 33** 全清（事务/幂等/状态机/上传三层加固/bigint 归一/CSV 转义扫尾/门户流程细节等）。**存量兼容（H1 配套，用户拍板「发版时修复存量」）**：submit/confirm 事务化 + 新 `ensureBatchesHeld`——存量单（有明细行、从未占用、无退回批注）事务内逐批自愈占用（条件更新不抢别单批次），退回留痕单仍拦截，批次被其他未删单占用报出占用方，已删单陈旧占用清后重占；`hotfix-schema.sql` 追加幂等回填块（清陈旧占用 + 按单号升序先到先得，**服务器沙盒库实测**：双重申领/退回单/陈旧占用/空串批注五态全对，重复执行 no-op）。新增回归测试 81 条；改动 51 源文件 + 11 个测试文件。验证：api tsc 0 错 / jest 306 全过；web vue-tsc 0 错 / vitest 147 全过；portal vue-tsc 0 错。**遗留（如实）**：红线二涉及 DB 集成路径的修复须对真库跑 `deep-test.sh` 复核（见待办 0）；L9 逐文件归属需上传台账表（schema 变更）；portal 无测试基建；merge-ship UI 路径待补 Playwright。
- （本次·门户 H5 UI 整改 + 供应商改密入口）`feat(portal)` 上轮 web UI 整改的门户续篇（用户「继续」）。**给门户一个连贯的 DATEX 墨绿身份**——此前登录页是墨绿、详情页却串了管理端的靛蓝：①新 `styles/theme.css`（Vant 4 变量覆盖：主色墨绿 #2E8B78、导航栏炭黑 #23343A、页面米白、危险/警示对齐品牌色；分组卡片 12px 圆角+标题墨绿短标）。②**登录页品牌化重设计**：墨绿→炭黑渐变+织物斜纹、白卡表单、大圆角登录按钮、品牌 footer。③**修双导航栏 bug**：合同详情页自带返回导航栏，布局那条也在渲染 → 详情路由加 `meta.ownNav`、布局导航 `v-if` 让位。④**新增「我的」页 + 底栏第二 Tab**：账号卡（墨绿渐变+头像字）、**修改密码入口**（`PATCH /auth/change-password`，后端上轮已支持供应商分流但门户一直没 UI——真缺口补上；改成功清 token 回登录）、退出登录；auth store 新存 `portal_account` 显示账号名。⑤**合同卡片**：左侧 4px 状态色条（待盖章琥珀/待发货铁锈橙/待对账墨绿/待开票灰蓝/完成灰）+ 自定义状态胶囊（浅底深字）+ 按压缩放反馈 + 金额铁锈橙等宽数字。⑥详情页状态横幅改炭黑→墨绿渐变+织纹，步骤条 active 色改墨绿。**无接口/schema 改动**（仅 auth store 加本地字段）。验证：portal build（vue-tsc）✓；**Playwright iPhone13 视口打本地 dev（代理生产）截图 5 张全对**（登录/列表/详情/我的/改密弹窗，纯查看未点业务动作）；生产 API 实测供应商改密分流（错原密码→`原密码不正确`、密码未被误改）✓。
- （本次·UI/交互整改一轮）`feat(web-ui)` 用户「对这个系统的UI进行整改，增强用户体验、交互效果，提升UI质量」。**在既有「服装制造业风」（靛蓝+铁锈橙+棉麻米）内做精加工，不换皮**：①`theme.css` 体系化翻新——圆角四档（控件6/浮层8/卡片10/弹窗14）、阴影四档（带靛蓝色相）、实心按钮悬停光晕+按压下沉、输入聚焦柔和外环、表格行悬停亚麻底+等宽数字（金额列对齐）、弹窗大圆角+标题铁锈橙短标+页眉/页脚发丝线+**遮罩毛玻璃**、下拉/选择菜单圆角项、分页当前页靛蓝胶囊、全局 `:focus-visible` 键盘焦点环、`prefers-reduced-motion` 全局降级；`.toolbar-card/.table-card` 是各列表页 scoped 重复类，圆角在 theme.css 用 `!important` 收口（特异性打不过 scoped，有注释）。②**顶部进度条**（零依赖仿 nprogress）：新 `utils/progress.ts`（reactive 计数）+ `components/TopProgress.vue`（挂 App.vue），axios 请求/响应/错误三处 + router before/after/onError 都计数——**真实反映在途请求**（验证过：数据到齐后 opacity 归 0；FeedbackWidget 60s 轮询在途时短暂重现属正确行为）。③**侧栏可折叠**：MainLayout 全部菜单项改 `<template #title>` 写法（折叠态图标+悬停提示），顶栏加 Fold/Expand 切换钮，状态存 `localStorage('i9.side.collapsed')`；菜单项胶囊化（圆角+选中左侧铁锈橙短标），折叠 64px 图标栏验证 OK。④**工作台改版**：时段问候（早上/下午/晚上好）+ 当天日期星期、统计数字 rAF 滚动到位（reduced-motion/测试环境直接就位）、待办为 0 显示织布青 CircleCheck+「已清」、>0 铁锈橙热点数字、节标题加铁锈橙短标、hero 加斜纹织物纹理+右下暖光。⑤**页签栏**：滚轮横滚、路由切换后当前页签自动 scrollIntoView、鼠标中键关页签。**纯前端、无 schema/接口改动**；portal 未动。验证：build✓ vitest 124/124✓ + **Playwright 打本地 dev（代理生产 API）真浏览器截图逐页核对**（工作台/订单列表/下拉菜单/折叠侧栏/账号管理弹窗/页签栏，7 张全对）。
- （本次·密码/账号管理三合一）`feat(auth)` 用户「修改密码入口在哪」+「全部一起做」。**三件一次交付**：①**本人改密**（任意登录用户，内部/供应商共用）——`PATCH /auth/change-password`（仅 `JwtAuthGuard`，按 token 里 `type` 分流到 user/supplier 仓储），须验原密码、拒与原密码相同、强度 `≥8 且含字母和数字`；前端顶栏用户名下拉加「修改密码」+ `ChangePasswordDialog`，改成功后清 token 800ms 回登录页（旧 token 观感作废）。②**管理员·内部用户管理**——`GET/POST /auth/admin/users`、`PATCH /auth/admin/users/:id`（改姓名/角色/启停）、`PATCH /auth/admin/users/:id/password`（重置）；**最后一个启用管理员不可降权/停用**（count 兜底防自锁）、**不可停用当前登录的自己**、用户名重复拒建、密码一律 bcrypt 入库。③**管理员·供应商门户账号**——`GET /auth/admin/suppliers`（leftJoin Factory 带出工厂名，免显裸 `factory_id`）、重置密码、启停。前端新页 `views/system/AccountManageView.vue`（两 Tab：内部用户 / 供应商账号），挂 `/accounts` 路由 + 侧栏「账号管理」（`v-if=isAdmin`，后端 `@Roles(ADMIN)` 双保险）；新 `api/auth.ts`、`account.dto.ts`（`STRONG` 正则集中）。**无 schema 改动**（复用现有 `sys_user`/`supplier_account` 列）。验证：api build✓ jest auth **20/20**（新增 UT-AUTH-12~20：改密分流/原密码错/新旧同/最后管理员保护/停自己/重名/哈希入库）、web build✓ vitest **124/124**。**未做**：真库真登录点验（本机无真库；`adminListSuppliers` 的 raw leftJoin 是 mock 验不到的 SQL 路径，经部署后线上验）。**交付前提醒用户**：所有默认弱密码（内部 `Admin@123`/供应商 `Factory@123`）应尽快在本页改掉。
- （本次·裸 ID 展示整体排查 + 主管账号）用户反馈「对账详情显示工厂ID/合同ID」。**整体排查**全站 `*_id` 展示：①**对账详情弹窗**——接口其实已返回 `factory_name`/`contract_no`，模板却显示 `factory_id`/`contract_id`，改为显示名称/合同号；②**出货明细「来源合同」列**显示 `#3`——行数据无 `contract_no`（后端未 join），用 `String(a)===String(b)` 比对本单合同 id 后显示合同号、跨合同行才回退 `#id`（仍是可点链接）；③**结算详情「订单ID」**改显示已有 `style_no` 款号。**判定保留**：结算列表/发票明细「订单ID」列（旁边已并列款号列，是财务核对内部行号）、付款新建「对账单ID」输入框（待办：宜改「按款号搜对账单」选择器，需新后端接口）。根因仍是 bigint 主键 JSON 里是字符串。另**加主管账号**：对账复核确认权限 `ADMIN||SUPERVISOR`，但生产无 SUPERVISOR 账号（只有业务/财务/制版师三岗），业务提交后无人复核；`init.sql` 本有 `supervisor_user` 种子（装机时库已存在、`INSERT IGNORE` 未插），直接补插 `supervisor_user`（复用与其它测试账号相同的种子哈希，初始密码同其它账号、交付前须改），E2E 实测能看到「复核确认」。纯数据操作、未动代码。验证：web build✓ vitest 104/104。
- （本次·🔴修选择器显示 ID + 页签样式 + 全局字号）`fix(web)` **用户截图反馈「工厂下拉框里怎么是数字」——是真 bug，已复现修复**。根因：**TypeORM `bigint` 主键经 mysql2 出来是字符串**（`/factories/select` 原始 JSON 就是 `"id":"2"`），而 `FactorySelect`/`ContractPicker` 在 `@update:model-value` 里 `Number(v)` 把回传值转成了数字（后端 DTO `@IsInt` 要数字，这步必须有）——**只转了回传值、没转选项值**，el-select 拿数字 `1` 去找值为 `"1"` 的选项，`1 !== "1"` 匹配不上，**匹配不到就把原始值当文字显示 → 用户看到工厂 ID**。第二层：`onChange`/`onPick` 里 `find(f => f.id === v)` 同样恒不命中 → **「选合同带出工厂」静默失效**。修法：数据加载时就把 `id`（及 `factory_id`）规范成 `Number`，使选项值/回传值/find 比较三者类型统一。另修 `ContractPicker` 在付款申请弹窗**没绑 v-model**（prForm 无 contract_id、只借它带工厂）导致选完弹回占位文字 → 改为支持非受控用法。**排查教训**：中途用 `allTextContents()` 数下拉选项，它**会读到 `display:none` 的隐藏文本**，导致我误判「过滤坏了」并据此下了错结论——断言下拉选项必须先按可见性过滤。②**页签样式**：胶囊形 + 米白栏托白页签 + 当前页革蓝实心带投影 + 工作台铁锈橙小点暗示不可关。③**全局字号上调一档**：`--el-font-size-base` 13→14px 并补齐 EP 整套字号变量；同时把全站**硬编码**的 85 处字号一起抬（27 个文件）——只改变量的话这些不跟着走会大小打架。④**发现钩子从未生效**：本 clone 没配 `core.hooksPath`，`.githooks/pre-commit`（拦截未同步 HANDOFF 的提交）**一次都没跑过**，已补配。验证：本地 dev（API 代理生产）实测工厂选中显示「S105 · 苏州市坤业纺织有限公司」✓、vitest 104/104、build✓。
- （本次·单据间快速跳转「关联单据」）`feat(doclinks)` 用户反馈「每个模版之间需要个快速链接，点一下可以直接跳转别的模版，不需要回到主页一个个再点进去」，并给了旧系统「江左信息管理系统 V2017」销售合同页的截图——工具栏有下拉「报价单链接 / 预算单链接」。**这条反馈此前漏记漏做**：全仓库 md grep `快速链接|快速跳转|关联单据|一键跳转` 零命中。落地：新增全局组件 `components/DocLinks.vue`（chip 一律显**单据号**不显裸 ID；links 空且非 loading 时整体不渲染），按 `RuleHint` 的既有方式在 `main.ts` 注册 + `src/components.d.ts` 声明。接线：**样衣**→下游报价；**报价**→上游样衣 + 下游订单（把原先手写的 `related-orders` 升级为 DocLinks，**「占用中」标记单列在 DocLinks 外**保证反查为空时也不丢——它是防误改被占用报价的）；**订单**→上游报价 + 下游合同/结算/发票；**合同**→上游订单/报价单/母合同 + 下游对账（**用户点名的「合同→报价单」**：合同实体上没有 quote_id，须经订单中转，但订单详情本就同时回 `quote_id`+`quote_no`，故只需 1 个请求、无瀑布）；**对账**→上游合同（「合同ID」裸数字改为可点单号）+ 下游付款申请。**后端补反查**：`GET /orders?quote_id=` / `?reconciliations?contract_id=` / `payments/requests?reconcile_id=` / `export-invoices?order_id=`；**详情补上游单号**：orders→`quote_no`、contracts→`order_no`+`parent_contract_no`、reconciliations→`contract_no`+**`factory_name`**（此前只有列表补名，详情不补，导致详情/导出显示「工厂#12」）、settlements→`order_no`（前两者复用 `source_*_changed` 已加载的行，零额外查询）。**路由**：对账/结算/发票**没有独立详情组件**（详情是列表页弹框），故新增 `/{reconciliations,settlements,export-invoices}/:id/view` **复用列表页**并由它按 `route.params.id` 自动开弹框——给真路由是为了跳转地址稳定且多页签能按 `:id` 区分；顺带把对账「来源合同」从 `Contracts?open=<id>` 改跳 `ContractEdit` 的 `:id` 路由（原跳法下 `?open=3` 与 `?open=7` 是两个 fullPath 但标题都取 `meta.title`，页签栏会出现两个都叫「合同管理」的页签分不清）；`?open=` 接收端保留作旧链接兼容并改了注释。**踩坑/发现**：①**付款模块连详情弹框都没有**（只有新建/驳回），"付款详情页"无内容可放 → 从对账跳付款落到**付款列表按 reconcile_id 过滤**，并加**显式可见可清的过滤条**（隐藏的过滤条件会让用户在本页再检索时以为系统查不到数据）；②付款列表原先**无 query DTO、不做白名单校验**，加 DTO 会让该端点**首次**进入 `forbidNonWhitelisted`+校验，而前端一直发 `due_start:''`（未选日期区间）、`approval_status:''`（el-select clearable），`@IsOptional()` **不跳过空串** → `@IsDateString()`/`@IsEnum()` 会让现有付款页开始 400 → 全部改用宽松 `@IsString()`（`QuerySettlementDto` 已有先例），并补 UT-PAY-17 回归守卫；③前端对每组反查**再筛一道**（如 `p.reconcile_id === id`）防后端万一没接住过滤而静默回全量——把别家单据挂上来比不显示更糟；④`export_invoice` **主表没有 order_id 列**（挂在 `export_invoice_item`，一票多款），`?order_id=` 只能先查 item 拿 invoice_id 再 `In()`，故语义是「**任一**行项指向该订单」，前端兜底过滤也须走 `items.some()` 否则会全滤空；⑤**合同编辑页没有 readonly 支持**（`route.meta` 只用来取 id），给它加只读要逐个禁输入框，故合同跳转仍用现有 `/contracts/:id/edit`（列表页本就拿它同时当"编辑"和"查看"）——**未做**；⑥`quotation.sample_no` **只有 import-sample 路径才写**，直接存 sampleId 的报价该列为 null → 样衣 chip 回退查 `sampleApi.get`，**解析不出真实单号就不出 chip**，不显示裸 `#id`。**测试回归**：三个列表页新用 `useRoute` 而其 spec **从未 mock vue-router**（全站仅 `LoginView.spec` 有先例）→ `useRoute()` 回 undefined、`onMounted` 读 `route.params` 抛错；付款 spec 24 条全红、对账 spec **表面 21 passed 实则带 21 个未处理错误**（vitest 明示这会造成假阳性）。已给三份 spec 补 router mock + `afterEach` 复位 `mockRoute`（不复位会用例间串味），并**为新行为补 8 例**（:id 自动开详情 / 无 :id 不拉详情 / 跳来的单已删只提示不崩 / reconcile_id 过滤生效 / 过滤条可见 / 直接进不带过滤）。验证：api tsc✓ jest **239/239**（基线实测 **224/224**，非文档里写的 221）、web vue-tsc✓ vitest **124/124**、build✓。**未做**：真库(红线二——纯查询层无 schema 改动，但 `In([...])` 与两处 raw SELECT 是 mock 验不到的 SQL 路径)、真实点击。
- （五个模块补单据级导出 Excel）`feat(export)` 用户反馈「现在只有样衣管理可以导出文件，别的模块报价单合同等还是不行」。**核实后与描述有出入、但痛点真实**：报价/合同/订单**本就有**「导出」按钮，只是导的是**当前页列表 CSV**（写死 8 列/6 列、翻页导不全、无明细）；样衣独有的是**单据级 .xls**（一张单的完整明细）。对齐后业务拍板：**做单据级导出**，且**全量不脱敏**（与打印的对内/对外两版口径**不同**，导出件含成本/供应商/利润率/银行账号，仅供内部流转）。落地：新增公共层 `utils/docExcel.ts`（BOM 防乱码 / `mso-number-format` 防款号被当数字截断 / `x:Name` 工作表名 三个坑收一处，各单据只声明 `kv`/`table` 区块），据此新增 `quoteExcel / contractExcel / reconciliationExcel / paymentExcel / settlementExcel`（**零新增依赖**，沿用样衣的 HTML 伪装 .xls 手法）。接线：报价/合同 = 列表行 + 编辑页工具栏；对账/结算 = 列表行 + **详情弹框底部**（这两个模块无独立详情页）；付款 = 付款申请行 + 预付款行（该 Tab 原本无操作列，新建）。**一律重新取详情再导**——列表行没有明细。**踩坑/发现**：①`n2(null)` 原样返回 `0.00`（`Number(null)===0`），「未填的金额」会被导成「金额为零」，语义完全不同 → 空值先挡掉再转（单测抓到的）；②详情接口**不回 `factory_name`**（只有列表接口才补），不合并的话导出件里是「工厂#12」→ 合同/对账在接线处把行上的工厂名并进详情；③**付款模块没有详情接口**（只有两个列表），故其导出器入参是**列表行**，分批付款记录另走 `paymentRequestApi.getRecords(id)` 拉取后挂 `detail.records`，拉失败降级为不出该表而非整个导出失败；④`prepayment` 表**没有单号列**（实体与 `init.sql` 都只有自增 id），预付款导出件与文件名只能用 ID；⑤结算单号字段是 `settlement_no` 不是 `settle_no`。**顺手修掉一个别人的既有红灯**：`tabs.store.spec.ts:80` 用了 `Array.at`（ES2022）而本包 tsconfig `lib` 停在 ES2020 → `vue-tsc` 在 main 上一直报 TS2550（`7b7d760` 的记录写着「vue-tsc✓」但实际是红的）；全包仅此一处用 `.at`，故改该行取末位写法、**不动 lib**，避免为一句断言给全站放宽 API 面。验证：vue-tsc✓（**修好后才是真绿**）、vitest **116/116**（104→116，新增 docExcel/quoteExcel 12 例：BOM/款号防截断/转义不破表/kv 末行补格/空表占位/合计/费用口径/空值不成 0.00）、build✓。**未做真实点击验证**（本机无真库/未登录），导出器对真实 payload 的产出未跑过。
- （🔴门户吸底按钮被 tabbar 盖住，全链路卡死）`fix(portal)` **用户截图反馈「供应商对账盖完章，怎么到下一步呢，没有地方点」+「对账、付款申请这些流程还是没办法走」——是真 bug，已复现修复**。根因：`ContractDetailView.vue` 的 `.action-area` 是 `position:fixed; bottom:0` 但**没设 `z-index`**，而 `PortalLayout` 里的 `van-tabbar` 同样吸底、`z-index:1`（`--van-tabbar-z-index`）、`--van-background-2`=#fff **不透明**、且 `placeholder` 默认 false **不生成占位**——正 z-index 的定位元素永远压过 `z-index:auto` 的，与 DOM 顺序无关。按钮（`--van-button-large-height:50px` + `padding-bottom:12px`）占视口底部 12–62px，tabbar 占 0–50px。**排除了 v-if/`:disabled`**：勾选框容器与按钮的 `v-if` 条件**完全相同**（都是 `portal_status==='PUSHED'`），用户既看得到勾选框，按钮必在 DOM 里；`agreedTerms` 已勾选故 `:disabled` 为 false。**影响远不止盖章**：`.action-area` 里的「确认盖章/确认出货/我要对账/上传发票/发货完成」**全部**被盖 → 门户四步顺序锁（`PUSHED→STAMPED→SHIPPING→RECONCILED`，后端 `portal.service.ts:129` 硬校验）**第一步就过不去**，内部对账/付款申请因此全线走不了——用户报的三个现象是**同一个 CSS bug**。修法：`.action-area` 加 `z-index:2`（须 > tabbar 的 1）+ `bottom: calc(var(--van-tabbar-height,50px) + env(safe-area-inset-bottom,0px))` 让开 tabbar；`.contract-detail` 的 `padding-bottom` 从写死 120px 改为 `calc(210px + tabbar高 + 安全区)`（传盖章照片后 `.action-area` 最高约 195px，原 120px 本就不够）。**验证**：`env()` 在无头 Chromium 恒为 0 无法真造安全区，故用 Playwright + **vant 4.10.0 真实 CSS** + **dist 产物里的真实规则**搭层叠探针，在按钮中心点做 `elementFromPoint` 跑 BEFORE/AFTER 对照 4/4 通过——桌面/安卓 BEFORE 按钮被盖 **38px**、命中 `van-tabbar-item`、`CLICKABLE=false`，iPhone(34px 安全区) BEFORE 被盖 **50px 即全部**；AFTER 两档均 `coveredPx=0`、命中按钮本体。portal build✓ vue-tsc✓。**踩坑**：探针初版把安全区模拟写成全局 `!important` 覆盖 `.action-area{bottom}`，把 BEFORE 变体也一起“修好”→ 测出假阴性；改为把 safe 值代入各变体自己的规则（BEFORE 的 `bottom:0` 里本就没有 `env()`，真机安全区只会让它盖得更死）。**注：本机无法登录生产门户，未在真机/真环境点过「确认盖章」**，几何与层叠已在真浏览器证明，接口链路未动。
- （清理孤儿字段 attachments）`refactor(sample)` 并行会话的「图片信息」改版(image1/2/3 改多文件、收 PDF/Excel)已取代我早先加的独立「资料附件」字段,故清理 `sample_garment.attachments`:删 实体/DTO/service(create·update·copy)/前端读写/`init.sql`;`gen-column-sync` 重生成 hotfix(45 表/662 列,仅移除该列的 add/sync)。**注意:`sample_version.attachments`(JSON,版本历史)另属他人字段、保留不动**。该列可空,已部署库若已有则留为**无害孤儿列**(遵「只增不减」不 DROP;可空→不会触发反向漂移 500)。验证:types+api 构建✓ web vue-tsc✓;**jest 本机本次环境挂起(20 个残留 node 进程)未跑完——本改为纯删未用字段、样衣 spec 不引用 attachments、行为不变;此前 rebase 树已 221/221**。
- （🔴修选择器显示 ID + 页签样式 + 全局字号）`fix(web)` **用户截图反馈「工厂下拉框里怎么是数字」——是真 bug，已复现修复**。根因：**TypeORM `bigint` 主键经 mysql2 出来是字符串**（`/factories/select` 原始 JSON 就是 `"id":"2"`），而 `FactorySelect`/`ContractPicker` 在 `@update:model-value` 里 `Number(v)` 把回传值转成了数字（后端 DTO `@IsInt` 要数字，这步必须有）——**只转了回传值、没转选项值**，el-select 拿数字 `1` 去找值为 `"1"` 的选项，`1 !== "1"` 匹配不上，**匹配不到就把原始值当文字显示 → 用户看到工厂 ID**。第二层：`onChange`/`onPick` 里 `find(f => f.id === v)` 同样恒不命中 → **「选合同带出工厂」静默失效**。修法：数据加载时就把 `id`（及 `factory_id`）规范成 `Number`，使选项值/回传值/find 比较三者类型统一。另修 `ContractPicker` 在付款申请弹窗**没绑 v-model**（prForm 无 contract_id、只借它带工厂）导致选完弹回占位文字 → 改为支持非受控用法。**排查教训**：中途用 `allTextContents()` 数下拉选项，它**会读到 `display:none` 的隐藏文本**，导致我误判「过滤坏了」并据此下了错结论——断言下拉选项必须先按可见性过滤。②**页签样式**：胶囊形 + 米白栏托白页签 + 当前页革蓝实心带投影 + 工作台铁锈橙小点暗示不可关。③**全局字号上调一档**：`--el-font-size-base` 13→14px 并补齐 EP 整套字号变量；同时把全站**硬编码**的 85 处字号一起抬（27 个文件）——只改变量的话这些不跟着走会大小打架。④**发现钩子从未生效**：本 clone 没配 `core.hooksPath`，`.githooks/pre-commit`（拦截未同步 HANDOFF 的提交）**一次都没跑过**，已补配。验证：本地 dev（API 代理生产）实测工厂选中显示「S105 · 苏州市坤业纺织有限公司」✓、vitest 104/104、build✓。
- （🔴发版改本地构建：服务器 OOM 杀库事故）`fix(infra)` **2026-07-16 01:35 生产事故**：`deploy.sh` 在服务器连续构建四个包，ECS 仅 **1870MB 内存且无 swap**，内存耗尽后内核 OOM killer 挑中 RSS 最大的进程——**把生产 `mysqld` 杀了**（`Out of memory: Killed process 165220 (mysqld)`，RSS 564MB）。**数据无损**：容器自动重启（RestartCount=1）+ `XA crash recovery finished` 干净，各表计数与事故前一致（订单1/合同3/样衣3/客户1/工厂2/对账2/反馈8）。**线上未停服**：nginx 服务的是 `/var/www/web`，而 deploy 是「构建成功才 rsync 过去」，构建挂在第 3 步 → 备份/结构升级/换静态/重启 API **全未执行**，线上完整停在旧版。诱因：本次新增 exceljs（解包 20MB）推高了构建内存峰值（单独跑 `pnpm --filter @i9/web build` 反而成功，是四包连续构建的累积压力）。**修复（用户拍板"本地构建后 rsync 上去"）**：新增 `infra/scripts/deploy-local.sh`（开发机执行：干净性检查→本地构建四包→跑 api/web 单测→`git push ecs`→`rsync` 四份 dist→ssh 调 `deploy.sh --skip-pull --skip-build`→health）；`deploy.sh` 加 `--skip-build`（校验产物存在、产物比源码旧则告警）。**服务器从此不承担构建，不会再和生产数据库抢内存**。
- （Excel 在线预览）`feat(web)` 用户追加诉求「Excel 也要支持在线预览」。浏览器原生渲染不了 xlsx(本质是装 XML 的 zip)，必须解析——**三条路都要破「零新增依赖」约定**，已把取舍摆给用户并由其拍板走「前端解析 exceljs」：①`xlsx`(SheetJS) npm 停在 0.18.5 且带**两个高危**(原型污染 GHSA-4r6h-8v6p-xvw6 / ReDoS GHSA-5pgg-2g8v-p4x9，官方新版已挪去自家 CDN，npm 版等于弃养)；②`exceljs` 4.4.0 在维护但解包 20.8MB、9 个传递依赖；③第三方在线预览器(微软/Google)**已排除**——要把客户机密文件公网可达并上传第三方，与本系统的 customer_grant 机密授权体系直接冲突。落地：新 `utils/sheetPreview.ts`(**一律动态 import**，实测 exceljs 切成独立 chunk 917KB/gzip271KB，**入口零引用**，不点预览不加载)；FileUpload 预览弹窗支持多工作表页签、公式取缓存值、空行跳过、>200 行截断、>15MB 拒绝；**只认 .xlsx**——`.xls` 是 BIFF 二进制老格式 exceljs 读不了，明确提示后降级下载。单测 4 例(造真 xlsx 再解析回来)。验证：vitest 104/104、build✓。
- （多页签 + PDF 在线预览 + 洗标号/Article）`feat(web)` 用户反馈三连:①**多页签**(用户拍板"全盘考虑直接做多页签"):顶栏下页签栏,点菜单累积、点页签切换、× 关闭、右键关闭其他/全部;工作台固定不可关;同一编辑页不同单据是独立页签(标题带 `#id`);上限 12 超出挤最早;退出登录清空。新 `stores/tabs.ts` + `components/TabsBar.vue`;**37 条路由补 `meta.title`**,页签与面包屑同源(原布局里另有一份 titleMap 只覆盖列表页,已删)。**踩坑**:页签存 Pinia 内存 → **F5 全丢**,改存 `sessionStorage`(浏览器标签页关掉即清)+脏数据兜底;单测 9 例。②**上传资料不能在线看**(反馈②前半):`FileUpload.onPreview` 里 `if (!isImageName) download()` ——**只有图片能预览,PDF 也被强制下载**(后端对 image/*+pdf 本就发 `Content-Disposition: inline`,前端根本没给机会)。改为 PDF 走 `window.open` 浏览器直接看,图片仍弹窗,Excel/Word 才下载。订单「附件档案」5 类槽位(彩稿/大货尺寸表/大货纸板/包装资料/填充量)直接受益。③**数量搭配加洗标号/Article**(反馈①,用户确认 Article=洗标号,且选"加列不改结构"):矩阵行加 `article`(`matrix_data` 是 JSON 列,**无 schema 改动**);贯通 读取(含旧格式)/保存/空行过滤/汇总/导入预览;**生产通知单打印带出洗标号**(工厂据此区分标类——反馈的真正诉求),整列为空则不占版面;CSV 模板加「洗标号」列并**兼容旧模板**(据表头 `head[2]==='洗标号'` 自适应固定列数,已发出去的旧模板不作废)。验证:web build✓ vitest 100/100、api 221/221、portal✓。**已验**:`matrix_data` 里 article 的 JSON 透传——用【真实 ValidationPipe + 真实 DTO】跑通(`__tests__/matrix-article.pipe.spec.ts` 3 例:单行/多行/老订单无 article 向后兼容),证明 `whitelist:true` **不剥嵌套 JSON 的键**。注:本想在生产做读改验还原的往返测试,但订单**没有删除接口**、建测试单会在生产订单列表永久留垃圾数据并占号,遂改为在管道层验证——风险点(whitelist 洗字段)本就与库无关,MySQL JSON 列存任意 JSON 风险极低。
- （反馈导出内联图片 + 表格操作列样式）`fix(feedback/ui)` ①**反馈导出 HTML 图片裂图**(用户反馈):`images` 存的是 `/api/v1/uploads/file?p=<相对路径>`——**相对地址 + 要 JWT 的接口**,导出件下载到本地用 `file://` 打开必然裂(解析成 `file:///api/v1/...`),改绝对地址也过不了鉴权。改为**读盘转 base64 内联**(复用 `FileService.resolvePath` 的防目录穿越),导出件自包含离线可看;单图 >2MB 或读不到**退回可点链接**不让整个导出失败,非图片(pdf/xlsx)保持链接。`FeedbackModule` 新 import `FileModule`。②**表格操作列排布**(用户截图反馈"太丑"):病根是 `el-dropdown`/`el-popconfirm` 包装元素打断了 Element Plus 的 `.el-button + .el-button` 边距选择器 → 间距忽有忽无 + inline-block 基线错位;theme.css 加 `.table-ops`(flex + gap 接管排布),订单/工厂/客户三个列表页套用;**颜色收归设计系统**:日常操作(编辑/查看/打印/复制/授权/生成合同)统一主色靛蓝,色彩只留语义(删除=危险红、审批=成功绿)——原先复制=info、授权/生成合同=warning 是拿警示色当装饰。无 schema 改动。验证:api build✓ jest 221/221、web build✓ vitest 91/91。
- （生产浏览器验收 E2E）`test(e2e)` 新增**打已部署环境的只读 Playwright 验收**:`packages/e2e/playwright.prod.config.ts`(testDir `./prod`,baseURL 取 `BASE_URL` 默认生产,**不拉本地 api/web、不走 global-setup 造数**)+ `prod/prod-01-picker-and-export.spec.ts` 4 例:①对账新建搜款号→选合同→断言带出**工厂名称**而非 `工厂#<id>` 回退;②付款新建工厂项**不存在 el-input-number**(裸 ID 已消灭)且下拉给的是名称;③样衣列表导出 Excel **真实下载**并校验 BOM/`mso-number-format`/款号/无乱码哨兵/旧数据不渲染寄样跟踪表;④**多轮分支用 `page.route` 拦截注入轮次**(生产样衣未填轮次,真 UI 无从触发;跑的仍是线上 bundle,**不往生产写任何数据**)校验轮次明细+件数合计 5+工价合计 150+单号回落首轮。跑法:`cd packages/e2e && npx playwright test -c playwright.prod.config.ts`(4/4 通过,对 `2957052` 生产实测)。踩坑:el-select 的 placeholder 渲染成独立 span 而非 input 属性,须用 `getByRole('combobox',{name})` 定位。**注:Claude in Chrome 扩展当时连不上(native host 挂在桌面 App 那条管道),故改用 Playwright——这条路不依赖扩展,更适合当发版门禁。**
- （修复导出 Excel 丢寄样多轮）`fix(sample)` 发版后端到端体检发现:#5 把寄样改多轮子表后 `summarizeRounds` **只回填 piece_count/labor_amount/labor_unit_price,不回填 `material_ship_no`/`return_no`/`ship_sample_date`**,而当晚新加的导出 Excel 只读这些旧单值列 → **走多轮流程录的样衣,导出的寄出单号/寄回单号/寄样日期全空,各轮尺码/单号/工价一条不出**(两功能相隔 18h 上线,中间没接上;生产现存 3 条样衣尚未填轮次,故未爆发)。修法:导出加「寄样跟踪」表(轮次/尺码/件数/寄出日期/寄出单号/寄回日期/工价单价/工价金额/备注 + 件数与工价合计);基本信息的寄样日期/寄出单号在旧列为空时回落首轮(单轮样衣观感与改版前一致);无轮次的旧数据仍走原单值列且不出该表。纯前端。**该工具此前零单测覆盖(bug 因此漏网)——补 `sampleExcel.spec.ts` 5 例**(BOM/款号防截断、多轮明细+合计、回落首轮、旧数据兼容、无材料占位)。验证:vitest 91/91(86→91)、vue-tsc✓。
- （付款新建两者都要 + 样衣导出 Excel）`feat(payment/sample)` ①**付款「两者都要」**(用户拍板):预付款/付款申请新建的「工厂」改 `FactorySelect`(无合同/通用路径)+ 新增可复用 `ContractPicker`(搜款号→选合同、带出工厂,有合同路径),两条路都能建单;两弹窗加 `destroy-on-close` 使选择器每次打开都刷新。②**样衣导出 Excel**(用户反馈):无第三方依赖生成 Excel 可打开的 HTML 表格(`.xls`;UTF-8 BOM 防乱码、`mso-number-format` 文本格式防款号被当数字截断),含基本信息+材料明细;样衣**列表每行** + **编辑/查看页**均加「导出Excel」。纯前端。验证:web vue-tsc✓ vitest 86/86。**至此对账/付款「工厂ID 搜不到」+ 样衣「Excel/文件下载」两组反馈全部落地**。

- （本次·对账单款号→选合同 + 对账/付款工厂改名称选择器）`feat(reconciliation/payment)` 用户反馈:对账/付款「工厂ID」是裸数字(用户不知道 ID→搜不到、建不了单)。①新增可复用组件 `FactorySelect`(按名称/编号搜工厂、存 ID);对账/付款列表筛选、对账「非合同」建单工厂均改用它。②对账「合同对账」建单改**款号驱动**(用户要求):输入款号→`GET /contracts/by-style`(新端点,复用 priceHint 的款号匹配:`style_nos LIKE` 或材料 `style_no`)列出该款所有合同→选中自动带出工厂+合同ID;出货明细行合同也从该款合同下拉选。验证:types+api 构建✓ jest 221/221 web vue-tsc✓ vitest 86/86。**`by-style` 查询本机无真库未实跑,经 deploy 落地验证**。**下一批**:付款新建「两者都要」(有合同走款号→合同/无合同走工厂选择器) + 样衣导出 Excel。

- （本次·样衣反馈第一步:附件/下载/尺码数量）`feat(sample)` 用户反馈两条(样衣管理),按用户「分两步」先做小改:①「图片信息」新增「资料附件」多文件字段(保留图1/2/3——下游报价/订单继承依赖;accept 图片/PDF/Excel、可多个;上传端点已放行 png/jpg/webp/pdf/xls/xlsx,**不含 Word**);②文件可下载:`FileUpload` 非图片点击直接下载、图片预览框加「下载」按钮(按文件名判类型,`<a download>` 同源强制下载);③样衣加「尺码/数量」字段(如 38 码 2 件)。schema:`sample_garment` +3 列(`sample_size`/`sample_qty`/`attachments`),`init.sql` + `gen-column-sync` 重生成(44 表/650 列,红线一);**本机无真库未实跑,经 `deploy.sh` 落地验证**。验证:types+api 构建✓ jest 221/221 web vue-tsc✓ vitest 86/86。反馈②的「多轮寄样追踪子表」(较大、且改「版师填件数+工价→自动生成对账单」计费逻辑)下一批做。

- （本次·生产反馈修复:问题反馈 Ctrl+V 粘不了截图）`fix(web)` 反馈悬浮件里 Ctrl+V 粘贴截图无效。根因:`FileUpload` 的 paste 绑在需先聚焦的 `.fu-wrap`(tabindex=0),而反馈弹窗焦点天然在「问题描述」文本框,粘贴事件到不了上传框。修复:`FileUpload` 加可选 `globalPaste` 模式(挂载时在 `document` 兜底监听 paste、不依赖焦点;事件标记去重防 wrap/document 双触发;加限额守卫);`FeedbackWidget` 启用 `global-paste` + 弹窗 `destroy-on-close`(监听随弹窗开关自动挂/卸,关掉后不再全局抢粘贴)。未启用该 prop 的其它上传处零回归。验证:web `vue-tsc --noEmit` 0 报错 + vitest 86/86;真实浏览器粘贴动作未自动化(图片剪贴板难自动化),建议部署后手工点一下或本地 dev 验。

- （本次·生产事故修复:建样衣 500）`fix(db)` 生产库 `sample_garment` 残留重构前(`e5f6e21`)孤儿列 `style_name VARCHAR(100) NOT NULL 无默认值`,现行代码 INSERT 只给 `style_no` → 建样衣(POST /samples)必 500(系统报错记录已抓到 16 次)。根因:`gen-column-sync.py` 只按 HEAD `init.sql` 补列、看不见「生产有、HEAD 无」的反向漂移孤儿列,故从未触及。修复:`hotfix-schema.sql` 手工迁移块加 `_i9_modify_col('sample_garment','style_name', 放宽为可空)`(仅列存在时执行/全新装机 no-op/幂等可重放);`init.sql` 不动(HEAD 本无此列)。git diff 重构前基线↔HEAD 核实:`style_name` 是 `sample_garment` 唯一 NOT-NULL 孤儿列(其余被删列均可空+有默认)、子表 `sample_material` 无孤儿列 → 放宽本列即彻底解锁建样衣。**本机无 MySQL/docker,真库未本地实跑;经 `deploy.sh`(升级前备份+应用 hotfix+关键列校验)落地验证**。系统性缺口:gen-column-sync 对反向漂移是盲的,待补(deep-test 回归用例 + 全表孤儿 NOT-NULL 列扫描)。

- （本次·业务规则灰提示 + 交互优化）①新增全局组件 RuleHint(浅灰信息条,关键短语加粗)+全局注册+components.d.ts 类型声明,把隐性业务规则显式呈现在界面:订单(数量不导入/供应商限库内/草稿才可改)、报价(样衣导入/超阈值审批/成单不可改)、样衣(品名不导入/参考价脱敏/寄出单号推版师)、合同(推送后锁定/三金=100%/敏感附件限授权/首次推送自动开门户+待定占位)、对账(同类型合同/补料并入/发票=对账额/超发复核填因)、付款(分批/水单必填/无合同银行款号)、结算(仅计已付/收汇汇率齐才出毛利/分批结算/红冲)、出口发票(一票多款/占比分摊)共12处;②MainLayout 内容区加路由淡入过渡(reduce-motion 降级)。web 构建绿 vitest 全通

- （本次·动态封面）登录页左侧品牌区升级为**服装制造主题动态封面**:Canvas 绘制会起伏的织物经纬网格 + 沿对角缝道 dash 位移的动态针脚(模拟缝纫走针,带走针高亮点)+ 缓缓上浮的线头微粒;叠加 logo 入场+浮动动效、6阶段流程(样衣→报价→订单→合同→对账→结算)药丸节点带脉冲墨绿点、标语「从一片布,到一张单,全程可控」;含 prefers-reduced-motion 降级(静态一帧)、resize 自适应、onBeforeUnmount 清理 RAF。web 构建绿 vitest 登录7/7

- （本次·DATEX 品牌化）客户提供真实品牌 DATEX(服装公司)——重新设计登录封面与全站标题:①从品牌图裁出全彩 logo(datex-logo.png)+方形标记(datex-mark.png)放两端 public;②登录页重设计:墨绿(#2E8B78)→炭黑(#23343A)渐变品牌区+织物经纬纹理+白卡承载真实logo+墨绿渐变登录按钮+DATEX字标,输入聚焦墨绿;③浏览器标题 web「DATEX 服装智造管理系统」/门户「DATEX 供应商门户」+favicon 换 datex-mark;④侧栏品牌 I9→DATEX标记+字标;⑤门户登录页加DATEX品牌头;⑥仪表盘/打印兜底公司名/登录测试文案同步。web/portal 双构建绿 vitest 登录7/7

- （本次·清库脚本）新增 `infra/scripts/clean-db.sh`——交付前清空业务/测试数据:保留 sys_user/sys_config/sys_dict/company_profile,清空其余39张业务+主数据表(客户/工厂/样衣/报价/订单/合同/对账/付款/结算/发票…),重置 sys_sequence(单据号重头);安全三重:①执行前强制整库备份到 /opt/i9/backups ②须输入 CLEAN 或 --yes 确认 ③动态"全表减保留表"防漏。用法 `bash infra/scripts/clean-db.sh [--yes] [--clear-dicts]`。已部署生产(未执行,待用户交付前自行运行)

- （本次·前端深度测试后修复7项,已上线 fb8a049）真 Chromium 打生产全按钮/全流程/全逻辑测试(A按钮事件31/32+B黄金主流程真点按钮走通报价转合同→订单下单→生成材料合同+待定占位→推送门户+自动开门户账号s000+C业务逻辑守卫5/6,零JS报错),修复7问题:①本司主体名称乱码(生产DB双编码→utf8mb4+hex 修为「I9 服装制造有限公司」;init.sql seed 本正确);②错误提示双弹+误导(新增全局 errToast 800ms同文案去重,14视图 .response.data.message→.msg 并统一走 errToast);③时间未格式化(新增 utils/format.ts fmtDateTime/fmtDate,付款/对账/结算 创建时间 UTC→本地);④裸ID换名称(对账/付款列表后端补 factory_name/contract_no);⑤状态列被横滚埋没(订单/样衣/结算/对账 状态列 fixed=right);⑥非草稿订单只读(编辑页强制readonly+列表隐藏编辑);⑦报价列表加查看按钮。jest 221/221 vitest 86/86 双构建绿;真栈验证:公司名/工厂名/时间格式 UI 生效。生产测试数据(客户103/报价33/订单110/合同67/门户s000)+全库种子数据待清库

- （本次·样衣行级生成采购,最后悬案清零）`feat(sample)` 用户拍板 B 方案:样衣稿🟠「生成材料订单」行级按钮落地为**无合同费用对账单**(打样材料采购小额零星,不走合同/门户盖章全套):POST /samples/:id/materials/:materialId/purchase——金额=数量×参考价,供应商带入(须落工厂库),生成 NO_CONTRACT/EXPENSE 对账单(DZ-款号-序号)+费用明细行,直接进 对账→付款 链;缺 数量/参考价/供应商 各自拦截(400);样衣变更记录 PURCHASE 留痕。web 样衣编辑材料表加「🟠生成采购」行操作列(业务视图/已存行可用,弹窗确认金额)。jest 221/221 vitest 86/86;真栈E2E:1.2×28.5→对账单34.2/费用行落库/缺价行400✓。**至此总览走查全部条目(含结算深水区 Q3/Q12/Q18 + 三项决议矛盾)全部清零,无功能遗留**(仅费用阶梯计费待费率表)

- （本次·结算深水区全按推荐项）`feat(invoice/settlement)` 用户拍板"全部按推荐项开发"(结算清单 Q3/Q12/Q18+补料并入):①**Q12 出口发票/收汇子模块**——新表 export_invoice(票头)/export_invoice_item(款项行,一票多款)/invoice_receipt(逐笔收汇,多汇率+水单);/export-invoices CRUD+收汇登记+票号查重;web 新菜单页「出口发票」(登记/款项行占比/逐笔收汇/待收汇差额);②**Q3 拼柜分摊**——结算「同步发票收汇」:按本订单在各发票款项行金额占比,把逐笔收汇分摊成结算收汇行(source=INVOICE+票号留痕,汇率随笔;重拉整组重建不翻倍,手录行保留);③**Q18 分批结算**——settlement 唯一键 uk_order 删除(hotfix 手工段幂等迁移,允许一单多结算)+新列 shipment_ids 圈定出货批(建单勾选批次,件数=所选批合计,刷新尊重圈定)+「款号累计视图」(GET /settlements/aggregate:累计出货/货款/收汇/毛利净利+待齐备单数+逐单列表);④**补料对账并入原合同**——CreateReconciliationDto+merge_into_parent(仅 SUPPLEMENT 且有母合同可勾,对账挂母合同名下),web 建单勾选框。schema:3新表+3列+1索引迁移(44表/647列,真库升级+幂等✓);jest 221/221 vitest 86/86;真栈E2E 15/15:一票两款1000/两笔多汇率/60%分摊600→结算4260加权7.1/重拉幂等/同单两结算/累计视图/并入母合同/非补料拒✓。阶梯计费仍待费率表(唯一遗留)

- （本次·待办清单收官）docs/待办清单-总览走查.html 升 **v2.0**:顶部新增「施工结果」节——43组全清零×11提交对照表、遗留边界如实声明(拼柜分摊/出口发票子模块/结算-出货批关联/补料并入/阶梯计费/利润率规划表/锁汇留痕/盖章撤销审批/门户二次验证/供应商分级——深水细则;通知渠道/CA 外部依赖)、三项决议矛盾的施工取向(1:N+占用中/不回写+源订单已变更标记/行级材料订单待澄清)、三个审批触发器默认关闭说明与门户初始密码提示

- （本次·P3长尾包）`feat(全模块)` 总览走查 #36-43 落地:①**佣金**——customer +commission_rate(客户档案字段),建单自动带出;下单审批扩展:佣金>阈值(approval.order.commission_threshold)或USD单(approval.order.usd_required)触发,系统参数默认0=不启用;②**尺码/颜色字典**——订单矩阵行改 DictSelect(color/size 自填累积);**字典维护管理页**(/dicts,管理员菜单,14类含收货人/目的地台账);③**结算深水**——期间费用归集四项分列(运杂/快邮/打样/其它按关键词入列,不再整笔落其它);退税「到账确认」端点+UI(可实到金额覆盖+留痕);成本行允许负数(退运/索赔冲减);④**付款补齐**——无合同付款+收款银行/账号/相关款号,BUSINESS 可发起,FINANCE 可审批,付款水单强制必填(登记/标记两路),「付款日」检索维度,预付登记+款号;⑤**合同小项**——未匹配供应商自动挂「待定供应商」占位合同(不再丢行);历史同款价提示端点+编辑页🕘按钮带入;首次推送自动开通门户账号(工厂编号做用户名,初始密码 Factory@123);报价明细列序品名在前;⑥**治理**——报价利润率下限审批(quote.min_profit_rate,默认0);⑦**迁移**——order_main +external_no;在产订单导入(POST /orders/import,CSV粘贴,状态可指定)+报价历史导入(POST /quotes/import),列表页管理员「历史导入」弹窗。schema:4列(41表/623列,真库升级+幂等✓);jest 221/221(contract/order/payment spec 适配) vitest 86/86;真栈E2E 16/16✓。**未竟边界(如实)**:拼柜分摊/出口发票子模块/结算-出货批关联/补料并入原合同/阶梯计费/利润率规划表/锁汇留痕/盖章撤销审批/门户二次验证/供应商分级显示——深水区细则,见待办清单 v2.0 遗留节

- （本次·P3打印/附件链/订单增强）`feat(order/quote/stats)` 总览走查 #32-35:①**订单三套脱敏打印**(ORD E2)——utils/orderPrint.ts:对客确认单(无成本/供应商)/生产通知单(无客户/价格)/内部单据(全量),列表+编辑页打印下拉;**报价PDF增强**(rev G1-G3)——打印内容勾选对话框(内部留档/含款图/含费用明细),对外口径默认去客户信息与利润率行,含款图嵌样衣图;②**附件图片随单继承链**(qc I4)——样衣款图→报价 image1/2(importFromSample,已有不覆盖)→订单彩稿候选 att_artwork(importFromQuote);合同材料照片无上游源(已知边界);③**订单复制**(qc I5)——POST /orders/:id/copy 主表+用料+矩阵复制为新草稿(新单号/清审批/清同步标);**列表金额列**(ORD E6)+**订单维度统计**(ORD D8)——GET /stats/orders?dimension=po|customer|factory|currency,报表页新卡(单数/数量/金额);④**收货台账消费**(ORD B6)——订单 PO 列目的地/收货人改 DictSelect(destination/consignee 字典,自填累积复用带出)。jest 221/221 vitest 86/86(quotePrint spec 适配对外默认+新增 internal 用例);真栈验证:复制草稿✓/按币种聚合✓/样衣图→报价✓

- （本次·P3发货增强包）`feat(portal/contract)` 总览走查 #29/#30/#31:①**跨合同合并发货**(唯一整题未实现清零/门户C2/qc G1)——门户列表「合并发货」入口:勾多张已盖章/发货中合同+逐合同数量+一套物流,POST /portal/contracts/merge-ship 生成同 merge_no 组号批次(明细分摊到各合同,各批独立走审批/对账),web/门户批次均显合并组号;②**逐物料行发货**——新表 contract_shipment_item:门户发货可切「按物料行填写」,分行填实发数,批次数量=Σ行、金额=Σ(行单价×实发数)(比整单加权更准),批次行明细 web/门户回显;③**发货地址链**——contract_shipment +ship_address:合同编辑选厂自动带工厂地址(为空时),门户发货弹窗预填合同发货地址可临时改并留痕(与默认地址不同时日志注记)。发货核心重构为 createShipmentBatch 共用(单发/合并同一路径)。schema:2列+1新表(41表/617列,真库✓);jest 221/221 vitest 85/85;真栈E2E 10/10:逐行130件/金额500精确/临时地址/默认地址/合并组两合同分摊/少于2张拒✓

- （本次·P2列表体验包）`feat(settlement/portal)` 总览走查 #26/#27:**结算列表**——「待收汇N/亏损预警N/待重算N」徽标(GET /settlements/stats)+品名/订单数/出货数/不含税货款/收汇$/毛利毛利率列+「亏损」红标+「尚有N笔未付」「待船务出货」行内提示+高级筛选(仅看亏损/仅看待重算,DTO白名单);结算单建单快照 style_name/order_qty;成本明细行补实发数(对账批次合计)+加权单价。**门户列表**——卡片显款号(style_nos)+「进入处理/查看」按钮区分;列表按待办优先排序(待盖章>待发货>待对账>待开票>已完成,FIELD 排序);**门户详情**——渲染数量搭配尺码矩阵(色/码/PO表格,横向滚动)+包装资料/彩稿附件入口(后端早已返回,门户A2补渲染)。schema:settlement +style_name/order_qty,settlement_cost +qty/unit_price(40表/608列,真库✓)。jest 221/221(settlement/portal spec 适配 qb/query mock) vitest 85/85;真栈验证:stats徽标/loss筛选/排序生效/快照带出/成本行500×10✓

- （本次·P2标记留痕包）`feat(全模块)` 总览走查 #20-25/#28:①**变更标记5类**——报价内容改→订单「源报价已变更」黄条(content_updated_at vs quote_synced_at);订单内容改→合同「源订单已变更」;草稿订单引用→报价「占用中」+被引用订单号列表(可点跳转);报价行耗用≠样衣实测→「已偏离样衣」;样衣未实测→报价行「单耗为预估」+订单页黄条;②**改值留痕**——新全局表 change_log(原值X→改为Y)+GET /change-logs:报价(汇率/利润率/数量)、合同(数量/总额微调)、结算(汇率/发票/收汇/费用/退税)全挂钩,结算详情有变更记录区;③**软锁+版本+红冲**——settlement.needs_recalc:上游对账确认/付款PAID后自动标「待重算」(列表标签+详情横幅),刷新付款汇总清标;红冲重开 PATCH /settlements/:id/reopen(仅管理员,版本快照留 REOPEN 痕,回草稿重算);④**对账细节**——列表+发货批次N批列,详情头+款号,超发放行原因展示;⑤**样衣废弃态**——SampleStatus+ABANDONED(不删改废弃,下游留快照,已成单不可废,废弃后状态守卫禁编辑);⑥**超发确认移到对账**——门户发货超合同量直接放行(日志注记),对账复核确认时超发须业务填原因(rec.over_reason 留痕,web 弹窗引导);客户调整状态转单提示后放行。schema:change_log 新表+5列+样衣枚举扩值(40表/604列,真库验证✓);jest 221/221(5个spec适配) vitest 85/85;真栈E2E 25/25✓

- （本次·P1-A项三连）`feat(sample/order/contract)` 总览走查 #11/#17/#18(用户已拍板):**A1 样衣改材料→同步未成单报价**——QuoteService.syncFromSample:按品名匹配保留议价(人民币单价/损耗率/单位/备注沿用),耗用/颜色/供应商随样衣刷新,已成单(ORDERED)不动,金额变化清审批;业务编辑材料与版师实测耗用两条路径均触发。**A2 机密名称遮蔽**——订单/样衣列表与详情,未授权用户看到的中间商/买家名称快照显示「🔒 机密」(行级可见性=创建人+被授权人+管理员,复用 customer_grant);OrderModule/SampleModule 接入 CustomerService。**A3 电子章体系**——company_profile/factory +seal_url(基础资料页可传章图);合同 PDF 落款自动贴双方电子章(供应商电子盖章后);门户盖章支持「纸质盖章照片」上传二选一,contract +stamp_mode/stamp_paper_url 留痕(上传人/时间沿用 stamped_by/at),web 详情显示盖章方式+照片。schema 4列(gen-column-sync 重跑,真库升级✓)。jest 221/221(order/sample spec 补 mock) vitest 85/85;真栈E2E 15/15✓

- （本次·P1订单一致性）`fix(order)` 总览走查 #10/#12/#13:①客户改名 C1 同步补齐订单快照——middleman_name/buyer_name 在 草稿/已下单 同步,已生成合同(CONTRACTED)起冻结;②报价转销售合同自动建订单不再带入报价数量(qty_total=0,留空强制补尺码矩阵,矩阵合计回填);③订单材料供应商下拉去 allow-create(只可从工厂库点选)+限面/辅料(factories/select 支持逗号分隔多类型,主/附身份均命中)。jest 221/221(factory spec 适配+新增多类型用例) vitest 85/85;真栈E2E 8/8:三态订单改名同步/冻结、转单qty=0、多类型下拉✓

- （本次·P1门户闭环）`feat(portal)` 总览走查 #8/#9/#16:①**对账退回闭环**——整单退回时自动释放占用批次(供应商可重勾重发起),退回留痕单不可再提交(防同批次双计费);门户详情回显对账单列表+退回批注红条;②**发货完成→已完成**——新门户动作「发货完成」(SHIPPING/RECONCILED 可宣布,ship_done_at 留痕);开票后 已宣布→COMPLETED(新枚举值,门户「已完成」tab/标签),未宣布→回 SHIPPING 允许续批再对账(顺带修复"开票后停在RECONCILED无法续批"断链);③**撤回发货批次**——未被对账占用前门户可撤,累计发货量回退+日志;④**发货必填**——实发数量+快递公司/单号 DTO+服务端+H5三层必填;web 批次审批表补附件列。schema:contract +ship_done_at, portal_status 枚举+COMPLETED(gen-column-sync 重跑,本地真库枚举扩值✓)。jest 220/220(改3处spec适配) vitest 85/85;真栈E2E 22/22:必填拦截/两批发货/撤回回退/审批→对账占用→退回释放→批注回显→留痕单拒提→重发起→确认→发货完成→开票→COMPLETED✓

- （本次·P0#6 订单生成合同入口）`feat(order)` 订单列表操作列+订单编辑页工具栏均加「生成合同」下拉(已下单/已生成合同/生产中可用):①材料合同——调既有 generate-from-order 按供应商拆单批量建草稿,弹窗确认+未匹配供应商清单提示+跳合同列表;②加工合同——跳 /contracts/new?type=PROCESS&order_id= 带入订单明细(复用既有从订单带入)。web contractApi +generateFromOrder。真栈验证:2供应商(1入库/1未入库)→created:1+unmatched:['某供应商']✓;vue-tsc/构建绿

- （本次·P0安全包）`fix(security)` 总览走查 #5/#7:①**门户不泄露采购单价**——加工合同 orderDetail.materials API 层剥离 unit_price/budget(工厂只见品名/耗用/尺寸等工艺字段);②**敏感附件访问控制**——上传带 ?sensitive=1 落 uploads/private/ 子目录,读取须 HMAC 短时令牌(5分钟);新端点 GET /uploads/sign(JWT)签发带令牌链接;web FileUpload +sensitive 属性(缩略图/预览自动换签名链接,v-model 始终存原始URL)+utils/secureFile.ts(openFile/signedUrl);已接入四处敏感上传:担保人身份证/付款水单/结算收汇水单/门户发票。存量公共文件不受影响(能力URL不变);历史已传敏感件仍在公共目录(已知边界,未迁移)。真栈E2E 12/12:公共可读/敏感403/伪造令牌403/签名端点401门禁/带令牌200/门户材料无单价字段✓

- （本次·P0结算包+P1结算三项）`feat(settlement)` 总览走查待办 #1-4+#14/15/19 一体落地:①**编辑通道**——PATCH /settlements/:id(草稿限定,财务两步走:建单→收汇后补汇率/发票/费用)+成本行/收汇行删除端点+web编辑弹窗;②**刷新付款汇总**前端按钮(重取出货件数随船务更新+AUTO成本快照行整组重建);③**总货款仅计已付款(PAID)**——已确认未付单独落 unpaid_goods_tax/unpaid_count,成本行灰显「未付·不计入」(结算Q8);④**同款多订单不再重复背成本**——按【订单→合同→对账】圈定聚合(无合同历史数据按款号兜底),期间费用同款多单按出货份额分摊(结算Q1);⑤**逐笔收汇×各自汇率**——settlement_receipt +exchange_rate/slip_url,各笔齐备时结算金额=Σ(金额×汇率)、头上汇率=加权平均(结算Q2/Q13);⑥**必填闸门**——收汇/汇率不齐 profit_ready=0:不出误导性负毛利、不可确认(结算稿C/E);⑦refreshCost 按各行税率换算不含税(弃一刀切÷1.13,结算Q10),财务费率 7%→SysConfig(finance_fee_rate)。schema:settlement +3列/settlement_cost +5列/settlement_receipt +2列(init.sql+gen-column-sync 重跑,红线一:本地真库升级✓幂等✓);jest 220/220✓ vitest 85/85✓ 真栈E2E冒烟22/22✓(建单PAID口径/闸门400/编辑重算/逐笔汇率/覆盖拦截/刷新重聚/删行回落/确认通过)

- （本次·总览全量走查)以《总览v1.0》内嵌17份文档为基准,四路核对~340条(三模块/订单/合同120题我方核+门户对账48题+结算稿57条首次专项+总纲修订115条,全部代码实证):主线九大主题全通;产出 docs/待办清单-总览走查.html——P0×7(结算无编辑通道/总货款计未付/同款多单成本重复背/门户泄露采购价/订单缺生成合同按钮/敏感附件无访问控制等)+P1×12+P2×9组+P3×15组+3项决议矛盾待拍板;走查中揪出并已修 HEAD 编译不过(53e5048)

- 9c872f7 三份稿差异全量清零(~40项):通用字典模块(自填累积)+报价14项(转合同自动建订单等)+样衣13项(意见附件/自动推送等)+基础资料19项(覆盖更新导入/授权有效期/C1改名同步/信用列/到期日筛选/交期偏移参数化);不修6项有据。门禁全绿+红线一diff空+E2E抽查✓

- （本次·样衣模块对齐设计稿 13 项）`feat(sample)` ①保存时填材料寄出单号自动推送版师(待派单→打样中);②制版师文本框改用户下拉(GET /auth/users?role= 扩 BUSINESS 可调,失败降级文本);③新增列 `sample_garment.feedback_attachments` VARCHAR(500) 样衣意见附件(init.sql+gen-column-sync 已重跑,业务/版师视图均可传);④版师视图隐藏材料「参考价格」+实际耗用全空保存软确认;⑤categories 后端必填;⑥版师列表默认只看自己名下+未指派;⑦列表加制单人列;⑧编辑页管理员删除按钮;⑨样衣制作单 A4 打印(`utils/samplePrint.ts`,脱敏不含参考价格,编辑页+列表入口);⑩变更记录全覆盖 CREATE/UPDATE/SHIP/COMPLETE/COPY;⑪高级筛选(款号/中间商/类别/制版师/制单人/制单·寄出日期范围);⑫历史样衣 CSV 导入(POST /samples/import,中间商按名精确匹配+材料分号拆行);⑬列表全列排序。web typecheck✓ vitest 85/85✓ api build✓ jest 213/213✓(**真库端到端未跑**,发版走 deploy.sh 幂等升级即补列)
- （本次·生产缺列事故根治）线上 8 张表大面积 `Unknown column` 500(报错记录功能抓到,`Customer.type`/`OrderMain.style_no`/`Factory.can_invoice` 等)——根因:`hotfix-schema.sql` 手工 delta 清单覆盖不全,生产库基线比清单更老。根治:新增 `infra/scripts/gen-column-sync.py` 从 HEAD init.sql 自动生成「全表补表+541列补齐+类型同步」AUTO 段 + 手工「枚举语义迁移」块(旧值→新值映射,BOTH 双身份回填 extra_types)。真库验证:**最老基线(8c46b21,23表)→hotfix→与 HEAD 全新库 diff(表/列/类型)为空 + 旧枚举数据迁移正确 + 二次幂等 + 对已是 HEAD 的库无害**;8 个生产报错请求本地全部 200。**服务器执行 `bash infra/scripts/deploy.sh` 即修复**。

- （本次·闭环+润色)`feat(portal)` 开票→自动生成付款申请(PENDING,幂等),打通 对账→开票→付款;`fix(web)` 脱敏字段显示「—」非0.00、5列表批量删二次确认。真栈验证,均已进 main(0abee64)
- （本次·新功能+审计补口）`feat` 用户反馈(悬浮件+提交+图片+admin管理/HTML导出)+ 系统报错自动记录(全局异常过滤器 APP_FILTER 自动归档、指纹去重、输入/输出上下文、admin专属、HTML导出排除已处理);`fix(api)` 审计补口:样衣状态机守卫/编辑丢字段、订单状态机(D1)/报价门槛(A7)、报价权限(F1)、工厂阻删补全下游、结算双重扣减、发票唯一索引;`fix(web)` 登录报错提示、菜单角色门控、Dashboard待审批计数。均真栈验证,单测 186/186
- （本次·阶段2起)`feat(portal)` 门户开票加财务管控:校验发票=对账金额(±0.01)+ 发票号查重 + 落到对账单(此前只写日志,金额拦截失效)。真栈验(supplier1)不一致拒/一致落库/重复拒;单测 186/186。**后续同闭环:开票→自动生成付款申请、门户「我要对账」步**
- （本次·阶段1续）数据/schema:`fix(api)` 结算总货款只计合同对账(消除无合同费用双重扣减致净利偏低,真栈验 goods=1000 非 1200);发票号唯一索引 uk_invoice_no(init.sql+hotfix `_i9_add_unique` 幂等迁移,真库验重复拒/多NULL放行/二次幂等)。单测 185/185
- （本次·阶段1）安全/数据 P0:`fix(api)` 上传端点 magic-bytes 校验+安全响应头(阻断存储型 XSS)；审批后改金额清 approval_status 防绕过；超付拦截事务内 pessimistic_write；reconciliation/settlement/payment/portal 分页钳制。均真栈验证,单测 185/185
- （本次·阶段0）准出审查 + P0 修复：`fix(api)` 编号从库取基线消除撞 S001；`fix(web)` 列表响应契约对齐+编辑页 size+客户银行子表映射（全站列表恒空/编辑页 400 已解）；`chore(devtools)` macOS 免 Docker 本地全栈 + Playwright 冒烟。分支 `claude/fix-stage0-p0`
- `30e70d1` docs：新增 HANDOFF.md + pre-commit 钩子（每次变更强制更新交接文档）
- `30e70d1` docs：新增 HANDOFF.md + pre-commit 钩子（每次变更强制更新交接文档）
- `860a0c3` docs：落盘全量项目记忆 `docs/项目记忆.md`
- `009d4c8` docs：CLAUDE.md 落盘发版结论（一键发版/Redis 退路/待办）
- `8ed94cb` feat(ops)：`deploy.sh` 改一键部署（备份+幂等结构升级+就绪自愈+回滚提示）
- `4c0a40c` fix：真机深测修复——补料合同号超列宽致 500 + deep-test 断言纠偏
- `3409dc4` fix(ops)：存量库结构热修复 `hotfix-schema.sql` + `upgrade-db.sh`（止血 Unknown column）
- `eedf30c`…`55bd3b5` 验收审计补口 14 项（结算串流程/合同·对账/合同·门户/付款）

> 追加变更时在最前面加一行 `<commit> <一句话>`，并同步「一句话现状/立即待办」。
