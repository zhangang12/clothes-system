upstream api {
    server 127.0.0.1:3000;
    keepalive 32;
}

# HTTP — 生产环境应启用下方 HTTPS server 块后将此块改为全部跳转
server {
    listen 80;
    listen [::]:80;
    server_name _;

    # 配置好 SSL 证书后取消注释以下行并启用下方 HTTPS 块
    # return 301 https://$host$request_uri;

    client_max_body_size 20M;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/javascript;

    # API 反向代理
    location /api/ {
        proxy_pass         http://api;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 30s;
    }

    # 文件访问（通过 X-Accel-Redirect 鉴权后才可访问）
    location /internal/files/ {
        internal;
        alias /data/uploads/;
        expires 1h;
        add_header Cache-Control "private";
    }

    # 供应商 H5 门户
    location /portal/ {
        root   /var/www;
        index  index.html;
        try_files $uri $uri/ /portal/index.html;
        add_header Cache-Control "no-cache";
    }

    # 管理后台 SPA
    location / {
        root   /var/www/web;
        index  index.html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";

        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}

# =============================================================================
# HTTPS server 块（配置 SSL 证书后取消注释，同时将上方 HTTP 块改为仅做 301 跳转）
# 证书推荐使用 certbot：certbot --nginx -d your-domain.com
# =============================================================================
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name your-domain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#     ssl_session_cache   shared:SSL:10m;
#     ssl_session_timeout 10m;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     client_max_body_size 20M;
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/javascript;
#
#     location /api/ {
#         proxy_pass         http://api;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade $http_upgrade;
#         proxy_set_header   Connection 'upgrade';
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 30s;
#     }
#
#     location /internal/files/ {
#         internal;
#         alias /data/uploads/;
#         expires 1h;
#         add_header Cache-Control "private";
#     }
#
#     location /portal/ {
#         root   /var/www;
#         index  index.html;
#         try_files $uri $uri/ /portal/index.html;
#         add_header Cache-Control "no-cache";
#     }
#
#     location / {
#         root   /var/www/web;
#         index  index.html;
#         try_files $uri $uri/ /index.html;
#         add_header Cache-Control "no-cache";
#
#         location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
#             expires 1y;
#             add_header Cache-Control "public, immutable";
#         }
#     }
# }
